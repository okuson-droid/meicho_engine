# Memory

> **未着手の作業は `TASKS.md` に一元化してある**（2026-09-10）。現在地を知りたいときは最初にそこを読む。
> `engine/` の文書は記録（数字・判断の理由）、`TASKS.md` は状態（いま何をするか）。
> **★2 つのチャット（エンジン／オンライン対戦アプリ）が並行している（2026-09-20）。最初に `LANES.md` を読む。**1 つのファイルの書き手は 1 つの持ち場だけ。このファイルはエンジンの持ち場が書く。
> **正本は PC のこのフォルダである**（2026-09-15 裁定）。GitHub は貼るための置き場であって正本ではない（`GITHUB_SETUP.md` §7）。

## Me
オクソン（マスター）。TCG『鳴潮：対決』の解析エンジン `meicho_engine` を個人開発。Claude（クロエ）を引継ぎ書の書き手・設計の相談役として使う。**2026-09-15 に別セッションの作業者（Codex）との連携を中断したので、実装と静的照合もクロエが行い、検査とビルドの実行だけをマスターの PC に依頼する体制になった。**

## People
| Who | Role |
|-----|------|
| **マスター** | オクソン本人。裁定者。提案は原則すべて推しを採用（D-069）。対人局の相手役でもある。**PC でしかできない作業（Rust のビルド・全体検査の実行）の実行者** |
| **クロエ** | Claude（このチャット）。引継ぎ書を書き、設計を相談し、**実装と静的照合も行う**。前の便の結果を JSON で照合してから次の 1 便だけ書く |
| **作業者** | 実装・測定を行っていた別の Claude／Codex セッション。**2026-09-15 に連携を中断したので現在は動いていない**（`WORKER_HANDOFF_*` 系統は止まっている） |

## Terms
| Term | Meaning |
|------|---------|
| 便 | 引継ぎ書 1 通ぶんの作業単位（便 A〜G・便 M など）。1 便で変える探索器の要素は 1 つ |
| champion | そのカードプールでいちばん強い AI。正本 `experiments/champion.py`。現在 **`planner_vc4cps_kheb_b75`**（SD001・2026-09-13 交代・D-082 追記 2。**以後の便では交代していない**） |
| 門番 | champion 交代の直接対決。**n≥300 で勝率の 95% 下端 > 0.5**（2026-09-11 に D-034 改訂 1 を撤回して元に戻した・D-081）。GSPRT は診断として残す |
| 錨 | 第三者（H・貪欲・素 planner）との勝率。候補が別のところで弱くなっていないかの確認 |
| 帯 | シード帯。`experiments/seed_bands.json` に登録してから使う。現在値は `seed_bands.json` の `next_free` を読む（**844000**・2026-09-25。段階1B と公式ルール線では帯を使っていない） |
| 対照 / null | 同じ AI どうしを同じ帯で回した基準。0.5 を含めば配線は健全 |
| T-14 | 回帰局面の検査（対人局で AI が詰みを逃した対抗）。候補の最初の関門 |
| 対抗 | 対抗ステップ（互いに伏せてカードを出す同時手番） |
| 詰み表 | 後知恵（真の手札）で AI と相手の提出を総当たりした勝敗表 |
| 天井（M4） | 相手の手札が完全に見えたときの強さの上限。**+77.1 Elo [+57.2, +97.5]**（D-076） |
| lc | 葉の相関。詰み表の全マス目の結果が同じ割合。**0.105 と低い**＝最後の一手の読み合いで決まる |
| D_t / W_t | 曖昧さ解消率と、相手の手札としてありうる区別できる手札の数。**中盤の D_t の中央値 0.095** |
| rules 版 | `engine/rules_draft.md` の版。**現在 v0.19**（v0.12 → B-1/B-2/B-3 で v0.13 → A-1/A-2 で v0.14 → A-4/B-4 で v0.15 → A-3/B-9 で v0.16 → A-5/B-7 で v0.17 → A-6（回復にライフの上限は無い）で v0.18 → **TE-13（【切り替え】は入れ替わった 2 枠だけ）で v0.19**）。**作業規約 1 のとおり、実装より先に版を上げる**。**版を上げたら `meicho/version.py` の `RULES_VERSION` も同時に上げる**（番人 `tests/test_versions.py`・D-117。版の直書きはここ以外に置かない） |
| 符号化 | 観測・行動の符号化の版。**現在 v6**（D-124）。`meicho_rs.encoding_info()` = `(ENCODING_VERSION, OBS_DIM, ACT_DIM)` = **`(6, 1923, 317)`**（v5 の 1,825 列の末尾に信念の要約 20＋`hand_known` 78）、`ACT_CODE_LEN` 13。**`load_cards()` を先に呼ばないと `RuntimeError`** |
| A-n / B-n | 公式ルールと実装の差異の番号（`engine/official_rules/OFFICIAL_RULE_RECHECK_20260917.md`・D-092）。A は初回監査ぶん 6 件、B は再照合で新たに見つけた 6 件＋B-7〜B-9 |
| R-n | 上記の差異に対するマスターの裁定の番号（D-092 追記 1・追記 2） |
| D-番号 | `engine/decisions.md` の設計判断の番号。**最新 D-135。次に使えるのは D-136**（番号は `decisions.md` の末尾が正本）。**D-135** は段階3 の事前の確認（束ねた V は V 1 本 3 本の平均より +0.057 [+0.036, +0.078]・教師の葉は束ねた V のまま）と段階3 の設計書 `GENERALIST_STAGE3_DESIGN.md`（裁定待ち）。**D-134** は TE-13（【切り替え】は入れ替わった 2 枠のキャラだけが誘発する・rules v0.19・SD001/SD02 と champion の指紋は不変・SK 系の環境デッキは大きく変わる・D-131 の教材の SK 系の局は旧挙動）。**D-133** は段階3 の土台の V（V_id 3 本を幅 768 の 1 本に組み直したアンサンブル `results/models/s2v_id_ens3.json`・`experiments/ensemble_net.py`・葉の費用は V 1 本の約 3.3 倍）。**D-132** は段階2 の表現比較（乱数 0 番で V_pf が勝ったが、乱数 1・2 番で向きがそろわず不採用＝V_id が既定。学習の乱数だけで強さが ±0.03 動く・追記 5: 段階3 の土台の V は V_id 乱数 3 本のアンサンブル（幅 3 倍の 1 本に組み直す）・`experiments/card_profile_proj.py`・`eval_s2_repr.py`・記録の読み込みを 2 回読みに）。**D-131** は段階2 の横断教材の記録（学習 10,096 局・検証 1,088 局・`results/drl/s2_v1_*`・記録本体は作業環境だけ）。**D-130** は「打ち方の近さ」の診断（`experiments/diag_play_profile.py`・熾霞と今汐は打ち方が近い・分割は変えない）。**D-129** は段階2 の組み合わせ表の生成器（`experiments/make_s2_schedule.py`・学習 16 デッキだけ）と 1 デッキぶんの下見（追記 1: manifest の `games` を本当の局数に）。**D-128** は環境デッキ 24 種（クロエが構築・学習 16／調整 4／最終評価 4）。**D-127** はデッキ類似度の道具（`experiments/deck_similarity.py`・S3 は候補の平均を引いて比べる・idf と平均は候補だけ）。**D-126** は監査で採用した 2 件（マスター対 AI の直接対局を副次の基準に／`webapp` の引退・OneDrive の外へ・PC 側の Claude Code）と類似度の 3 層。**D-125** は検査の組（重い検査は既定で飛ばす・`--run-slow` で全部・`--pc` で PC の組だけ）。**D-124** は段階1C-c（符号化 v6: 信念の要約と統一した hand_known を末尾に・相手のデッキ表は引数で渡す・ネットは 0 列で移行）。**D-123** は段階1C-b（異種デッキの記録入口 `experiments/record_mix.py`・Rust の `opp_from_seat`・相乗りで `MAX_LIFE` を削除）。**D-122** は段階1C-a（`observe` の `hand_known` を統一した確かな既知に・旧来は `hand_known_scan`・D-023 の積の漏れを発見）。**D-121** は汎用 AI 段階1C の設計と裁定（`GENERALIST_STAGE1C_DESIGN.md`）。**D-120** は汎用 AI の費用分離（時間の 82% はネットの掛け算・`G1_COST_SPLIT_NOTES.md`）。**D-119** は便 C の判断 31 件を推しで閉じ、J-1〜J-6 を汎用 AI に寄せて採った（時間のかかる実行は相談）。**D-118** は便 R（公開して手札に入ったカード・TE-10 の漏れ）の診断と裁定——いまは実装せず、汎用 AI の入力の整備（段階2 より前）で入れる。**D-117** は rules の版を `meicho/version.py` の 1 か所に集めた（TE-3）と対人局の記録の `think_ms`（TE-4）。**D-115** は `m5m1_pimc` の基準の作り直し・**D-116** は PC の全検査が失敗 0 に戻った（985 通過・26 skip は全部 torch 無し）。**D-110〜D-112** は SD02 の強さの段階（TE-6・`heuristic`／`greedy`／`planner_lh`）・**D-113** は書き戻しのバイト比較必須・**D-114** は M1 のトレース点（`meicho/trace.py`）。**D-番号はエンジンの持ち場だけが振る**（`LANES.md` §3）。**D-109** は並行作業の決まり（`LANES.md`）への切り替え。**D-108**（追記 1〜5）はアプリの計画の経緯で、**以後のアプリの決定は APP-番号で `engine/app/DECISIONS_APP.md`**。それ以前の直近の並びは **D-099**（A-3/B-9・rules v0.16）・**D-100**（A-5/B-7・rules v0.17）・**D-101**（champion の指紋が B-9 で動いた。原因を B-9 単独に切り分けて貼り替え）・**D-102**（PC の全検査 955 通過/2 失敗）・**D-103**（BP01 の画像の検査を環境非依存に）・**D-104**（A-6・rules v0.18・基準 8 種の貼り替え）・**D-105**（`scripts/` の経路を conftest 1 か所に）・**D-106**（A-6 の取りこぼし。満タン＝0 回復を前提にした検査を直した）・**D-107**（裁定 2 件: `MAX_LIFE` を消す／champion の強さは測らない） |
| fingerprint | 既定の打ち方が不変であることの指紋（4 種）。**champion `planner_vc4cps_kheb_b75` は `f4b80b25c35cfa77`**。歴代は **`planner_vb3cps` `87988cc627c0b64e`／`planner_vc4cps` `ff72e98e303d87ae`／`planner_vc4cps_kheb` `b4d3b2a1986b71b9`**。**4 種とも 2026-09-19 に A-6（D-104）で貼り替え、同日 PC で一致を確認済み**（`check_champion_fingerprint.py` と pytest の 14 節点）。**指紋は打ち方を変えなくても動く**（決定列に選択が増えるだけ・ルールが変わるだけで動く・D-091 §3／D-104） |
| SD001 / SD02 | カードプール（構築済みデッキ）。学習した AI があるのは SD001 のみ |
| V / π₀ / 代打ち π | 葉の価値関数（V_4' が現 champion）／相手モデルの方策／先読み中の代打ち方策 |
| GSPRT | ペンタノミアル逐次検定（α=0.025・β=0.10・p₁=0.55・上限 2,400 局）。**2026-09-11 に門番から外れ、診断・事前のふるいになった**（D-081）。**「越えたか」だけに使い、大きさは固定 n で測る** |
| Kaggle | 1 時間を超える測定を回す外部資源（D-072）。マスターは未登録。**要るのは便 E 本体** |

## Projects
| Name | What |
|------|------|
| **文献活用計画** | `engine/LITERATURE_PLAN_20260906.md`（順序と経過は §3・§10）。便ごとの引継ぎ書は `engine/HANDOFF_<日付>_LIT_<便>.md`、結果は `LIT_NOTES.md` と decisions.md。**どこまで進んだかは `TASKS.md`**。便 A 後半（D-082）で閉じて以降、AI 線は止まっている |
| **公式ルール線** | 発売後の公式総合ルール・FAQ と実装の照合（`engine/official_rules/`）。**2026-09-17 に静的照合を完了し差異 12 件を確定**（`OFFICIAL_RULE_RECHECK_20260917.md`・D-092）、**2026-09-19 に 12 件すべて閉じた**（D-094〜D-096・D-093 追記 1・D-099・D-100・D-104。B-5 は抑制を入れないで決着・B-6 は実装しない）。**★A-2（D-095）と A-6（D-104）で SD001/SD02 の対局が変わった**＝便 A〜C の勝率と champion 交代の判定は**旧エンジンの測定**である |
| **段階1A / 段階1B** | 決定列に選択行動を足し（D-088）、符号化を v5 に上げた（D-089・D-091）。学習済みネット 12 本を移行済み（控え `results/models/*.json.enc4.bak.json` は**消さない**） |
| **カード線（便 K）** | BP01 のカードデータをエンジンに入れる線。**2026-09-13 に完全に閉じた**（D-083＋追記 1〜5・D-085）。閉じた時点は rules v0.12・符号化 v4 だったが、**その後 rules は v0.15・符号化は v5 に上がっている**。BP01 の 87 種がすべて揃っている |
| **オンライン対戦アプリ（アプリの持ち場）** | 別のチャットが開発する（D-108）。台帳 `engine/app/TASKS_APP.md`・決定 `engine/app/DECISIONS_APP.md`（APP-番号）・エンジンへの頼みごと `engine/app/TO_ENGINE.md`（こちらからは `engine/TO_APP.md`）。文書の正本は Claude Docs「鳴潮：対決 オンライン対戦アプリ プロジェクト計画書」と「同 要件定義書」。**エンジン本体（M1 の出来事列を含む）はエンジンの持ち場が実装する**（`LANES.md` §6） |
| **環境デッキ群とデッキ類似度** | 設計書 `engine/DECK_SIMILARITY_DESIGN.md`（D-126）。構成・キャラ・動きの 3 層で測り、似たデッキを学習と評価にまたがらせない。道具 `experiments/deck_similarity.py`（decksim-3・D-127/D-128）、閾値 `results/decksim/thresholds.json`（`th-1-provisional`・仮）。**環境デッキは 2026-09-24 にクロエが 24 種作った**（D-128）——`decklists/env/ENV_<GA>_<GB>_<GC>.json`、生成器 `experiments/make_env_decks.py`、検査 `tests/test_env_decks.py`。割り振りは学習 16・調整 4・最終評価 4（手割り・`results/decksim/env_v1_split.json`）|
| **配布版** | 知人向け Windows 実行ファイル（D-074）。マスターの PC で作る。`scripts/make_dist.py` の `verify` が**公式素材（`■【` の印）を配らないための番人**である（D-098 §2(a)） |
| **対人検証アプリ** | `engine/webapp/`。マスターが champion と対局し記録する。記録は `engine/results/human_games/2026-09.jsonl`。**`app_version < 2` の記録は段階1A の選択を持たないので `is_legacy_stage1a()` で補完する**（D-098 §2(b)） |
| **BP01 のカードデータ** | `cards/cards_official_20260910.json`（公式サイトから全 191 レコード取得・D-078）。**`adopted_effect` / `adopted_name` の欄があればそちらを採用**。経緯は `cards/OFFICIAL_FETCH_20260910.md`（未掲載だった 3 枚は 09-12 に掲載・`OFFICIAL_FETCH_20260912.md`）。**カード画像は 2026-09-13 にマスターの手動スクショへ切り替えた**（D-084）——正本は `cards/` 直下の **123 枚**（1 番号 1 ファイル）、パラレル 38 枚は `cards/parallel/`（索引に入らない）、旧 76 枚は `cards/_old_images/`、原本は `cards/手動スクショ/`。**実装カード 123 番号すべてに画像がある**（D-084 追記 1）。経緯は `cards/MANUAL_SHOTS_20260913.md` |

## Preferences
- 返事は簡潔・表なし（モバイル）。数字は基準・n・95% 区間つき
- 引継ぎ書は 1 便ずつ。前の便の結果を確認してから次を書く。計画は変えない（発売前でも）
- 提案はすべて推しを採用（包括方針 D-069）。判断が要る点は推測で埋めず列挙する
- 1,000 局ごとの定期報告。手を動かしてもらう依頼は手順を省略しない
- 正本は `engine/` の文書（rules_draft.md・decisions.md・各 NOTES）。詳しくは `memory/`

## 踏みやすい罠（毎回ここで転ぶ）
- **書き戻したファイルが PC に載っていないことがある（OneDrive）。**更新日時だけ進んで中身が古い。ビルドや検査を頼む前に `findstr /c:"<直した行の一部>" <ファイル>` で確かめる（D-091・D-084）
- **依頼書で出力をファイルに流す（`> x.txt 2>&1`）なら「画面には何も出ない」と先に書く**（D-116。マスターが止まりかけた）
- **`python scripts\xxx.py` の形で起動すると `meicho` が見つからない。**`engine` で `set PYTHONPATH=%CD%` を先に 1 回打つ。コンソールを開き直したら打ち直す
- **D 番号は別セッションと衝突する。**書き戻す前に PC の `decisions.md` の末尾を必ず見る（2026-09-11 に実際に衝突した）。**2026-09-20 からは D-番号を振るのはエンジンの持ち場だけ**（`LANES.md` §3）
- **「written」は載ったことを意味しない。**2026-09-20 のアプリのチャットでは `decisions.md` への書き戻し 6 回のうち 3 回が載らず、3 回とも 2 回目で載った（`engine/PARALLEL_WORK_REVIEW_20260920.md` §2）。**書き戻したら必ず再ステージして比べ、台帳類は `engine/WRITELOG_ENGINE.md` に控えを残す**（`LANES.md` §5）
- **skip は「通った」ではない。**作業環境で常に skip になる検査が PC で初めて落ちることがある
- **「基準が通った」は「その基準が変更点を踏んだ」を意味しない。**通った帯がその変更を一度も踏んでいないだけかもしれない（D-097・D-098 §5）
- **判定を写して 2 箇所に書くと、写した側だけが取り残される**（D-098 §5・D-075）
- **経路も判定である。**同じ 1 行を 4 か所に書いたら、いつか 1 か所が抜ける（`scripts/` を `sys.path` に入れる行・D-105）。**全検査が緑でも、名指しで回すと落ちる**——検査どうしが import の副作用で支え合っていることがある
- **★失敗一覧は数えるものではなく読むものである。**「資材が無いから」は仮説であって、1 件ずつ理由を見るまで結論ではない。**失敗が多い環境ほど本物の失敗が隠れる**（D-106 §3）
- **ルールを覆したら、覆した前提に寄りかかっている検査を『名前ではなく挙動で』探す。**A-6 では `grep MAX_LIFE` で探したため、`MAX_LIFE` を使わず「満タンから回復する」状況で同じ前提に乗っていた検査を取りこぼした（D-106 §6）
- **★`device_commit_files` が拒否したら、書き直すときは別の名前の `stagedPath` を使う**（D-127）。同じ `stagedPath` だと、拒否された回の中身が書かれることがある。書き戻したら必ず再ステージしてバイト比較する
- **★`device_stage_files` は `/mnt/user-data/uploads/…` の作業用の写しを PC の中身で上書きする**（D-128）。台帳を再ステージした瞬間に、まだ書き戻していない自分の直しが消える。**再ステージの前に、自分が直したファイルの控えを別の場所に取る**
- **★書き戻す前に元の改行コードを確かめる**（D-128）。PC では `decisions.md`・`TASKS.md`・`WRITELOG_ENGINE.md` が **CRLF**、`CLAUDE.md`・設計書・`.py`・`.json` が **LF** である。LF で書き直すとファイル全体が変わってしまう
- **★検査の回し方（D-125）**: 作業環境では毎便 `python3 -m pytest tests -q -rfs --run-slow`（約 40 分・既定は重い 20 件を飛ばして約 7 分）。PC への依頼は `python -m pytest tests --pc -q -rs`（PC にしか無い資材の検査と Windows の部品の検査だけ）。検査を足したら `--junitxml` の結果から `scripts/make_test_sets.py` で組を作り直す。**作業環境で `--run-slow` を回さなかった便は完了扱いにしない**
- **★符号化 v6（D-124）の信念の要約は `encode(ob, pi, opp_decklist)` の第 3 引数が無いと 0 になる**。葉の V（`PlannerAgent._eval`・Rust `Greedy::net_value`）と記録（`series_record`）は自分の `opp_decklist` を渡す。**方策ネット（代打ち・π₀・PolicyAgent）は渡していない**——π を v6 の記録で学ぶなら、使う側の口も揃えること
- **★違うデッキ同士の対局で `rs.series` 系を使うときは `opp_from_seat=True`**（D-123）。奇数シードで A/B の席が入れ替わるがデッキは席に固定なので、既定（False）では**半分の局で相手デッキ表が誤る**。段階2 の教材は `experiments/record_mix.py`（常に True）で作る
- **★`hand_known` と `hand_known_scan` は別物**（D-122）。`hand_known` は統一した確かな既知（汎用 AI・画面・監査が読む）、`hand_known_scan` は旧来のスキャン・B-9 のぶんで**現 champion と符号化 v5 だけが読む**。champion の既知を検査するときは `hand_known_scan` を見る
- **日本語 Windows の cp932 で道具が落ちることがある**（`π₀` の `₀` は cp932 で書けない・D-082 追記 1）
