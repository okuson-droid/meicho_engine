# Memory

> **未着手の作業は `TASKS.md` に一元化してある**（2026-09-10）。現在地を知りたいときは最初にそこを読む。
> `engine/` の文書は記録（数字・判断の理由）、`TASKS.md` は状態（いま何をするか）。

## Me
オクソン（マスター）。TCG『鳴潮：対決』の解析エンジン `meicho_engine` を個人開発。Claude（クロエ）を引継ぎ書の書き手・設計の相談役として使い、実装と測定は別セッション（作業者）に回す。

## People
| Who | Role |
|-----|------|
| **マスター** | オクソン本人。裁定者。提案は原則すべて推しを採用（D-069）。対人局の相手役でもある |
| **クロエ** | 引継ぎ書を書く役の Claude（このチャット）。前の便の結果を JSON で照合してから次の 1 便だけ書く |
| **作業者** | 実装・測定を行う別の Claude セッション。成果物は `Claude outputs/*.zip` か、PC の `engine/` への直接の書き戻しで届く |

## Terms
| Term | Meaning |
|------|---------|
| 便 | 引継ぎ書 1 通ぶんの作業単位（便 A〜G・便 M など）。1 便で変える探索器の要素は 1 つ |
| champion | そのカードプールでいちばん強い AI。正本 `experiments/champion.py`。現在 **`planner_vc4cps_kheb_b75`**（SD001・2026-09-13 交代・D-082 追記 2） |
| 門番 | champion 交代の直接対決。**n≥300 で勝率の 95% 下端 > 0.5**（2026-09-11 に D-034 改訂 1 を撤回して元に戻した・D-081）。GSPRT は診断として残す |
| 錨 | 第三者（H・貪欲・素 planner）との勝率。候補が別のところで弱くなっていないかの確認 |
| 帯 | シード帯。`experiments/seed_bands.json` に登録してから使う。現在値は `seed_bands.json` の `next_free` を読む（便 A 後半が 709000..714999 と便 K の 706000..708999 を登録済み・その次は **715000**） |
| 対照 / null | 同じ AI どうしを同じ帯で回した基準。0.5 を含めば配線は健全 |
| T-14 | 回帰局面の検査（対人局で AI が詰みを逃した対抗）。候補の最初の関門 |
| 対抗 | 対抗ステップ（互いに伏せてカードを出す同時手番） |
| 詰み表 | 後知恵（真の手札）で AI と相手の提出を総当たりした勝敗表 |
| 天井（M4） | 相手の手札が完全に見えたときの強さの上限。**+77.1 Elo [+57.2, +97.5]**（D-076） |
| lc | 葉の相関。詰み表の全マス目の結果が同じ割合。**0.105 と低い**＝最後の一手の読み合いで決まる |
| D_t / W_t | 曖昧さ解消率と、相手の手札としてありうる区別できる手札の数。**中盤の D_t の中央値 0.095** |
| D-番号 | `engine/decisions.md` の設計判断の番号。**最新 D-083＋追記 1〜5（便 K を PC に展開して閉じた・2026-09-13）。その前が D-082＋追記 1〜4（便 A 後半の報告／cp932 の直し／champion 交代／交代で落ちた検査の直し／PC の Rust 再ビルド。**便 A 後半はこれで閉じた**）。便 C の報告は D-077＋追記 1〜5 と D-081。**便 K は D-079＋追記 1〜9**（追記 8・9 は 9/12 の未掲載 3 枚と u19〜u21）。D-080 は §3.1-2 の「ちょうど3種類」の裁定。**D-084 はカード画像を手動スクショに切り替えた記録**（2026-09-13）。**D-085 は `BP01-057` と `BP01-062` の効果を入れた記録**（段 K-5・2026-09-13。語彙 3 つ・rules v0.12 のまま・符号化 v4 のまま）。次に使えるのは **D-086** |
| fingerprint | 既定の打ち方が不変であることの指紋（4 種。champion は **`e1662edb32b144a9`**・一つ前の `planner_vc4cps_kheb` は `e82b796960e04c4a`・そのもう一つ前の `planner_vc4cps` は `9b5ad48d2de6a7e0`） |
| SD001 / SD02 | カードプール（構築済みデッキ）。学習した AI があるのは SD001 のみ |
| V / π₀ / 代打ち π | 葉の価値関数（V_4' が現 champion）／相手モデルの方策／先読み中の代打ち方策 |
| GSPRT | ペンタノミアル逐次検定（α=0.025・β=0.10・p₁=0.55・上限 2,400 局）。**2026-09-11 に門番から外れ、診断・事前のふるいになった**（D-081）。**「越えたか」だけに使い、大きさは固定 n で測る** |
| Kaggle | 1 時間を超える測定を回す外部資源（D-072）。マスターは未登録。**要るのは便 E 本体** |

## Projects
| Name | What |
|------|------|
| **文献活用計画** | `engine/LITERATURE_PLAN_20260906.md`（順序と経過は §3・§10）。便ごとの引継ぎ書は `engine/HANDOFF_<日付>_LIT_<便>.md`、結果は `LIT_NOTES.md` と decisions.md。**どこまで進んだかは `TASKS.md`** |
| **カード線（便 K）** | BP01 のカードデータをエンジンに入れる線。AI 線（文献活用計画）とは独立。**2026-09-13 に完全に閉じた**（D-083＋追記 1〜5）——書き戻し・ネット移行 12 本・Rust 再ビルド・**全検査 774 通過／失敗 0**・`verify_report.py K` 行 30／不一致 0。**三者一致が名乗れる**。**rules v0.12・符号化 v4**（`NA` 78 / `NC` 45 / `OBS_DIM` 1,313 / `ACT_DIM` 226）。BP01 の 87 種がすべて揃った。**2026-09-13 に `BP01-057` と `BP01-062` の効果も入って完全に終わった**（段 K-5・D-085。T-K-10 の旗は外した。**PC の Rust 再ビルドも 2026-09-13 に済んだ**・wheel の札に `bp01_k5` が出る。古い wheel は新しい op で panic するので、この再ビルドは前提だった）|
| **配布版** | 知人向け Windows 実行ファイル（D-074）。マスターの PC で作る |
| **対人検証アプリ** | `engine/webapp/`。マスターが champion と対局し記録する |
| **BP01 のカードデータ** | `cards/cards_official_20260910.json`（公式サイトから全 191 レコード取得・D-078）。**`adopted_effect` / `adopted_name` の欄があればそちらを採用**。経緯は `cards/OFFICIAL_FETCH_20260910.md`（未掲載だった 3 枚は 09-12 に掲載・`OFFICIAL_FETCH_20260912.md`）。**カード画像は 2026-09-13 にマスターの手動スクショへ切り替えた**（D-084）——正本は `cards/` 直下の **123 枚**（1 番号 1 ファイル）、パラレル 38 枚は `cards/parallel/`（索引に入らない）、旧 76 枚は `cards/_old_images/`、原本は `cards/手動スクショ/`。**実装カード 123 番号すべてに画像がある**（D-084 追記 1。`cards/BP01_NO_IMAGE.json` の許容リストは空）。経緯は `cards/MANUAL_SHOTS_20260913.md` |

## Preferences
- 返事は簡潔・表なし（モバイル）。数字は基準・n・95% 区間つき
- 引継ぎ書は 1 便ずつ。前の便の結果を確認してから次を書く。計画は変えない（発売前でも）
- 提案はすべて推しを採用（包括方針 D-069）。判断が要る点は推測で埋めず列挙する
- 1,000 局ごとの定期報告。手を動かしてもらう依頼は手順を省略しない
- 正本は `engine/` の文書（rules_draft.md・decisions.md・各 NOTES）。詳しくは `memory/`
