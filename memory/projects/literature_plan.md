# 文献活用計画（`engine/LITERATURE_PLAN_20260906.md`）

- 順序: 診断 M1〜M8 → 便 D → 便 A → 便 B（A の後）→ 便 C（M4 で範囲）→ 便 E（Kaggle）→ 便 F → 便 G。
- 完了: 便 D 前半（D-070）・便 A 前半（D-071・lu50 は champion にしない）・便 E-0（D-073・champion 交代）・
  便 D 後半（D-075・GSPRT と Nash averaging）・**便 M（D-076・診断 M1〜M8 がこれで全部終わった）**。
- **便 M の分岐の答え 3 つ**:
  - **便 C の範囲 = 全部**（II-7・II-8・II-9・バケット化）。天井 Elo **+77.1 [+57.2, +97.5]**（n=1,200・境界でない）
  - **便 B（IIMC）の優先度 = 上げない**（便 A 後半の後）。中盤の D_t 中央値 **0.095**（閾値 0.30）
  - **便 E 反復 7' の磁石 = 要らない**（2 世代とも循環なし。ただし V_1 対 V_3 は上端 0.488 で際どい）
- そのほか便 M で分かったこと: **lc = 0.105 と低い**（II-7 の主役は本数増ではなく硬い制約フィルタ）／
  **探索が `hand_known` を使っていない**（決定の 29.2%・候補が 0.743 倍に縮む）／
  カンニング逆説は起きなかった／D-034 を改訂（門番を GSPRT に・ラダーの条件を Nash の台に）。
- **便 C の引継ぎ書は 2026-09-10 に書いた**（`engine/HANDOFF_20260910_LIT_C.md`・作業待ち・報告 D-077）: 階段で 1 段 1 つまみ——C-1 `known_hand`（＋被覆率ハーネス）→ C-2 `world_weight`（π₀ の到達確率・T=2.0・ε=0.2・一様分 1/4）→ 2×3 掃引 → C-3 `endgame_enum`（N=64・16 本・C=0.6）→ C-4 `draw_buckets` → 交代判定は最後に 1 回（ラダー v10 は Kaggle）。T-14 は診断扱い。帯 676000..705999。
- 便 M の照合で見つけた訂正: `hand_known` の効きは全決定平均 0.743 ではなく、**効く場面（29.2%）で W が 8〜20 分の 1**（平均 0.121・中央値 0.047）。
- **次にやることは `TASKS.md` を見る。**
