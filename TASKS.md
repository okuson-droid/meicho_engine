# Tasks

> **未着手の作業はここに一元化する**（2026-09-10）。`engine/` の文書は**記録**（何をなぜ決めたか・数字）で、
> ここは**状態**（いま誰が何をするか・終わったか）。同じ作業を両方に書かない。
> 設計の順序（便 D → A → B → C → E → F → G）は `engine/LITERATURE_PLAN_20260906.md` §3 が正本で、
> これは作業リストではないので重複ではない。

## Active

- [ ] **Kaggle の登録と初回セットアップ** - `engine/COMPUTE_PLAN_20260908.md` §4.1〜4.3。**要る理由は便 E 本体だけになった**——ラダーは `--reuse-from` で 3 時間強に縮んだので（D-077 追記 5・実測 3 時間 11 分）、交代判定のために Kaggle を待つ必要はもう無い
- [ ] **配布版（Windows 実行ファイル）の作成** - `engine/HANDOFF_20260908_DIST.md`。マスターの PC での作業
- [ ] **対人局を続ける（SD001 のみ）** - 相手は champion **`planner_vc4cps_kheb_b75`**（2026-09-13 交代・D-082 追記 2。アプリの既定も変えてある。**一つ前の `planner_vc4cps_kheb` も選べる**。**一つ前の `planner_vc4cps` も選べる**ので、過去の 4 局と同じ相手でも比べられる）。記録は `engine/results/human_games/2026-09.jsonl`。**2026-09-08 の 4 局から増えていない**

## Waiting On

- [ ] **★自動選択にしてある選択を選択肢化する（便 K の外・`ENCODING_VERSION` 5 の枠）** - D-079 追記 5・6。K-3 の妨害効果（相手の協奏エリア／トラッシュから選ぶ）と **K-4 の回収先・置く札**が、いまは「トラッシュの左端から自動」である。**K-4 のほうは戦略的な意味が大きい**（どの札を回収するかは手の強さに効く）。選択肢を 1 つ足すと `encode.CHOICE_KINDS` が伸びて `N_SCALAR` が変わり、符号化の版上げとネット移行がもう一度要るので、**新しい選択をまとめて 1 回の版上げにする**のが推し。作業規約 7（D-004 の自動選択は暫定）の枠

- [ ] **BP01 の未定義事項 u1〜u18 を発売後の公式ルールで確かめて `unverified` の旗を外す** - **正本は `engine/rules_draft.md` §9-6 の表**（v0.12 に取り込み済み）。**2026-09-10 にマスターが 18 件すべて裁定済み（D-079 追記 1・追記 4）＝実装の解釈は確定**しており、K-3〜K-4 はそのまま書けばよい。**u18（「相手にダメージを与えた時」の範囲＝そのカード自身が与えたときだけ）は K-2 で見つかった新規**。**旗を残すのは「出所が公式テキストではなく我々の読み」だから**で、裁定待ちという意味ではない。確かめる相手は同梱マニュアルと公式 Q&A（発売 2026-09-12）。u17 だけは裁定ではなく便 G への申し送り（＜音骸＞は共通カードなので SD001/SD02 に足した構築が環境に出うる）
- [ ] **★輪 2 の探索器を champion に揃えるか（マスターの裁定・便 E 本体の前に）** - 便 C の交代で champion に `known_hand` / `endgame_enum` / `draw_buckets` が増えたが `vb.py` は変えていないので、**いま輪 2 は一つ前の champion の探索器で回る**（D-081 追記 1）。揃えずに回すと「champion とは違う探索器が作った記録で champion の葉を学ぶ」ことになる。**推し: 便 E 本体に入る前に揃える**（1 便で変える要素は 1 つ、の原則からは 「輪を回す便」で揃えるのが自然）。急がないが、便 E の前には決めること
- [ ] **便 C の判断が要る点の裁定（31 件）** - D-077・D-081。段 C-0 の 4 件（`test_lit_m.py` の `next_free` を `>=` に／TSSR はプール比／被覆率の対局は現 champion ミラーに固定／`host_name()` は知らない値を拒否）＋ 段 C-1 の 2 件（覗き見監査の「隠蔽情報」を「`observe` が返さないもの」と定義し直した／`known_hand` を champion の既定に含めてよいか＝推しは含める）＋ **段 C-2 の 4 件（固定 n の計画外追加を認めるか／★本数を既定で増やすか＝「lc が低いから効きが薄い」という理由が実測で否定された／II-8 の母数掃引はしない／掃引の道具に 1 局ごとの記録を足す）**＋ **段 C-3 の 6 件（`endgame_eval` を 16 から上げるか／W=1 でも列挙に入る副作用／`world_weight` と併用時は重みを掛ける／投票の寄与を分離して測っていない／固定 n の追加／0.535 は 2 つのつまみの合計で C-3 単独の寄与ではない）**＋ **段 C-4 の 6 件（候補名は `khweb` でなく `kheb`／「上位 3 枚」と層の切り方は掃引しない／固定 n の区間 0.526 は 0.5 を含む／つまみ 0 のとき `_determinize` の呼び出し方も従来のまま／費用・監査の道具を `experiments/` に残す形で置いた／C-4 単独の寄与は別帯なので言えない）**＋ **交代判定の 5 件（ラダー v11 を回さない／使い回しの線の厳しさ／使い回した組には席別の勝敗が無い／T-C7 の検査を「部分集合＋共通部分は完全一致」に読み替えた／★D-034 の p₁ = 0.55 の見直しは今回に遡及させない）**＋ **交代の 3 件（撤回の範囲を改訂 1 だけに限った／ラダー v11 の解析を交代後に回し直さない／抜き取り検査に 8 組を選んだ）**。**24 番（ラダー v11 を回さない）と 28 番（p₁ の見直しを遡及させない）は、マスター裁定でどちらも推しが採られなかった**——改訂 1 が撤回され、ラダーを回して交代した。**残りはすべて推しで進めてある。異議があれば戻す**

## Someday

- [ ] **便 E 反復 7' で「2 世代前に勝ち越せているか」を再確認** - D1 の C2（V_1 対 V_3）が上端 **0.488** と際どかった（D-076 判断 3）。磁石つき正則化は入れない方針だが、7' で見直す
- [ ] **計画外の気づき 3 件の裁定** - `RULES_VERSION` の 1 本化／`provenance._champion_label` の `null`／`PRERELEASE_GOAL.md` の SD02 記述（D-075 の気づいた点・D-076 でも据え置き）

## Done

- [x] ~~**PC の Rust を再ビルドする（段 K-5 の残り）**~~ (2026-09-13・D-085 追記 1・マスターが実施) — **これで段 K-5 は完全に閉じた。** `features()` に **`bp01_k5`** が出ることを確認。古い wheel は新しい op で panic して SD001 の対局すら始められない状態だったので、任意ではなく前提だった。**名指しで通過を確認した 3 つ**: `test_bp01_k5.py` **9 件すべて通過（skip 無し）**／`test_rust_matches_python_on_every_smoke_deck` **12 件通過**（3 スモークデッキ × 4 シード。スモークデッキに 3 枚足したので今回いちばん動いた場所）／`test_the_two_published_cards_have_their_effects` **通過（xfail ではなく）**。**★全検査の通過・失敗・skip の総数は受け取っていない**ので、「全件で失敗 0」とは言えない。総数が要るときは PC で回し直して skip の内訳ごと数えること

- [x] ~~**`BP01-057` と `BP01-062` の効果を入れる（便 K の残り・段 K-5）**~~ (2026-09-13・D-085・クロエが直接実施) — **便 K の積み残しが無くなった。** 語彙を 3 つ足した: `grant_rush_draw_to_variation_skills`（u19・数えは新しい状態欄 `variation_rush_draw`・連撃で使った＜変奏スキル＞だけを数え、追撃と同じく**加算**、ターン終了で消える）／常在型の `cost_mod`（u20・**下限 0**・`_effective_cost` が読むので使用条件にも支払いにも効く）／`levelup_by_effect` の任意 `level`（u21・**省略時は従来どおり**なので既存カードは不変）。リーダー切り替えと【判定】は既存の op で足りた。**rules は v0.12 のまま・符号化は v4 のまま＝ネット移行は不要**。**挙動不変の根拠 4 つ**: Python の fingerprint 3 種 不変（`773a71c15c5bc16e` ほか）／champion の fingerprint 不変 **`e1662edb32b144a9`**／**Rust の digest が SD001・SD02 × {H 同型, 貪欲} × 各 200 局で変更前の wheel と全局一致**／新しい語彙を使うカードが 2 枚だけであることを検査で固定（T-K5-9）。新設 `tests/test_bp01_k5.py` 9 件、**T-K-10 は設計どおり「予想外に通った」で落ちたので旗を外した**。作業環境で **490 通過・skip 3・失敗 0**。**★3 枚をスモークデッキに入れた**（`BP01-057`/`BP01-062` は `K_smoke_ANKO`、`BP01-049` は `K_smoke_TSUBAKI`。抜いたのは共通カードの 2 枚目なので被覆は減っていない）——入れないと「効果があるのに 1 度も踏まれないカード」が 3 枚残る。**★計画外に 1 件直した**: `test_no_image_allowlist_is_consistent` の `assert allow` が、D-084 追記 1 で許容リストが空になった日から「読めているのに落ちる」検査になっていた（D-082 追記 3 と同じ型の誤り）

- [x] ~~**撮れていない 19 番号を撮り足す**~~ (2026-09-13・D-084 追記 1・マスターが撮影／クロエが対応づけ) — BP01-017 / 020 / 044〜047 / 051 / 052 / 054〜056 / 060 / 063〜069。**ちょうど不足分の 19 番号で過不足・取り違えなし**。`cards/` 直下 104 → **123 枚**＝**実装カード 123 番号すべてに画像が揃った**。`images.status()` は indexed 123 / implemented 123 / **missing・no_image・stale_allowlist・duplicates すべて空**。`cards/BP01_NO_IMAGE.json` の許容リストは**空**（58 → 19 → 0）

- [x] ~~**カード画像をマスターの手動スクショに切り替える**~~ (2026-09-13・D-084・クロエが対応づけ／マスターが PC で実行) — **142 枚を正本にした。** 番号はファイル名に無いので、カード右下に刷られた番号の帯だけを切り出して一覧に貼り合わせ、**142 枚すべてを目視で確定**（OCR は精度が足りず補助）。`cards/apply_manual_shots.ps1` を 1 回実行。内訳は**固有番号 104**（SD01 23・SD02 23・BP01 58）＋**パラレル 38**（`-R` 22・`-SP` 9・`-PR` 7）で、**新しく画像がついた番号は 39**。解像度 620〜626 × 868〜882 px（旧 334 × 485）＝効果文が読める。114.1 MB（旧 18.6 MB・画素数 3.4 倍 × 枚数 1.9 倍／1 画素あたりのバイト数は不変）。旧 76 枚は `cards/_old_images/` へ**移動**（消していない）、原本は `cards/手動スクショ/` に残る。**★パラレルは `cards/parallel/` に分けた**——`build_index` は `-R` を持たない方を選ぶので、`-SP` のような接尾辞は「パラレルでない」と判定されて**通常版を押しのける**。サブフォルダは `os.listdir` の 1 階層から構造的に見えないので取り違えようがない。`cards/` 直下は 1 番号 1 ファイル。**★副産物: D-063 v3 の既知の取り違えが消えた**（`SD01-019_轟音.png` が `SD01-018` の複製で轟音の画像が一度も入っていなかった件。`KNOWN_DUPLICATES` を空にし、「直ったら落ちて知らせる」検査は役目を終えたので削除）。`cards/BP01_NO_IMAGE.json` を **58 → 19** に更新（しないと `stale_allowlist` に 39 番号が出て落ちる）。`images.status()` は indexed 104 / implemented 123 / **missing 空・unused 空・duplicates 空**、`test_card_images.py` ＋ `test_cards_folder.py` **25 通過・失敗 0**。**★踏んだ落とし穴: 取り込んだ `webapp/images.py` と `BP01_UNLISTED.json` が PC の実物より古い版で、そのせいで「許容リストは未配線」「未掲載 3 番号は未取得」と 2 回誤報した**（OneDrive 配下・フォルダ一覧の更新日も古かった）。経緯は `cards/MANUAL_SHOTS_20260913.md`

- [x] ~~**便 K（BP01 のカードデータをエンジンに入れる線）**~~ (2026-09-13・D-083 と追記 1〜5・クロエが書き戻し／マスターが PC で実行) — **完全に閉じた。** 38 ファイルの書き戻し（衝突 4 件は手で継いだ）→ ネット移行 12 本 → Rust 再ビルド（wheel 624,467 バイト）→ **全検査 774 通過・25 skip・xfail 1・失敗 0**（除外なし・1 時間 7 分）→ `verify_report.py K` **行 30／不一致 0／再計算できない行 0**。rules **v0.12**・符号化 **v4**（`NA` 78 / `NC` 45 / `OBS_DIM` 1,313 / `ACT_DIM` 226）・BP01 の **87 種がすべて揃った**（正本 169 レコード / 123 番号）。**三者一致（Python / Rust / Windows）が名乗れる状態になった**

- [x] ~~**便 A 後半の次の便を裁定する**~~ (2026-09-13・マスター裁定) — **AI 線（便 E / B / F）はいったん止め、カード線＝便 K の展開へ進む。**理由は「CPU の性能は足りている・ブースターパックのカード性能が公開されたので次の段階へ進みたい」。**積み残し（便 E 本体・便 B・便 F）は「今後より AI を強化したいとき」まで後回し**——クロエの推しは (c) 便 F の前倒しだったが採らなかった。便 F の中身は §3.6 と D-083 に書いてある

- [x] ~~**PC の Rust を再ビルドする（便 A 後半の交代の残り）**~~ (2026-09-13・マスターが実施) — **これで便 A 後半は完全に閉じた。** `rust/dist/meicho_rs-0.1.0-cp311-cp311-win_amd64.whl` が **596,762 バイト・2026-09-13 15:54（JST）** に作り直され（前の版は 584,927 バイト・09-11 18:49）、**再ビルド後の全検査が通った**（マスター報告）。**これで `bundle_p` を使う道具（ラダー・`arena_rs`・門番・`tests/test_lit_a2.py` の Rust を要する 8 件）が PC でも動く。**交代そのものは D-082 追記 2、交代で落ちた検査の直しは追記 2・3 にある

- [x] ~~**champion 交代 `planner_vc4cps_kheb` → `planner_vc4cps_kheb_b75`**~~ (2026-09-13・マスター裁定・D-082 追記 2・クロエが直接実施) — **D-081 の 5 条件をすべて満たしたうえでの交代**: 門番 別帯 n=1,200 で **0.530 [0.502, 0.558]**／対照 null 0.503 [0.475, 0.532]／錨 3 種は 9 本すべて 0 を含む（どこでも弱くなっていない）／fingerprint 4 種 不変・新 **`e1662edb32b144a9`**・一つ前 `e82b796960e04c4a` 不変／ラダー core5 v13 で **Nash の台に単独** nA=0.0000・Elo 1 位。**中身は便 C の 11 個 ＋ `bundle_p = 0.75` だけ**（1 便で変える要素は 1 つ）。**直した 4 か所**: `experiments/champion.py`／`gauntlets/core5.json`（**v14**・`champion` 欄）／`webapp/agents.py` の `DEFAULT_OPPONENT`（旧 champion は一覧に残す）／`experiments/provenance.py` の表示名。**★証拠は便 C の交代よりずっと薄い**——門番の余裕は **0.002**（便 C は 0.013）・**T-14 は 1 手も直っていない**・費用 +27.0%。**閾値は測る前に固定し、測ったあとに動かしていない**（D-081 の撤回を前例にしない）が、**この 0.002 も次の便の前例にしない**。**交代で検査が 7 件落ちた**: 読み替えて直した 4 件（`test_champion_vc4.py` 3・`test_lit_c.py` 1）と、**書き方の誤りを直した 3 件**（`test_lit_a2.py` が `champion.kwargs_for` を**その場で読んで**基準を作っていた——**§10-5 で一度学んだことの繰り返し**。便 C の 11 個を明示の定数に固定し直した）。**1 件も消していない。**最終 **通過 703／失敗 0／skip 26／xfail 1**。**残るのは PC の Rust 再ビルドだけ**（Active）

- [x] ~~便 A 後半（A-2 束ねたソルバ `bundle_p`）~~ (2026-09-12・D-082・クロエが直接実施) — **§3.1 の便 A が閉じた。** 新しいつまみ `bundle_p`（既定 0.0・Python と Rust）＝AI の提出分布を決定化 K 本に**共通**に置いて束ねたゲームを解き、**平均戦略**の最大の手を出す。**既定で挙動不変**（fingerprint 4 種・Rust の digest）／**`bundle_p=1.0` は現行 champion と一手・一点・乱数の消費まで同じ**（計画書の「p=1 で A-1 と一致」を置き換えた）。ソルバは `meicho/bundle.py` ＋ `rust/src/bundle.rs`（**交互更新の予測つき RM+**・反復 300・乱数を引かない・3×3 の厳密解と可搾取度 **1.35e-4**）。**★効いたのは被搾取だけ**: 現 champion 0.145 ±0.028 → **b75 0.180 ±0.031**（+0.035・0.025 を越えた唯一の候補）。**錨 3 種は 9 本すべて 0 を含む**。費用 +27.0%。門番（b75 だけ・別帯・n=1,200）**0.530 [0.502, 0.558]**・対照 null 0.503。**ラダー v13 で Nash の台に単独**（nA=0.0000・Elo 1555・**1 時間 55 分**・82 組を使い回し・抜き取り 9 組は完全一致）。検査 **702 通過・失敗 0・skip 26・xfail 1**（新設 18 件）。**★★ T-14 の 9/3 の 2 敗は 1 手も直らなかった——葉の V・世界の作り方・対抗の集約規則の 3 便続けて、自己対戦では前進して対人の穴は動かない。この 3 つは的の外にある**。**★わざと壊す 3 つのうち「Rust の足し算を降順」は対局の毎手一致をすり抜けた**ので、ソルバの戻り値を**ビット単位**で比べる検査に替えた（`meicho_rs.solve_bundled` を検査専用に開けた）。**★予測つき RM+ は最終反復も収束する**ので「平均戦略を返しているか」は退化した行列でしか見分けられない。**★作業環境は PyPI と crates.io へ出られなかった**——Rust は**マスターの PC の `~/.cargo` の `.crate` 36 個を持ち込んでオフラインでビルド**（sha256 は `Cargo.lock` と全一致）、pytest は `_shim/` の間に合わせ。帯 709000..714999（`next_free` 715000）＋**便 K の 706000..708999 を代行登録**

- [x] ~~便 K 段 K-5（仕上げ）~~ (2026-09-11・D-079 追記 7・クロエが直接実施) — **便 K の実装側は閉じた**。仮デッキ 3 つ × **1,000 局**を Python と Rust で回して**打ち切り 0・例外 0・数字は完全一致**（`K_smoke_ANKO` 平均 23.20 ターン／`K_smoke_SANGE` 32.45・引き分け 1／`K_smoke_TSUBAKI` 25.95）。道具は `scripts/smoke_bp01.py`（新設）。`experiments/verify_report.py K` を新設し **行 30／不一致 0／再計算できない行 0**（`results/k/manifest_K.json`）。**アプリでの表示を検査に固定**（画像の無いカードが名前＋効果文で出ること／`unverified` のスキルに「※テキスト未確認」が出ること）。検査 **426 通過・21 skip・失敗 0**（この作業用の写しに材料が無い 3 モジュールを除外）。**T-K-1 の digest は K-1 から 1 つも動いていない**。**★K-4 までのスモークは A-3 の探索と同じシード帯を踏んでいた**ので、帯 **706000..708999** を登録して回し直した（`next_free` 709000）。**★Rust には `RandomAgent` の型が無い**ので `rs.series` の `{"kind": "random"}` で回す。**残るのは Windows の Rust 再ビルドだけ**（5 点セットの依頼文は `BP01_NOTES.md` §K-5。**送るのは便 C の交代判定のあと**）

- [x] ~~便 K 段 K-4（残りのキャラ固有・21 番号）~~ (2026-09-11・D-079 追記 6・クロエが直接実施) — **BP01 の 68 番号すべてに効果が入った**（スキルを持つ 64 枚・バニラ 4 枚）。**T-K-4 通過**。新 opcode 12・新条件 2（Python と Rust の両方）で opcode の分岐 37 → **50**・条件 13 → **15**。`unverified` のスキル **62 件**。**スモークデッキを 3 つに増やした**（§3.6 は 2 つだが、2 つでは 散華・漂泊者の BP01 カードが 1 度も盤に出ない）。3 デッキとも乱択 60 局で**打ち切り 0・例外 0**。検査 **389 通過・3 skip・失敗 0**。**★付与スキルは付与先にスキルを生やしてはいけない**（`_action_timing_index` のキャッシュがカード定義の不変に寄りかかる）。**★Rust の取りこぼしがもう 1 件**（`own_card_damage_buff` の走査。K-3 の 2 件と同根）。**★opcode のパラメータのキャラ名・カード名は `NAME_PARAM_KEYS` で伏せる**（K-2 と同じ罠の 2 度目）
- [x] ~~便 K 段 K-3（【優勢】と＜音骸＞・12 番号）~~ (2026-09-11・D-079 追記 5・クロエが直接実施) — ＜音骸＞のエリア 1 枚制限 (u5)・協奏エリアの「2 種類以上」(u6・u7)・妨害 5 種（**乱数は 1 枚動かすごとに 1 回**）。処理済み 47/68。opcode の分岐 31 → 37。検査 364 通過・3 skip・失敗 0。**★BP01 のデッキで回して初めて Rust の取りこぼしが見つかった**（`test_rust_engine.py` は SD001/SD02 を回すので BP01 を 1 度も踏まない）→ `tests/test_bp01_k3.py` に**仮デッキでの毎手一致**を足した。**★妨害効果の「選ぶ側」(u8) は自動選択（左端）にした**——選択肢にすると `CHOICE_KINDS` が伸びて符号化の版上げがもう一度要るため
- [x] ~~便 K 段 K-2（新しいタイミングと状態欄）~~ (2026-09-10・D-079 追記 3・追記 4・クロエが直接実施) — **`rules_draft.md` を v0.11 → v0.12 に上げた**（§3.2・§7・§9・§10）。`Timing` 6 → **13 種**・状態欄 **＋12**・条件 5 → **13 種**・opcode の分岐 19 → **31**（Python と Rust の両方）。台帳の (b) 群 **20 番号**の効果を実装＝効果が入ったのは **35/68**。検査 **343 通過・3 skip・失敗 0**（`tests/test_bp01_k2.py` を新設・15 件）。**T-K-1 の digest 8 本は K-1 から 1 つも動いていない**。**★効果の解決の途中で誘発するタイミングは既存の待ち行列に載せられなかった**（`_queue_fire` が `pending_skills` と `choice_resume` を置き換えるため）ので、割り込み専用の `pending_triggers` を足した。**★opcode のパラメータにカード番号を書いたら `test_card_space` が「機構の仕様にカード ID が漏れている」で落ちた**（D-050 条件 1）ので `pending_effect["card"]` から取る形に直した。**★Python/Rust の差 3 件はすべて「片方だけ直した」**で、毎手一致の検査が拾った。**★u18 を新発見・裁定（そのカード自身が与えたときだけ）。推しの理屈が誤っていた**（実装の都合をルールの範囲の話にすり替えていた・D-079 追記 4）
- [x] ~~便 K 段 K-1（68 番号＋3 枠の登録・符号化 v4・ネットの移行）~~ (2026-09-10・D-079 追記 2・クロエが直接実施) — 登録簿 **52 → 123 枚**、`NA` 34→**78** / `NC` 18→**45** / `OBS_DIM` 596→**1,313** / `ACT_DIM` 111→**226**、`ENCODING_VERSION` 3→**4**。**Python と Rust がどちらも `(4, 1313, 226)`**。`reconcile_cards` **食い違い 0 / 未登録 0**。**T-K-1 の digest 8 本すべて不変**＝カードを 71 枚足しても SD001/SD02 の対局は 1 手も変わらない。`scripts/migrate_nets_k.py` を新設。`heuristic.LEADER_COLOR` を導出化（D-047 監査の「高」）。画像の許容リスト `cards/BP01_NO_IMAGE.json`（58 番号）。**★ネット移行の検算は float32 では測れない**（`pi_small64_e10` で 1.05e-5 出たが float64 では 3.55e-15＝厳密。差は BLAS の足し算の順序）ので**関門を float64 の一致に移した**。**★T-K-4 が通る段が K-4 → K-1 に前倒し**（K-0 の裁定 1 の帰結）
- [x] ~~便 K 段 K-0（台帳と不変の検査）~~ (2026-09-10・D-079・クロエが直接実施) — **対局なし・登録簿は 1 行も触っていない**。`cards/` の正本を **166 レコード / 120 番号**に更新（既存 76 行はバイト単位で不変）・`cards/BP01_MECHANICS.md`（68 番号の機構台帳）・`engine/rules_draft_v0.12_proposal.md`・`cards/BP01_UNLISTED.json`・`engine/tests/test_bp01.py`・`engine/BP01_NOTES.md`。`reconcile_cards` **食い違い 0 件 / 未登録 68**。検査 **19 通過・1 xfail（T-K-4）・失敗 0**。分類は **K-1 15 / K-2 20 / K-3 12 / K-4 21**。新しい語彙は **op 26・Timing 7・状態欄 12・条件 10**（既存の 2 倍以上）。**★ 段ごとに登録すると符号化の版上げが 4 回になると分かったので、K-1 で一度に登録する（裁定 1）**
- [x] ~~作業の整理と便 K（BP01 のカード入力）の引継ぎ書~~ (2026-09-10 夜・D-078 追記) — 残る作業を **AI 線（便 C）とカード線（便 K）の 2 本**に分けた。`engine/HANDOFF_20260910_CARDS_K.md`。未登録は 68 番号（前から 13＋新規 55）。**機構の追加（音骸 10・優勢 8・登場 8・レベルアップ誘発 10 ほか）で rules v0.12 に上げてから実装**。**訂正: 既存 `cards_structured.json` は 76 レコード／65 番号**（「76 番号」は誤り。新規 55 は不変）
- [x] ~~カード画像の取得（要否）~~ (2026-09-10・マスター裁定) — **取らない**。テキストで十分。BP01 の新規番号はアプリで名前と効果文の表示。経路の記録（マスターの Chrome 経由・圧縮 webp のみ・「SAMPLE」透かし）は `cards/OFFICIAL_FETCH_20260910.md` と D-078 に残っている
- [x] ~~裁定 2 件（公式テキストの割れ）~~ (2026-09-10・D-078) — **どちらも「任意（〜てもよい）」が正**（マスター裁定）。`BP01-033`「協奏エリアに**置いてもよい**」／`SD02-006`「手札に**加えてもよい**」。**いずれも既存 `cards_structured.json` と一致＝公式サイト側の表記が誤り**。簡体字「散华」4 件は「散華」に、`BP01-032` ★ の行頭 `■` は補って統一。採用値は `adopted_effect` / `adopted_name` の欄に並記し、取得記録の `effect` / `name` は書き換えていない。**D-078 を消費した（次の便は D-079 から）**
- [x] ~~第 1 弾（BP01）のカードリストを公式サイトから取得~~ (2026-09-10) — **全 191 レコード・失敗 0・転記は行単位のハッシュで全行一致を確認**。ユニーク番号 120（BP01 74 / SD01 23 / SD02 23）。BP01 封入は 84 番号（キャラ 37 / アクション 47）。**欠番 3 件は公式が未掲載**（商品詳細の 87 種と差分が一致）。既存 `cards_structured.json` 76 番号との照合で**内容の相違は 2 件だけ**（`BP01-033`・`SD02-006`）、残りは表記の差。**簡体字「散华」の混入 4 箇所はローカライズ漏れ**。成果物は `cards/cards_official_20260910.json` / `.csv` / `OFFICIAL_FETCH_20260910.md`

- [x] ~~PC の Rust 再ビルド（新 champion の `draw_buckets` ぶん）~~ (2026-09-11・マスター実施) — 完了。これで PC でも Rust 側で新 champion を回せる（ラダー・門番・fingerprint）。**便 K のカード拡張ぶんの再ビルドはまだ**（zip の展開時に別途）
- [x] ~~便 C の次の便の裁定~~ (2026-09-11・マスター裁定・D-081 追記 2) — **便 A 後半（A-2 束ねたソルバ）**に決まった。引継ぎ書 `engine/HANDOFF_20260911_LIT_A2.md` を書いて PC に置いた。**計画書 §3.1 からの読み替え 1 件**: A-1 が実装されていないので「p=1 で A-1 と一致」を「p=1 で現行 champion とビット一致」に置き換えた。便 A 前半の判断②・③（`lu50` の交代・追試）は**2 世代前の champion 上の値なので取り下げ**（アプリの候補としては残す）
- [x] ~~**champion 交代 `planner_vc4cps` → `planner_vc4cps_kheb`**~~ (2026-09-11・D-081 と追記 1・クロエが直接実施) — **マスターの指摘「0.51 くらいを上回れば交代する規則だったはず」から記録を当たった結果、0.55 のほうが新しい条文だと分かった**（元の D-034 裁定 4＝2026-08-24 が「n≥300・95% 下端 > 0.5」、GSPRT・p₁=0.55 は 2026-09-09 の改訂 1）。**マスター裁定: 改訂 1 だけを撤回**（改訂 2 のラダー条件は残す）。**これは事後の閾値変更である**——撤回は 0.534 を見たあとなので、この 1 件は事後選択。記録は「規則に照らして自動的に通った」ではなく「**マスターが規則を直したうえで通した**」とし、**以後この前例は使わない**と D-081 に明記した。戻した門番で 5 条件すべて満たす: 直接対決 **別帯 0.542 [0.513, 0.570]**（n=1,200）・合算 **0.534 [0.514, 0.554]**（n=2,400・Elo +23.5）／**ラダー core5 v11 で Nash の台に単独で乗り nA = 0.0000**（前 champion は台から落ちて −0.2141）・Elo **1542 [1521, 1565] の 1 位**（2 位 1522 [1500, 1546]・**区間は重なる**）・非推移性 0 組／監査 違反 0／fingerprint 新 **`e82b796960e04c4a`**・前 `9b5ad48d2de6a7e0` 不変／マスター裁定。**★ラダーの使い回しが実戦で働いた**——91 組のうち 70 組を v9 から使い回し、候補が絡む 13 組と抜き取り検査 8 組で **3 時間 11 分**。**抜き取りは 8 組とも記録と 1 局も違わず**、前 champion の Elo は v9・v11 で完全一致。使う前に Python↔Rust の一致も 4 組 × 40 局で確かめた。**★都合の悪い数字**: ラダー内の直接対決（n=300）は 0.553 [0.497, 0.609] で**下端が 0.5 をわずかに割る**。`ladder.py` 自身の「champion 候補」欄も「なし」と出る（あれはラダー内 n=300 の簡易判定で条文とは別物）。**★交代で検査が 10 件落ちた**（反映漏れ 3・便 C の土台の崩れ 4・由来の表示名 2・検査が狭すぎた 1）。全部**読み替えて**直した（消していない）。最終 **673 通過・26 skip・失敗 0**／`verify_report.py C` **296 行・不一致 0**。**★輪 2 の探索器が champion とずれた**（`vb.py` は不変）＝Waiting On。**残るのは PC の Rust 再ビルドだけ**（依頼文は `engine/WORKER_HANDOFF_LIT_C.md` §9。アプリは再ビルド前でも動く）
- [x] ~~便 C の交代判定（最終候補 `planner_vc4cps_kheb`）~~ (2026-09-11・D-077 追記 5・**結論は同日 D-081 で覆った↑**) — **（当時の条文では）交代しない**。門番 GSPRT（別帯 700000）が**不合格側で止まった**（2,280 局・1,140 ペア・LLR −2.47）。固定 n=1,200 は 0.542 [0.513, 0.570]、段 C-4 の帯の 0.526 と**合算して 0.534 [0.514, 0.554]（n=2,400）＝Elo +23.5**・費用 +17.3%——**強くはあるが門番が問う 0.55 には届かない**。対照 0.506 [0.478, 0.534]（0.5 を含む＝配線は健全）・監査 違反 0・fingerprint は champion `9b5ad48d2de6a7e0`（不変）／候補 `e82b796960e04c4a`。**★いちばんの学び: 段の門番を 3 回通っても交代の証拠にはならない**——同じ比較を段ごとに検定するのは多重検定で、真の値が p₀ と p₁ の中間にあると帯によってどちらにも転ぶ。段の門番は関門、交代は別帯で 1 回。**★ラダーを 5〜9 時間から約 2.4 時間にする仕組みを作った**（`ladder.py --reuse-from` / `--reuse-verify`）——v9 の 78 組は体の定義が変わっていないので使い回し、候補が絡む 13 組だけ回す。**体は 1 つも減らさない**。v9 の 4 組を回し直して**完全一致**を確認済み。**ラダー v11 は回していない**（交代しないので。**のちに撤回後に回した**）。検査 670 通過・26 skip・失敗 0
- [x] ~~便 C 段 C-4（ドロー結果のバケット化 `draw_buckets`）~~ (2026-09-10 深夜・D-077 追記 4) — **残す**。**門番 GSPRT 合格**（720 局・360 ペア・LLR +3.71）／**固定 n=1,200 は 0.526 [0.498, 0.554]＝区間は 0.5 を含む**（言えるのは「弱くなっていない・おそらく少し強い」まで）。錨 3 種とも上端 ≥ 0（H +0.010／貪欲 +0.013／素 planner +0.020）。費用 +17.3%。監査 違反 0。検査 **666 通過・26 skip・失敗 0**。**★被覆率は動かない（cov_hand K=6 が 0.137 → 0.139）——これは「効かない」ではなく「この段の物差しの外」。C-1 と C-3 が直したのは相手の手札の集合、C-4 が触るのはこれから引く札の並びだから**。新設 `meicho/buckets.py`・`rust/src/buckets.rs`・`experiments/c4_cost_audit.py`。候補は **`planner_vc4cps_kheb`**（引継ぎ書の `khweb` から読み替え）
- [x] ~~便 C 段 C-3（II-9 終盤の整合世界の全列挙）~~ (2026-09-10・D-077 追記 3) — **残す**。**門番 GSPRT 合格**（1,160 局・580 ペア・LLR +3.65）／**固定 n=1,200 は 0.535 [0.507, 0.563]＝区間の下端が 0.5 を上回る**（段 C-1 単独の 0.519 より強いが、別の帯なので引き算はできない）。錨 3 種とも上端 ≥ 0（H −0.003／貪欲 +0.020／素 planner +0.040）。**★決定の 13.5% で「ありうる相手の手札」は 64 通り以下しか無く、そこを引かずに全部並べると真の手札を持っている割合は 0.741 → 0.960、W ≤ 16 の 1,081 決定では 1.000（取りこぼしが原理的に無い）**。**★対人局の「烈火を読み切れなかった負け」の局面（W=48）が 0.10/0.50 → 1.00 になった**。**★投票はほとんど手を変えていない（列挙に入った決定の 11.8%）＝効き目の大半は世界の集合が正しくなったこと**。費用 +14.0%。監査 違反 0。検査 662 通過・26 skip・失敗 0
- [x] ~~便 C 段 C-2（II-8 π₀ の到達確率の重み）~~ (2026-09-10・D-077 追記 2) — **落とす**（門番 GSPRT が不合格側で止まった・400 局・LLR −2.91／固定 n=1,200 は 0.505 [0.477, 0.533]＝互角／費用 +24.8%）。**★なぜ効かないかも分かった: η の定義で「t 以降に相手が出した札」を足し戻すので、相手が出した札はどの世界にも入ってしまい、世界を分ける力が消える**（実装は設計どおり・設計が的を外している）。**★2×3 掃引で引継ぎ書の予想が外れた: 本数 6→24 で +0.055〜+0.067。本数 24・重みなしのセルは下端 0.532 が 0.5 を上回る＝「lc が低いから本数増は効きが薄い」は否定された**（ただし費用 2.4 倍・セルどうしの差は断定できない）。検査 655 通過・26 skip・失敗 0
- [x] ~~便 C 段 C-1（II-7 (a) `known_hand`）~~ (2026-09-10・D-077 追記 1) — **残す**。**門番 GSPRT が合格側で止まった**（640 局・LLR +4.00。回す前の予想「不合格側に止まる」は外れた）。固定 n=1,200 は **0.519 [0.491, 0.547]**＝大きさは「弱くなっていない・おそらく少し強い」まで。錨 3 種とも上端 ≥ 0。**被覆率 cov_hand は K=6 で 0.047 → 0.110、見えている札がある決定に限ると 0.018 → 0.257（14 倍）。1 本あたりの当たりやすさ（TSSR プール比）は 0.449 → 1.019 ＝公平になった**。費用 +1.8%。監査 違反 0。検査 647 通過・26 skip・失敗 0。**引継ぎ書 §0.3 (x) の「Rust に `peeked_opp_hand` が無い」は誤りで、最初から在った**
- [x] ~~便 C 段 C-0（片付けと測る道具）~~ (2026-09-10・D-077) — **いまの探索は真の相手手札をほとんど見ずに手を選んでいる**（決定化に真の手札が入る割合は K=6 で 0.047・**K=24 でも 0.069**＝本数増は効かない／1 本あたりの当たりやすさは公平の **45%**）。一方**相手がその対抗で出す札は K=24 で 0.996 が候補に入っている**＝読み違いは「重みの置き方」の側。道具 3 つ（`coverage.py`・`verify_report.py`・`manifest_lit_c.json`）。帯 676000..705999・`next_free` 706000。検査 642 通過・26 skip・失敗 0
- [x] ~~由来の `host` に `cowork-2` を足す~~ (2026-09-10・D-077) — `provenance.HOSTS` に追加。**知らない host 名は拒否する**ようにした（既定は `workenv-2` のまま）
- [x] ~~便 M の結果を照合し、便 C の引継ぎ書を書いて PC に書き戻す~~ (2026-09-10) — 照合はすべて一致。訂正 1 件（`hand_known` の効きは全決定平均 0.743 ではなく、効く場面で 8〜20 分の 1）

- [x] ~~便 M の結果を受け取る（D-076）~~ (2026-09-10) — 分岐の答え 3 つ: **便 C は全部**（天井 +77 Elo）／**便 B は急がない**（中盤 D_t 中央値 0.095）／**便 E 7' の磁石は不要**（循環なし）。訂正 3 件と D-034 改訂条文も入った。検査 636 通過・26 skip・失敗 0
- [x] ~~便 M（診断の残り）を作業者に渡す~~ (2026-09-09)
- [x] ~~便 M の引継ぎ書を書いて PC に書き戻す~~ (2026-09-09)
- [x] ~~便 D（後半）の結果を照合し、zip を PC に展開~~ (2026-09-09)
