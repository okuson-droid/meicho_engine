# Tasks

> **未着手の作業はここに一元化する**（2026-09-10）。`engine/` の文書は**記録**（何をなぜ決めたか・数字）で、
> ここは**状態**（いま誰が何をするか・終わったか）。同じ作業を両方に書かない。
> 設計の順序（便 D → A → B → C → E → F → G）は `engine/LITERATURE_PLAN_20260906.md` §3 が正本で、
> これは作業リストではないので重複ではない。
>
> **2 つのチャットが並行している（2026-09-20）。最初に `LANES.md` を読む。**この台帳はエンジンの持ち場だけが書く。アプリの台帳は `engine/app/TASKS_APP.md`。
>
> **最新の引継ぎ書（エンジンの持ち場）**: `engine/HANDOFF_20260925_ENGINE.md`（2026-09-25 改訂・TE-13 を直したチャットから。同じ日の朝の版（段階2 のチャット）を上書きした）。**段階3 の設計書 `engine/GENERALIST_STAGE3_DESIGN.md` を書いた（2026-09-25・D-135）。次はマスターの裁定**（Active の先頭）。
> 併走系統の `engine/HANDOFF_20260916_PTCG_WRITEUPS.md` は**完了している**（D-090・2026-09-19 に改訂して状態を明記）。判断が要る点 **J-1〜J-6** は 2026-09-21 に裁定した（D-119）。時間のかかる実行は Waiting On の「J の実行で時間がかかるもの」。

## Active

- [ ] **★マスター: 段階3（汎用 V の反復）の設計書の裁定（D-135）** - 設計書 `engine/GENERALIST_STAGE3_DESIGN.md`。判断が要る点は §10 の 7 件（推しつき）: 計算資源（推し Claude Code のクラウド版・`DEVFLOW_PLAN_20260925.md` 段 1 を先に＝TE-13 の直しを GitHub へ）／記録の温度（推し D-064 の規則で反復ごとに下見）／選択の評価の規模（推し 16 ブロック × 300 局）／学習の窓（推し 直近 1 反復＝D-131 は段階3 で使わない）／PT-6 の置き場と形（推し 反復 2 で A/B・51 ビン HL-Gauss）／PT-5（推し 反復 2 のあとに相談）／最終評価の主比較（推し V_sel − V_0）。**事前の確認は済**: 束ねた V（V_0）は V 1 本 3 本の平均より +0.057 [+0.036, +0.078]（調整デッキ・各 2,400 局）＝教師の葉は束ねた V のまま（D-135）
- [x] **~~デッキ類似度の道具 `experiments/deck_similarity.py` の実装（D-126・設計書 `engine/DECK_SIMILARITY_DESIGN.md` §13）~~ → 2026-09-24 に完了（D-127・D-128）。** 順序は守った（検査 → 道具 → 錨 → 閾値の裁定 → 候補を読む）。現行は **decksim-3**（検査 22 件）、閾値は `results/decksim/thresholds.json`（`th-1-provisional`・**変更の見込み高**）。候補（環境デッキ 24 種）で回した結果は `results/decksim/env_v1.json` / `.md`。**§7.3 の割り振り規則は単独グループが無い候補集合で最終評価を作れないことが分かり、規則は変えずに手割りの口（`--split-override`）を足した**（D-128 §5・§6）。残りは設計書 §12「打ち方の近さ」を段階2 の記録の段で 1 回見ること

- [ ] **監査の採用 2 件の実行（D-126）** - (1) 計画書 `GENERALIST_AI_REVIEW_D086.md` §1 に「マスター対 AI の直接対局」を副次の受け入れ基準として書き足す（置き場は段階4・便 F の位置づけは裁定待ちと明記）／(5) `webapp/` の引退の条件と日付・リポジトリを OneDrive の外へ移す日・PC 側の Claude Code（device_bash）を試す——日付と手順はマスターと決める

- [ ] **オンライン対戦アプリ（アプリの持ち場）** - 台帳は `engine/app/TASKS_APP.md`、決定は `engine/app/DECISIONS_APP.md`（APP-番号）、エンジンへの頼みごとは `engine/app/TO_ENGINE.md`。決まりは `LANES.md`
- [ ] **送り箱 TE-12 を受けた（知らせ・2026-09-21）** - 新アプリの公開の道具（`app/release/publish.py`）が `scripts/make_dist.py` の `ALLOW`・`_matches_allow`・`champion_models()`・`verify(out_dir, binary_ok=)`・`warnings(out_dir)` を import している。**これらの名前や引数を変えるときは `engine/TO_APP.md` で知らせる**（`_matches_allow` を公開の名前にしてほしいという希望つき・急がない）
- [ ] **送り箱 TE-11 を受けた（知らせ・2026-09-21）** - 新アプリが `webapp/agents.py`（`available`・`default_for`・`build`・`DEFAULT_OPPONENT`・`OPPONENTS`）と `webapp/record.py` の `verify` を import している（呼び出しは `engine/app/core/cpu.py`・`persist.py::legacy_view`）。**登録名を消す・名前を変えるときは `engine/TO_APP.md` で知らせる**。SD001 の「つよい」は登録簿の既定の相手（champion）に自動で付いていく。AI は `trace.tracing` の外・別スレッドで呼ばれている。シードは帯 722000..741999（APP-008）
- [ ] **送り箱 TE-7 を受けた（知らせ・2026-09-20）** - `engine/app/` に M2 のコードと検査が入った。**`engine/` の直下で引数なしの `pytest` を回すと `app/tests/` も集まる**（`test_server.py` は `aiohttp`/`pytest-asyncio` が無ければ skip）。エンジンの全検査の件数が変わりうるので、依頼書では `pytest tests/` のように場所を指定する。`app/tests/test_game.py` はカードプール全体から無作為にデッキを組むので、カードを足したあとにそこが落ちたら `engine/TO_APP.md` で知らせる
- [ ] **送り箱 TE-8 を受けた（知らせ・2026-09-20）** - 新アプリが `webapp/view.py` の `skill_text`・`action_label`・`choice_reason` を import している。**この 3 つの名前か引数を変えるときは `engine/TO_APP.md` で知らせる**。`meicho.engine._effective_cost` も呼ばれている（内部関数。変えたら知らせる）
- [ ] **送り箱 TE-9・TE-10 を受けた（知らせ・2026-09-21）** - TE-9: アプリがトレース点を使い始めた（`engine/app/core/game.py`）。**トレース点の位置や `reveal` の出方を変えるときは `engine/TO_APP.md` で知らせる**（アプリの秘匿の検査がトレース点ごとの観測を物差しにしている）。TE-10: アプリ側で「公開して手札に入ったカード」を覚えるようにした。将来 `observe` の `hand_known` に入れるなら知らせる。**積集合で忘れると見えない出方（`opp_hand_random_to_deck_bottom`）が読めてしまう場面がある**との指摘（未確認・Someday の AI 線の材料に追記）
- [x] **~~TE-13 【切り替え】が切り替えに関わっていないキャラでも誘発する~~ → 2026-09-25 に終えた（D-134・rules v0.19）。** マスター裁定「段階3 の前に直す」。公式 603.1.2.1.1／906.2／913.9.1 のとおり、誘発するのは入れ替わった 2 枠（リーダーと相手になったバック）のキャラだけにした（Python `_queue_switch_triggers`・Rust `queue_switch_triggers`・3 か所）。重なりの下のカードは従来どおり誘発する（§6.3-3・TE-13 が任せた判断を推しで閉じた）。検査 `tests/test_te13_switched_scope.py` 41 件（直す前に Python 5 件・Rust 毎手一致 17 件が落ちるのを見た）。**SD001/SD02 は 1 手も変わらない・champion の指紋 `f4b80b25c35cfa77` 一致**。環境デッキの SK 系 8 種は random 同型戦 400 局中 349 局で展開が、147 局で勝敗が変わる（SK 以外 800 局は 0）。**D-131 の教材のうち SK 系の学習デッキ 4 つが絡む局は旧挙動**（段階3 の設計で扱いを決める・D-134 §5）。全検査 1,315 通過・5 失敗（既知）・4 skip。`engine/TO_APP.md` TA-11 で知らせた。**残りは PC の再ビルド**（Waiting On）
- [ ] **`test_d065.py::test_distil_makes_the_student_agree_with_the_teacher` が落ちる（前からある・D-132 §8 で発見）** - torch のある環境で初めて回った。検査用の偽の記録では学習前から一致率 1.000 で、検査が成り立っていない（`drl_train.py`・`drl_data.py` を直し前に戻しても落ちる）。PC も作業環境も torch が無く skip だったので誰も気づかなかった。偽の記録の作り方を直す（小さい）

- [ ] **Kaggle の登録と初回セットアップ** - `engine/COMPUTE_PLAN_20260908.md` §4.1〜4.3。**要る理由は便 E 本体だけになった**——ラダーは `--reuse-from` で 3 時間強に縮んだので（D-077 追記 5・実測 3 時間 11 分）、交代判定のために Kaggle を待つ必要はもう無い
- [ ] **配布版（Windows 実行ファイル）の作成** - `engine/HANDOFF_20260908_DIST.md`。マスターの PC での作業
- [ ] **対人局を続ける（SD001 のみ）** - 相手は champion **`planner_vc4cps_kheb_b75`**（2026-09-13 交代・D-082 追記 2。アプリの既定も変えてある。**一つ前の `planner_vc4cps_kheb` も選べる**。**一つ前の `planner_vc4cps` も選べる**ので、過去の 4 局と同じ相手でも比べられる）。記録は `engine/results/human_games/2026-09.jsonl`。**2026-09-08 の 4 局から増えていない**

- [ ] **環境デッキ 24 種で段階2 の記録を回す（D-128 のつづき）** - 組み合わせ表は `results/decksim/env_v1.json` の `decks_block`（`lineage`・`group`・`split`）。道具は `experiments/record_mix.py`（異種デッキは常に `opp_from_seat=True`・D-123）。**シード帯を先に登録すること**。回す前に設計書 §12「打ち方の近さ」を 1 回見て、「構成は遠いのに打ち方が同じ」組があれば閾値（`th-1-provisional`）を見直す材料にする。**2026-09-24 に下見まで済（D-129）**: 生成器 `experiments/make_s2_schedule.py`（検査 `tests/test_s2_schedule.py` 15 件・学習 16 デッキだけを使い調整・最終評価は相手側にも出さない）を作り、帯 825000..825999（diag）を登録して `ENV_SANGE_RF_ANKO` に関わる 19 ブロック × 2 局を回した。manifest の決定数は記録と一致・相手デッキ表の食い違い 0 / 3,857 行・約 10 局/秒。**同日のつづき**: manifest のデッキ別 `games` を本当の局数にし、席の数を `seat_games`／`seat_games_teacher` に分けた（D-129 追記 1・マスター裁定）。設計書 §8「打ち方の近さ」の診断を 24 デッキ × 3 相手 × 200 局で回した（D-130・帯 826000..826999）——旗 19 / 248 組、熾霞と今汐は構成で別グループだが打ち方が近い（最終評価の 2 グループがこの 2 人）、分割をまたぐ旗 4 組、秧秧と散華は打ち方では区別できる。**分割は変えない**（§8）。D-130 §4 の判断 3 件は推しを採用（D-130 追記 1）。**本番の記録も済（D-131）**: 学習 10,096 局（帯 827000..837095・5 つに分けて記録・1,017,982 決定）＋別帯の検証 1,088 局（839000..840087・110,624 決定）。学習 16 デッキだけ・引き分け 0・相手デッキ表の食い違い 0 / 1,128,606 行。manifest と組み合わせ表は `results/drl/s2_v1_*`。**記録本体は作業環境にしか無い**（次に V を学ぶチャットで `regenerate` 行から作り直す・約 17 分）。**表現比較は学習まで済（D-132 案）**: V_id（検証 v_logloss 0.5900）と V_pf（カードの素性を第 1 層に足して畳み込む・0.5883）を作った。当てはまりの差 −0.0017 [−0.0051, +0.0015] は区別できない。ネットは `results/models/s2v_id_s0.json`・`s2v_pf_s0.json`。**評価も済（D-132 追記 1・2）**: 対 素 planner で V なし 0.480・V_id 0.592・V_pf 0.618（各 4,800 局）。**V_pf − V_id = +0.025 [+0.007, +0.044]＝基準を満たした**（差の大半は YANG_RF の 1 デッキ）。**乱数 1・2 番の確認も済（D-132 追記 3・4）**: 差 V_pf − V_id は乱数 0／1／2 番で +0.025／+0.003／−0.071 [−0.097, −0.044]＝**向きがそろわず V_pf は採らない**（V_id を既定・V_pf は道具ごと残す）。**学習の乱数だけで強さが ±0.03 動く**ので、以後の V の比較は乱数 3 本以上で判定する。**段階3 の土台の V は V_id の乱数 0・1・2 番の 3 本のアンサンブル**（D-132 追記 5・2026-09-25 マスター裁定）。**アンサンブルも作った（D-133・2026-09-25）**: `experiments/ensemble_net.py` で 3 本を幅 768 の 1 本に組み直した（ロジットの平均・Rust は変えない）。ネット `results/models/s2v_id_ens3.json`（約 39 MB・PC には `.meta.json` だけ。部品 3 本から `ensemble_net.py` で作り直す）・検査 `tests/test_ensemble_v.py` 6 件。葉の費用は 1 局 2.83 秒＝V 1 本の約 3.3 倍。強さは未測定。**次**: `PTCG_KAGGLE_NOTES_20260916.md` の PT-6・PT-5 を読んで計画書 §6 段階3（汎用 V の反復）の設計を書き、裁定をもらう（反復 1 の記録は作業環境で少なくとも約 8 時間・Kaggle を使うか）。~~その前に安い確認として、アンサンブルが V 1 本より強いかを約 2 時間で測れる~~ → **2026-09-25 に測った（D-135）: 束ねた V 0.668 [0.649, 0.687]・V 1 本 3 本の平均との差 +0.057 [+0.036, +0.078]。設計書も書いた（Active の先頭・裁定待ち）**。**段階3 の報告に 1 行**: 最終評価の 4 デッキは熾霞と今汐の SK 系だけで打ち方の幅が狭い（D-130 §2-1）

## Waiting On

- [ ] **★マスター: TE-13 の Rust の再ビルドと確認（D-134）・後回し（2026-09-25 マスター裁定）** - 依頼は **`engine/REBUILD_REQUEST_20260925.md`**（5 点セット・20〜40 分・急ぎ度 中）。(1) 書き戻しの確認 (2) 再ビルド (3) 指紋と `test_te13_switched_scope.py` (4) PC の組の検査。**済むまで PC の Rust（対人検証アプリの AI・新アプリの CPU・`rs.series` 系）は直す前の挙動で動く**。指紋は変わらない見込み（作業環境で一致）

- [x] **~~★マスター: 環境デッキ群のレシピ（D-121 裁定・デッキメーカーで作る）~~ → 2026-09-24 にマスターが取り下げ、クロエが 24 種を構築した（D-128）。** マスターが出した構築規則 R-1〜R-6（3 グループから 1 人ずつで 2×3×4 = 24 通り／緑 6〜9・青 9〜11・赤 20 以上／音骸なし／重撃はアンコだけ・通常攻撃はツバキだけ／アタッカーの赤を多め／キャラカードは全部）を条項にして実装した。生成器 `experiments/make_env_decks.py`（乱数なし）・デッキ `decklists/env/` 24 個・検査 `tests/test_env_decks.py` 174 件。H 同型戦 1,440 局で引き分け・打ち切り 0。類似度（`results/decksim/env_v1.md`・decksim-3）では**系統は 8 つ**（アタッカー × GB が漂泊者かショアキーパーか）で、**漂泊者（男）と（女）はほぼ同じ系統**（S3 0.946〜0.967・24 枠のうち 8 枠が重複。マスター裁定で 24 のまま残す）。割り振りは**学習 16・調整 4・最終評価 4**（§7.3 の規則では最終評価が 0 件になるため、規則は変えずにグループ単位で手割り・マスター裁定 2026-09-24 案 A・`results/decksim/env_v1_split.json` に理由つきで残した）

- [x] **~~★A-6 の再ビルドと確認（マスターの PC）~~ → 2026-09-19 に完了。** 公式 3 ファイル **29 通過**／指紋 **`f4b80b25c35cfa77` 一致**／指紋の 14 節点すべて通過／**全検査 2 失敗・964 通過・26 skip（3 時間 6 分 49 秒）**。失敗 2 件のうち 1 件は既知（`test_lit_c.py::test_worlds_module_matches_diag_pimc`・記録が旧エンジン製）、**もう 1 件は A-6 の取りこぼしで D-106 で直した**（`test_bp01_k2.py` が「満タンなら 0 回復」を前提にしていた）。途中で D-105（`scripts/` の経路）も出た。**これで D-092 の公式ルール差異 12 件はすべて閉じた。** **D-106 の直しも 2026-09-19 に PC で確認済み**（`tests\test_bp01_k2.py`＋`test_official_a6.py`＋`test_engine.py` で **123 通過・失敗 0**）。**engine の PC 作業はこれで空になった。**全検査は挙動が変わっていないので次の便と一緒でよい（そのときの期待: 落ちるのは `test_lit_c.py::test_worlds_module_matches_diag_pimc` の 1 件だけ、通過 964 → 966）。

- [x] **~~★A-6 で champion が弱くなっていないかを測るか~~ → 2026-09-19 マスター裁定: **測らない**（D-107）。** クロエの推しは「測る」だったが採用されなかった。**「弱くなっていない」と確かめたのではなく、確かめないと決めた**——`SD01-023`「奏鳴」(+5) を champion のネットが過小評価している可能性は残ったままである（D-104 §5）。**D-097 §5「次の champion 交代の判定は新エンジンで測り直してから行う」はそのまま生きている**ので、次に交代を判定するときにはどのみち測り直すことになる。今回の裁定は「A-6 を理由に前倒しで測ることはしない」の意。

- [x] **~~★BP01 の画像の前提~~ → 2026-09-19 にマスターが確認し、検査を書き替えた（D-103）。**「D-084 の手動スクショで裁定は実質変わった」。検査は**画像の欠落をその場で作る**形にして環境に依存しないようにした。**PC で落ちていた 2 件のうち 1 件が消える。**（以下は経緯）- `tests/test_bp01_k4.py::test_every_bp01_card_renders_as_japanese_without_an_image` の末尾が `assert shown > 0`（**画像の無いカードが 1 枚も無いと落ちる**）で、**PC の全体検査で落ちている 2 件のうちの 1 件**である（D-102）。この検査は「2026-09-10 のマスター裁定により BP01 の画像は取得しない」を前提に書かれているが、**2026-09-13 に手動スクショへ切り替えた（D-084）ので実装カード 123 番号すべてに画像がある**。**裁定が変わったという理解でよいか。**よければ検査を「画像があること」を確かめる形に書き替える（10 分で終わる）。意図せず入ったのなら別の直しになる。質問は 2026-09-18 から未回答。

- [x] **~~A-3・B-9・A-5・B-7 の再ビルドと確認~~ → 2026-09-18 に完了（D-102）。**PC の全体検査 **955 通過 / 2 失敗 / 27 skip（1 時間 53 分 37 秒）**——**直前の 13 失敗が 2 件まで減り、残る 2 件はどちらも既知で今回の直しとは無関係**（`test_bp01_k4` の画像の件＝下の質問待ち／`test_lit_c` の `m5m1_pimc.jsonl` の作り直し待ち）。`BASELINE_DIGESTS_230000` と Python↔Rust の毎手一致も PC で通過。指紋は貼り替え後の値で一致している。以下は経緯。 - **再ビルドと §4(a)〜(c) は実行済み。残るのは §4(d) の全検査だけ。** **★指紋は B-9 が原因で動き、裁定により `099a7f9382843ff2` に貼り替えた（D-101）。**貼り替えた 2 ファイル（`scripts/check_champion_fingerprint.py`・`tests/test_lit_a2.py`）は **作業環境で通過を確認済み**。依頼は **`engine/REBUILD_REQUEST_20260918.md`**（5 点セット。**同じ日に 2 便ぶん入ったので 1 通に書き直した**＝再ビルドは 1 回で済む）。`rust/src/engine.rs` と `rust/src/state.rs` を変えたので、PC の wheel は直る前のままである。**この 1 通に「D-098 で貼り替えたまま一度も回していない指紋の検証」も入っている。**作業環境（Linux・資材なし）では **859 通過 / 74 失敗 / 46 skip**（新設の検査は 22 件）、**直す前と失敗の集合は完全に同一**（74 件はすべて置かなかった資材による）。**§6 にマスターへの質問が 1 件ある（BP01 の画像の前提・PC は要らない）。**

- [ ] **再検証で確定した公式ルール差異の修正と回帰検証** - **対象は 12 件になった**（D-092・報告 `engine/official_rules/OFFICIAL_RULE_RECHECK_20260917.md`）。~~A-1 デッキ・トラッシュ枯渇時敗北~~・~~A-2 リフレッシュ~~ → **2026-09-17 に直した（D-095・rules v0.14）。**公式 701 のルールチェックを 1 本にして 700.1.1（処理待ちチェックの先頭）と 903.1（引いた後）で回す形。新設検査 9 件、既存 2 件を書き換え。**★SD001/SD02 の対局が変わった**——ミラー 200 局で手順 42%/55%・勝敗 20%/19%。原因は A-1 ではなく **A-2 のリフレッシュ時点**（デッキ切れ決着は 200 局で 0 件）。**基準を貼り替えた（マスター裁定）**: fingerprint H・G と `FROZEN_DIGESTS` 8 件（旧値は札つきで保存）。**`BASELINE_DIGESTS_230000` と fingerprint P は不変**。**過去の勝率は注記だけ付けて測り直さない（マスター裁定）＝便 A〜C の数字と champion 交代の判定は v0.13 以前のゲームを測ったものである。****★champion の指紋は作業環境では測れない。PC の再ビルド後に必ず確かめること。**／~~A-3 割り込み待ち行列の解決順の選択（700.1.2/.3）~~ → **2026-09-18 に直した（D-099・rules v0.16）。**`_start_next_trigger` と `_start_next_skill` を `_start_next_pending(s, queue)` に畳み、割り込みの待ち行列にも順序選択を出す形にした。**既存の `order` を使い回したので符号化は不変**（v5・`OBS_DIM` 1,825・`ACT_DIM` 317）＝**学習済みネット 12 本は 1 本も触っていない**。**SD001/SD02 では 1 度も発火しない**（実測 0 回）。**BP01 仮デッキ `K_smoke_TSUBAKI` では random で手順 116/200・勝敗 36/200 が動く**／~~A-4 誘発済みスキルを落とさない~~・~~B-4 条件の評価を誘発時点に~~ → **2026-09-17 に直した（D-096・rules v0.15）。**`_keep_triggering` を積む側に置き、解決側の再評価を外した。**`_active_skill_refs` では絞らない**（条件つきの常在型スキルがあるため）。**★SD001/SD02 は 0/200 で無変化＝基準は 1 つも動かなかった。**BP01 の仮デッキは ANKO で勝敗 11/200 変わる。**★途中で穴を開けた**——`_start_rush` が `_queue_fire` を通らないので連撃スキルの条件が一度も評価されなくなり、既存検査が捕まえた（D-096 §3）。／~~A-5 Lv.0 ちょうど 1 枚（101.1.1.1）~~ → **2026-09-18 に直した（D-100・rules v0.17）。**`GameConfig.validate` を「1 枚以上」から「ちょうど 1 枚」へ。**準備の配置が同名 Lv.0 を「後勝ち」で黙って選んでいた箇所**（Python の dict 内包・Rust の同等処理）も拒む形にした。**現プールでは挙動不変**（Lv.0 は 9 名それぞれ 1 枚・検査で固定）／~~A-6 回復上限を外す~~ → **2026-09-19 に直した（D-104・rules v0.18）。****D-011（2026-08-21 の裁定「上限 20 でクリップ」）を覆した**——公式 101.6 は開始ライフを定めるだけである。**★SD001 の対局が大きく変わる**（planner で手順 80/200・勝敗 37/200）。基準 8 種を貼り替えた（D-104 §4）。SD02 と BP01 の仮デッキは 0/200。**これで公式ルール差異 12 件はすべて片付いた。**（以下は着手前の経緯）（D-100 §4・クロエの推し）。**理由: A-6 は SD001 の対局を変える＝champion の指紋が動いて貼り替えが要るが、D-098 の貼り替えがまだ一度も回されていない。検証待ちの貼り替えの上に新しい貼り替えを重ねると、不一致が出たときにどちらが誤りか分けられなくなる。**`REBUILD_REQUEST_20260918.md` §4(c) で指紋が確認できてから着手する。あわせて**ライフが 20 を超えうると符号化のライフ欄の正規化が範囲外を取りうる**（R-6 の要確認事項）ので、その便で見る／~~**B-1 `【登場】/【レベルアップ】` を 1 スキル 1 回に**~~・~~**B-2 【レベルアップ】の誘発主体を「下になったカード」に**~~・~~B-3 誘発範囲を置いたカードとその下に限定~~ → **2026-09-17 に直した（D-094）。`rules_draft.md` を v0.13 に上げてから反映**。**B-1 は B-2・B-3 を直すと自動的に解けた**（1 枚のカードが「一番上」と「その下」に同時にならないので、カードデータも Skill の表現も変えていない）。新設検査 `tests/test_official_b123.py` 13 件、既存 2 件を書き換え。**SD001/SD02 は 1 手も変わらない**（digest の基準が一致＝champion の指紋と過去の勝率はそのまま読める）。**BP01 構築の打ち方は変わる**（該当 8 枚の利得が半分）。**★A-3 が未実装なので、下のカードが複数あるときの解決順は暫定の固定順のまま**／B-4 条件の評価を誘発時点に（FAQ 47/48）／**B-5 は「抑制を入れない」で決着**（R-9・D-092 追記 1。代わりに「`_queue_fire_nested` は基本行動 1 回につき 1 回」を `rules_draft.md` に明文化して検査で固定し、B-2/B-3 の列挙を位置で振り分けることで構造的に保証する）／B-6 投了（**実装しない**・`SIMPLIFICATIONS.md` に明記する）／~~**B-7 0 ダメージに削られたら「各ターン最初に受けるダメージ」の旗を消費しない**~~（901.2.1・R-11） → **2026-09-18 に A-5 と同じ便で直した（D-100・rules v0.17）。**`_damage` は旗を立ててから 0 以下を見て戻っていた。**直しの本体は「0 に削られたあと、次に来る本物のダメージがその軽減を受ける」こと**である。**SD001/SD02 は 0/200 で無変化**（軽減を持つのは `BP01-002` だけ）。**BP01 仮デッキ `K_smoke_TSUBAKI` では random で手順 17/200・勝敗 3/200 が動く**／~~**B-9 対抗で出せないターンプレイヤーの手札全公開**~~（604.1.1.2 後段・R-13・D-092 追記 2） → **2026-09-18 に A-3 と同じ便で直した（D-099）。**`_pump` の末尾で `peeked_opp_hand` に流し込む。**★公開の時点は「提出を集める前」にした**——公式の条番号 604.1.1.2 → 604.1.1.3 が「非ターンプレイヤーは公開を見てから置く」と定めるためである（報告 §5 の R-13 はここに触れていなかった）。ターンプレイヤーが置けるかは手札から決まる＝選択が無いので、同時手番の構造は壊れない。**SD001/SD02 でも発火する。素の planner では 0/200 だが、★champion（`known_hand` つき）の打ち方は変わる**——指紋の帯 471500 の 10 局中 1 局・勝敗 0/10（D-101）。**この局面は罰ではなく、603.1.3 で対抗フェイズごと飛ばせる**（実装も飛ばせて正しい）。**planner は 222 回中 0 回で踏まない＝champion は実戦で踏まない**が、**BP01 では【自分の対抗フェイズ開始時】(`BP01-007`)・【各対抗フェイズ終了時】(`BP01-025`) のせいで「入るのが正しい」場合があるので、BP01 を含む評価の前に直すこと。**／~~**B-8 Rust の `match_params` を Python に合わせる**~~ → **2026-09-17 に直した（D-093 追記 1）。食い違いは 1 件ではなく 2 件だった**（`match_params` ＋ `pay_cost_return_self_to_hand` の「支払い済み」の印が Python `paid: true` / Rust `chosen: 1`）。**2 件目は `K_smoke_ANKO` しか回していなかったので見えていなかった**ため、毎手一致を**仮デッキ 3 種 × シード 40**に広げた。500 局・96,367 手で全欄一致・digest の基準も一致・Python は 1 行も触っていない。**★残るのは PC の再ビルドだけ**（依頼は **`engine/REBUILD_REQUEST_20260917.md`**・B-1/B-2/B-3 と合わせた 1 通・5 点セット。`B8_REBUILD_REQUEST_20260917.md` は差し替え済み）。**それが済むまで PC 側の `test_bp01_k3.py` は落ち続け、D-083 の「三者一致」も名乗り直せない。****着手の順序の推し**: B-1/B-2/B-3（rules_draft を **v0.13** に上げてから。SD001 の対局は変わらないので指紋が動かない）→ A-1/A-2 → A-4/B-4 → A-3 → A-5/A-6（ここは SD001 が変わる＝指紋の貼り替えと 1 局ごとの勝敗の突き合わせを同じ便で）。**先に書き換える既存検査**: `tests/test_engine.py::test_deck_out_does_not_lose_the_game`（A-1・D-025 を覆す）・`test_bp01_k2.py::test_enter_and_levelup_fire_on_the_levelup_action_but_not_on_setup`（B-1）・`test_bp01_k4.py::test_return_to_chara_deck_...`（B-2）。**初回報告では修正未実施。別チャット（Codex）は 2026-09-15 に連携中断したので、その後の修正は無いものとして扱う**。確定した差異をPython/Rustへ反映し、対応する回帰検査と毎手一致を確認してから完了にする。今回の作業は状態記録のみで実装変更なし。


- [x] **~~★指紋の貼り替えの検証~~ → 2026-09-18 に完了（D-101）。** 作業環境にネット 4 本を持ち込んで champion を再現し、名指しの節点を全部回した。**D-098 で貼り替えた 4 つの値は 4 つとも正しかった**（b75 は B-9 を外した状態で `6ca46b15dd151228` がそのまま出た）。歴代 3 体は動いていない。以下は経緯。 - **2026-09-18: 依頼は `engine/REBUILD_REQUEST_20260918.md` に A-3/B-9 の再ビルドとまとめた（同じ PC 作業なので 1 通）。**2026-09-17 に歴代 champion 3 体の指紋を貼り替えた（D-098）。`planner_vb3cps` `c84616b0d707a705` / `planner_vc4cps` `2d884df547ac6990` / `planner_vc4cps_kheb` `f3d1b52aed0abdad`、および `test_lit_a2.py` の `B75_FINGERPRINT` を `6ca46b15dd151228` へ。**書き替えた 5 ファイルを一度も回していない**（マスターの環境の都合で 2026-09-17 に検査を中止）。**作業規約 5 の意味でこの便は完了していない。**`PREV_FINGERPRINT`（`test_lit_a2.py` / `test_lit_c.py`）と `B75_FINGERPRINT` は**推定値**で、前回は手前の行で落ちて一度も走っていない。回すのは `python scripts\check_champion_fingerprint.py` と、`tests/test_champion_vc4.py` ＋ `test_lit_a.py::test_defaults_unchanged_lit_a` ＋ `test_lit_a2.py::test_a2_defaults_unchanged` ＋ `test_lit_a2.py::test_a2_rust_p_one_has_the_champion_fingerprint` ＋ `test_lit_c.py::test_kheb_champion_fingerprint` ＋ `test_lit_d.py::test_champion_fingerprint_unchanged_lit_d`（節点を名指しで回すこと。前回 11 節点で 29 分・全体は 2 時間 3 分）


- [ ] **次の champion 交代の判定は新エンジンで測り直してから行う** - D-097 §5（マスター裁定）。過去の勝率は**測り直さないが「旧エンジン（rules v0.14 より前）での測定」と明記する**。理由は、過去の実験は A も B も同じ旧エンジンで測っているので**その実験の中での A と B の比較は有効**であり、崩れたのは「その結論が直したルールの下でも成り立つか」のほうだからである。**旧エンジンの勝率を交代の根拠に使わないこと。**

- [ ] **採用済みの汎用化計画追加3点（2026-09-15）** - `engine/GENERALIST_AI_REVIEW_D086.md` §5.4・§6・§8.4。公式ルール差異修正とStage 1B完了後に観測・符号化・Python/Rust受け渡しの費用を分離。横断教材で構築系統・抽出配分を記録し、大規模化時に類似度補正を比較。通常の追加学習確認後に2ネット交互更新を別便で比較する。**計画反映済み。「観測・符号化の費用分離」は 2026-09-22 に済（D-120・`engine/G1_COST_SPLIT_NOTES.md`）——ボトルネックはネットの推論（82%・うち代打ち π 75〜77%）で、符号化 7%・受け渡し 1% 未満。残る 2 点（類似度補正の比較・2 ネット交互更新）は未着手**。順序と門番は維持し、詳細は計画書を正本とする。

- [ ] **汎用 AI の入力の整備に入れるもの: 公開して手札に入ったカード／見えない出方の漏れ（D-118 裁定）／PT-3 信念の要約（D-119・J-3）（2026-09-21）** - マスター裁定「いまの champion には入れない。汎用 AI を作るときに入れる」。入れる便は `GENERALIST_AI_REVIEW_D086.md` §6 段階1 型の入力の整備で、**段階2（横断教材の生成）より前**。(1) 公開して手札に入ったカード（`reveal_top_to_hand`・`reveal_n_take_matching`・`search_deck`）を観測と符号化に入れる。SD001 の対抗の提出の 48.8〜62.0% で相手の手札の既知が 18〜21% → 37〜41% に増える（D-118 の診断）。(2) BP01-039（`opp_hand_random_to_deck_bottom`）で知識を全部忘れる。BP01 のデッキで学ぶ教材に公開されない情報が混ざるのを防ぐ。欄の形と符号化はその便で決め直す（既存の V に合わせる必要は無い）。診断の道具は `experiments/diag_revealed_hand.py`。**同じ便で PT-3（W_t・既知札数・確定した上下限などの信念の要約）も入れ、符号化の版上げを 1 回にまとめる**（D-119・J-3）。PT-4（先読みの結果を入力に）は段階2 の表現比較の第 3 候補。**PT-4 は数日かかるので範囲は相談してから**

- [ ] **J の実行で時間がかかるもの（D-119・始める前にマスターに相談）** - どれも裁定済み（置き場と禁止事項は決まった）で、実装・測定は未着手。(1) PT-5 オラクル批評家の実装（数日＋反復 1 回・D-026 の例外の条件つき）／(2) PT-4 先読みの結果を入力に（Python と Rust・数日）／(3) PT-2 搾取者リーグの型（型 1 つ 1〜2 日・型ごとに n=600 程度）／(4) PT-1 詰みの証明器の診断（約 1 日・対局なし。19 回の見逃しは旧エンジンの局なので、再現できるかを先に確かめる）→ 候補化 2〜3 日。V の学習に入れる順序は PT-6（分布ヘッド）が PT-5 より先

- [ ] **BP01 の未定義事項 u1〜u18 を発売後の公式ルールで確かめて `unverified` の旗を外す** - **正本は `engine/rules_draft.md` §9-6 の表**（v0.12 に取り込み済み）。**2026-09-10 にマスターが 18 件すべて裁定済み（D-079 追記 1・追記 4）＝実装の解釈は確定**しており、K-3〜K-4 はそのまま書けばよい。**u18（「相手にダメージを与えた時」の範囲＝そのカード自身が与えたときだけ）は K-2 で見つかった新規**。**旗を残すのは「出所が公式テキストではなく我々の読み」だから**で、裁定待ちという意味ではない。確かめる相手は同梱マニュアルと公式 Q&A（発売 2026-09-12）。u17 だけは裁定ではなく便 G への申し送り（＜音骸＞は共通カードなので SD001/SD02 に足した構築が環境に出うる）
- [ ] **★輪 2 の探索器を champion に揃えるか（マスターの裁定・便 E 本体の前に）** - 便 C の交代で champion に `known_hand` / `endgame_enum` / `draw_buckets` が増えたが `vb.py` は変えていないので、**いま輪 2 は一つ前の champion の探索器で回る**（D-081 追記 1）。揃えずに回すと「champion とは違う探索器が作った記録で champion の葉を学ぶ」ことになる。**推し: 便 E 本体に入る前に揃える**（1 便で変える要素は 1 つ、の原則からは 「輪を回す便」で揃えるのが自然）。急がないが、便 E の前には決めること
## Someday

- [ ] **便 E 反復 7' で「2 世代前に勝ち越せているか」を再確認** - D1 の C2（V_1 対 V_3）が上端 **0.488** と際どかった（D-076 判断 3）。磁石つき正則化は入れない方針だが、7' で見直す
- [ ] **計画外の気づき 2 件の裁定** - `provenance._champion_label` の `null`／`PRERELEASE_GOAL.md` の SD02 記述（D-075 の気づいた点・D-076 でも据え置き）。~~`RULES_VERSION` の 1 本化~~ は 2026-09-21 に済（D-117）
- [ ] **`ladder.py --reuse-from` が `rules_version` を照合しない件（D-117 で気づいた）** - 照合はデッキ・モード・体の定義・seed0・局数だけ。A-2（D-095）・A-6（D-104）で同じシードでも別の対局になったので、それより前の組を使い回すと旧エンジンの結果が混ざる。抜き取り検査（`reuse_verify`）で気づけるはずだが未確認。**次にラダーを回すときに、照合に足すか決める**

## Done

- [x] ~~**段階1C-a 観測の整備**~~ (2026-09-22・D-122) — `observe` の `hand_known` を**統一した確かな既知**（`GameState.known_opp_hand`: スキャン・B-9＋公開して手札に入った札・見える形で出たら減らす・見えない形で出たら全部忘れる）にした。旧来は `hand_known_scan`（現 champion と符号化 v5 が読む・champion 退役で消す）。TE-10 の漏れも塞いだ。**★D-023 の積の漏れを発見**（知っていた札を使ったあと同じ番号を見えない形で引くと「知っている」に戻る）——新しい欄には無い。指紋（Python 3 種・Rust champion）不変。TA-9 で知らせた

- [x] ~~**汎用 AI 段階1C の設計の裁定（D-121）**~~ (2026-09-22) — 欄は統一する（旧欄は champion 退役まで一時的に残す）／環境デッキ群はマスターがデッキメーカーで作る／ほかは推し

- [x] ~~**汎用 AI 段階1B後「観測・符号化の費用分離」**~~ (2026-09-22・D-120) — 作業環境（2 コア）で champion 同士の評価 200 局・記録 200 局と callgrind の内訳。**時間の 82% はネットの掛け算・うち代打ち π が 75〜77%**／符号化 7%／Python と Rust の受け渡し・記録の書き出しは 1% 未満。評価 0.179 局/秒・記録 0.173 局/秒・約 196 KB/局・digest 20/20 一致。代打ち π を外すと 4.2 倍速、ネット無しで約 116 倍速。**C 化・バイナリ化は採らない**。PC での測り直しは不要と判断（推し）。帯 823000..823999

- [x] ~~**便 C の判断 31 件／J-1〜J-6 の裁定**~~ (2026-09-21・D-119) — 便 C の 31 件は**推しのまま閉じた**（本数は「増やさない」で確定。ただし探索予算の設計では候補から外さない）。J-1〜J-6 は**6 件とも推しを採り、汎用 AI に寄せた**: J-2 オラクル批評家は教師専用として認める（D-026 に例外を追記）／J-3 符号化 v6 は汎用 AI の入力の整備で D-118 と一緒に／J-5 は PT-6 を先に／J-4 は便 F の前倒し／J-1 は門番を免除しない／J-6 の診断対象は推しどおり。**実行に時間がかかるものは始める前に相談**（Waiting On の項目）

- [x] ~~**マスターの PC: ネットの移行・再ビルド・PC の組の検査（`engine/REBUILD_REQUEST_20260922b.md`）**~~ (2026-09-22・D-124 追記 1) — すべて正常（移行 12 本 max_abs 0・指紋一致・`--pc` の失敗 0）。**段階1C は PC でも閉じた**。PC の AI はまた立つ

- [x] ~~**検査の組を分ける（D-125）**~~ (2026-09-22) — マスター裁定で A（PC は PC にしか無い物だけ `--pc`）と B（重い 20 件に印・既定で飛ばす・`--run-slow`）。作業環境の全検査 40 分のうち 33 分が重い 20 件だった。既定 7 分。組は `tests/test_sets.json`・作り直しは `scripts/make_test_sets.py`

- [x] ~~**段階1C-c 符号化 v6**~~ (2026-09-22・D-124) — v5 の列は動かさず末尾に信念の要約 20 列（PT-3: 既知・未知の枚数・W・食い違い／確定の旗・色とコスト帯の上下限）と統一した `hand_known`（NA）を足した（OBS_DIM 1825 → 1923）。相手のデッキ表は **AI の想定を引数で渡す**（裁定）。葉の V と記録だけが渡し、方策ネットは渡さない（段階2 で口を決める）。ネットは 0 列で移行（`scripts/migrate_nets_v6.py`・max_abs 0）。検査 `tests/test_encoding_v6.py`（11 件・全列挙と上下限が厳密に一致）。champion 指紋一致

- [x] ~~**マスターの PC: Rust の再ビルドと名指しの検査 1 回（`engine/REBUILD_REQUEST_20260922.md`）**~~ (2026-09-22・D-123 追記 1) — **417 通過・1 失敗・3 skip（27 分 23 秒）**。失敗 1 件は `test_versions.py` の時間の検査で、Windows の時計の刻み（約 16 ms）が原因＝検査の側を直し、打ち直しで通過。`opp_from_seat` の札 True・champion 指紋 `f4b80b25c35cfa77` 一致。**PC で全検査を回してよい状態に戻った**

- [x] ~~**段階1C-b 異種デッキの記録入口**~~ (2026-09-22・D-123) — Rust の対局列に `opp_from_seat`（各局で相手の席の行動デッキを相手デッキ表にする・既定は従来どおり）／新 CLI `experiments/record_mix.py`（組み合わせ表・教師の既定はネット無しの計画探索・§5.4 の manifest）／検査 `tests/test_record_mix_1cb.py`（11 件・従来の取り違えを再現してから直ったことを確かめる）。**速度の下見: 2 コアで 1 局約 0.1 秒・記録 1 局約 170 KB → 1 万局は約 9 分・約 1.7 GB**。指紋 4 種一致。帯 824000..824999

- [x] ~~**`MAX_LIFE` という名前を消す**~~ (2026-09-22・D-107 の裁定を D-123 に相乗り) — `state.py`・`state.rs` から削除、検査 2 本を `STARTING_LIFE` に。検査は旧名が戻らないことも見る

- [x] ~~**マスターの PC: 名指しの検査 1 回（`engine/REBUILD_REQUEST_20260921.md`）**~~ (2026-09-21・D-117 追記 1) — **67 passed, 2 skipped（失敗 0）・5 分 53 秒**。skip は `test_lit_d.py` の torch 無し 2 件（PC では正常）。期待どおり

- [x] ~~**送り箱 TE-3・TE-4: 現行 `webapp/` の版の直書きと `ms`**~~ (2026-09-21・D-117・マスター裁定はどちらも推し) — TE-3: 版は `meicho/version.py` の 1 か所に集めた。**同じ取り残しが 5 か所あった**（`webapp/record.py` v0.10・`provenance.py` v0.11・`ladder.py`・`gendata.py`・`train_linear.py` v0.10）ので全部 import に替え、番人 `tests/test_versions.py`（rules_draft.md の見出しとの一致／直書きの一掃）を置いた。**rules の版を上げたら `meicho/version.py` も上げる。**既存の記録は書き換えず、当てにならない欄を D-117 に明記。TE-4: AI の行に `think_ms`（`ai.act` の中だけ）を足した。`ms` は変えない。`APP_VERSION` は 2 のまま（3 は新アプリの番号）。指紋 3 種不変。TA-8 で知らせた

- [x] ~~**マスターの PC: 全検査 1 回（`engine/REBUILD_REQUEST_20260920.md`）**~~ (2026-09-21・D-116) — **985 passed, 26 skipped（失敗 0）・2 時間 43 分**。skip は全部 torch 無し（PC では正常）。期待どおり

- [x] ~~**`results/lit/m5m1_pimc.jsonl` の作り直し**~~ (2026-09-20・D-115) — 元の記録は残し、いまのエンジンで帯の頭の 2 局を回した別名の記録 `results/lit/m5m1_pimc_v018.jsonl` を作って検査はそちらを見る（道具 `experiments/regen_m5m1_pimc.py`）。**作業環境で済んだので PC は使っていない。**0〜58 行目は元の記録と W まで一致＝数え方は不変。検査は「W の数え方の回帰の番人」になった

- [x] ~~**送り箱 TE-5: M1 のトレース点**~~ (2026-09-20・D-114) — `meicho/trace.py` 新設・`engine.py` にトレース点・`tests/test_trace_m1.py`（19 件）。**挙動は不変**（bench の指紋 3 種・Python↔Rust 毎手一致・トレースあり/なしの毎手一致）。Rust は触っていない＝**PC の再ビルドは不要**。仕様書から変えた点 5 つは `engine/TO_APP.md` TA-7。**残り: PC の全検査で新設の検査を含めて回す**（下の「PC への依頼」に同乗）

- [x] ~~**送り箱 TE-2: アプリ用のシード帯の予約**~~ (2026-09-20) — **722000..821999**（10 万個・`kind: "app"`・研究の評価と学習には使わない）を `seed_bands.json` に登録（`next_free` 822000）。`engine/TO_APP.md` TA-5 で返した

- [x] ~~**SD02 の段階の中段を決める測定（マスター裁定: アプリの推しを採用）**~~ (2026-09-20・D-112) — **SD02 は `heuristic`／`greedy`／`planner_lh`**。`greedy` は対 H 0.580 ±0.040・対 `planner_lh` 0.247 ±0.035。素の `planner` は `planner_lh` と区別できない（0.538 ±0.040）ので中段にならない。記録 `engine/TE6_SD02_TIERS_NOTES.md` §7。結果は `engine/TO_APP.md` TA-3。**気づいた点（未裁定）: つよいの段を素の `planner` に替えると強さを落とさず速くなる見込み（D-047 と同じ向き）**

- [x] ~~**TE-6 追加（マスター裁定 C）: SD02 で `planner_lh` より強い版を探す**~~ (2026-09-20・D-111) — **試した 3 体（ネット抜きの champion のつまみ／決定化 12 本／mcts640）のどれも `planner_lh` より強いとは言えない**。決定化 12 本はふるい 0.550（n=300）→ 別帯 0.497 ±0.040（n=600）で上振れだった。記録 `engine/TE6_SD02_TIERS_NOTES.md` §6。結果は `engine/TO_APP.md` TA-2。**SD02 の段階をどうするかは再びマスターの裁定待ち**（所見: 下と間で作るのが現実的＝APP-005 の推しと同じ）

- [x] ~~**送り箱 TE-6: SD02 の強さ 3 段階を確定する測定**~~ (2026-09-20・D-110) — **`planner_lh` と `mcts160` は区別できない**（直接対決 0.513 ±0.040・n=600）。対 `heuristic` は `planner_lh` 0.832 ±0.030／`mcts160` 0.862 ±0.028。帯 715000..715999。記録 `engine/TE6_SD02_TIERS_NOTES.md`。結果は `engine/TO_APP.md` TA-1 で返した。**段階の選び直しは未裁定**（推し: `heuristic` と `planner_lh` の間に素の `planner` などを置いて SD02 で順を測る）

- [x] ~~**並行作業の進め方を決める（2026-09-20）**~~ (2026-09-20・D-109) — 決まりは `LANES.md`、理由と叩き台への採否は `engine/PARALLEL_WORK_REVIEW_20260920.md`（レビューはアプリのチャットが書いた）。依頼の引継ぎ書は `engine/HANDOFF_20260920_PARALLEL_REVIEW.md`。**送り箱 TE-1（切り替えの書き込み 5 件）を終えた（D-109）**: `TASKS.md` のアプリの項目を 1 行に置き換え／`CLAUDE.md` に `LANES.md` の案内・アプリの線・APP-番号を追記／`decisions.md` に D-109 と D-108 の後ろに 1 行／`HANDOFF_CONVENTIONS.md` に系統と送り箱の決まり／`engine/WRITELOG_ENGINE.md` を新設

- [x] ~~**公式ルールと実装の再検証・結果の反映（2026-09-15依頼）**~~ (2026-09-19・D-092〜D-107・クロエが直した／マスターが PC で実行) — **閉じた。**静的照合で確定した差異 **12 件をすべて片付けた**（B-1/B-2/B-3 は D-094・v0.13／A-1/A-2 は D-095・v0.14／A-4/B-4 は D-096・v0.15／B-8 は D-093 追記 1／**A-3・B-9 は D-099・v0.16／A-5・B-7 は D-100・v0.17／A-6 は D-104・v0.18**。B-5 は「抑制を入れない」で決着・B-6 は「実装しない」）。**champion の指紋は 4 種とも貼り替え、PC で一致を確認済み**（`planner_vc4cps_kheb_b75` = `f4b80b25c35cfa77`・D-104 §4）。PC の全検査は **2 失敗 / 964 通過 / 26 skip**、うち 1 件は A-6 の取りこぼしで **D-106 で直して確認済み**、**残る既知の失敗は `test_lit_c.py::test_worlds_module_matches_diag_pimc` の 1 件だけ**（記録が旧エンジン製）。途中で D-101（指紋が B-9 で動いた切り分け）・D-103（BP01 の画像の検査）・D-105（`scripts/` の経路）も出た。経緯は `engine/HANDOFF_20260919_OFFICIAL_RULES_CLOSE.md`。

- [x] ~~**段階1B「符号化v5・Rust・モデル移行便」**~~ (2026-09-16・D-089 と D-091＋追記 1・クロエが直した／マスターが PC で実行) — **閉じた。** PC で Rust が**初めて建った**。止めていたのは `Zone` の import 漏れ（`encode.rs`・`lib.rs`・E0433 × 9 は同一原因）と `state.rs::to_json` の非網羅 match（`Resume::AfterClashCosts` / `StartRush`。Python は実際にこの 2 つを設定するので、**対抗のコスト支払いや連撃の支払いが挟まる局面で Rust だけ状態を書き出せなかった**）。符号化の食い違いは **index 95＝「選択肢の数」**で、Rust が選択の種類ごとに手書きしていて 4 か所ずれていた（`pay_or_damage` の `2` と `reveal_count` の `max+1` を削除、`switch_back` と `order` に `options.len()` を追加。`zone_card` は正しかったので触っていない）。ネット移行 12 本（`.enc4.bak.json` 12 本）／wheel **655,617** バイト（2026-09-17 訂正・当初 655,672 と記載）／`encoding_info()` = **(5, 1825, 317)**。最後は `tests/test_drl.py` **18 通過・3 skip・失敗 0**、champion の指紋 **一致**。**★基準は貼り替えた**——段階1A が決定列に選択行動を足したせいで `BASELINE_DIGESTS_230000` と champion の指紋が古くなっていた（D-091 §3）。champion は `e1662edb32b144a9` → **`9d4ff39d024e4394`**。**★言えないこと 3 つ**: (1) **全検査は回していない**（マスター裁定で今回だけ見送り・D-091 追記 1）。(2) 勝敗そのものの突き合わせをしていないので「打ち方が変わっていない」は推論である。(3) **歴代 champion 3 体の指紋は未再測**で「D-088 以前の値」の札つき。**★踏んだ落とし穴**: 書き戻したはずの `encode.rs` が PC に載っておらず（**更新日時だけ進んで中身が古い**・OneDrive）、同じ失敗の値が 1 ビットも違わないことで気づいた。**書き戻したらサイズか中身で載ったことを確かめること。**

- [x] ~~**Kaggle「ポケモンTCG AI Battle Challenge」上位解法の読み込み（文献読解の便）**~~ (2026-09-16・D-090・クロエが直接実施) — `engine/HANDOFF_20260916_PTCG_WRITEUPS.md` の依頼。記事 12 本を読み `engine/PTCG_KAGGLE_NOTES_20260916.md` に要約と提案 **PT-1〜PT-10** を書いた。計画書 §10.11 に追記。**champion・探索器・帯・計画の順序は不変。実装・測定なし。**判断が要る点 **J-1〜J-6** は次の引継ぎ書 §0.3 で閉じる（推しは J-2 の V_oracle 以外は既存規約の延長）。**いちばん安い次の一手は PT-1 の診断**（対 H 40 局の詰み見逃し 19 回＋T-14＋対人 6 局のうち「公開情報だけで証明可能な詰み」の割合・対局なし・1 日）

- [x] **公式総合ルール・FAQのMarkdown化と初回監査報告をmainへ保存**（2026-09-15・`ff4f647`） - `engine/official_rules/`の3文書を保存済み。初回監査は作業ブランチ `stage1b-encoding-v5` のPython/Rustソースとの静的照合で6件を報告。**この完了は文書保存と初回静的監査のみ**で、全ルール一致・再検証完了・実装修正完了を意味しない。初回はPythonテスト未実行、Rustビルド未実行。再検証と修正は上の別項目で管理する。

- [x] ~~**段階1A「自動選択のPython実装便」**~~ (2026-09-14・D-087/D-088) — 協奏の支払い、K-3/K-4の公開領域選択、効果レベルアップ、準備のバック順をPythonの合法手にした。同一IDは1候補へ畳み、旧AIの既定回答とfingerprint 3種は不変。対局記録はv2、v1は旧既定回答を補って再生。新設検査8件、関連検査189通過・20 skip・失敗0。符号化v4・Rust・モデルは未変更で、非対応は段階1Bへ

- [x] ~~**PC の Rust を再ビルドする（段 K-5 の残り）**~~ (2026-09-13・D-085 追記 1・マスターが実施) — **これで段 K-5 は完全に閉じた。** `features()` に **`bp01_k5`** が出ることを確認。古い wheel は新しい op で panic して SD001 の対局すら始められない状態だったので、任意ではなく前提だった。**名指しで通過を確認した 3 つ**: `test_bp01_k5.py` **9 件すべて通過（skip 無し）**／`test_rust_matches_python_on_every_smoke_deck` **12 件通過**（3 スモークデッキ × 4 シード。スモークデッキに 3 枚足したので今回いちばん動いた場所）／`test_the_two_published_cards_have_their_effects` **通過（xfail ではなく）**。**★全検査の通過・失敗・skip の総数は受け取っていない**ので、「全件で失敗 0」とは言えない。総数が要るときは PC で回し直して skip の内訳ごと数えること

- [x] ~~**`BP01-057` と `BP01-062` の効果を入れる（便 K の残り・段 K-5）**~~ (2026-09-13・D-085・クロエが直接実施) — **便 K の積み残しが無くなった。** 語彙を 3 つ足した: `grant_rush_draw_to_variation_skills`（u19・数えは新しい状態欄 `variation_rush_draw`・連撃で使った＜変奏スキル＞だけを数え、追撃と同じく**加算**、ターン終了で消える）／常在型の `cost_mod`（u20・**下限 0**・`_effective_cost` が読むので使用条件にも支払いにも効く）／`levelup_by_effect` の任意 `level`（u21・**省略時は従来どおり**なので既存カードは不変）。リーダー切り替えと【判定】は既存の op で足りた。**rules は v0.12 のまま・符号化は v4 のまま＝ネット移行は不要**。**挙動不変の根拠 4 つ**: Python の fingerprint 3 種 不変（`773a71c15c5bc16e` ほか）／champion の fingerprint 不変 **`e1662edb32b144a9`**／**Rust の digest が SD001・SD02 × {H 同型, 貪欲} × 各 200 局で変更前の wheel と全局一致**／新しい語彙を使うカードが 2 枚だけであることを検査で固定（T-K5-9）。新設 `tests/test_bp01_k5.py` 9 件、**T-K-10 は設計どおり「予想外に通った」で落ちたので旗を外した**。作業環境で **490 通過・skip 3・失敗 0**。**★3 枚をスモークデッキに入れた**（`BP01-057`/`BP01-062` は `K_smoke_ANKO`、`BP01-049` は `K_smoke_TSUBAKI`。抜いたのは共通カードの 2 枚目なので被覆は減っていない）——入れないと「効果があるのに 1 度も踏まれないカード」が 3 枚残る。**★計画外に 1 件直した**: `test_no_image_allowlist_is_consistent` の `assert allow` が、D-084 追記 1 で許容リストが空になった日から「読めているのに落ちる」検査になっていた（D-082 追記 3 と同じ型の誤り）

- [x] ~~**撮れていない 19 番号を撮り足す**~~ (2026-09-13・D-084 追記 1・マスターが撮影／クロエが対応づけ) — BP01-017 / 020 / 044〜047 / 051 / 052 / 054〜056 / 060 / 063〜069。**ちょうど不足分の 19 番号で過不足・取り違えなし**。`cards/` 直下 104 → **123 枚**＝**実装カード 123 番号すべてに画像が揃った**。`images.status()` は indexed 123 / implemented 123 / **missing・no_image・stale_allowlist・duplicates すべて空**。`cards/BP01_NO_IMAGE.json` の許容リストは**空**（58 → 19 → 0）

- [x] ~~**カード画像をマスターの手動スクショに切り替える**~~ (2026-09-13・D-084・クロエが対応づけ／マスターが PC で実行) — **142 枚を正本にした。** 番号はファイル名に無いので、カード右下に刷られた番号の帯だけを切り出して一覧に貼り合わせ、**142 枚すべてを目視で確定**（OCR は精度が足りず補助）。`cards/apply_manual_shots.ps1` を 1 回実行。内訳は**固有番号 104**（SD01 23・SD02 23・BP01 58）＋**パラレル 38**（`-R` 22・`-SP` 9・`-PR` 7）で、**新しく画像がついた番号は 39**。解像度 620〜626 × 868〜882 px（旧 334 × 485）＝効果文が読める。114.1 MB（旧 18.6 MB・画素数 3.4 倍 × 枚数 1.9 倍／1 画素あたりのバイト数は不変）。旧 76 枚は `cards/_old_images/` へ**移動**（消していない）、原本は `cards/手動スクショ/` に残る。**★パラレルは `cards/parallel/` に分けた**——`build_index` は `-R` を持たない方を選ぶので、`-SP` のような接尾辞は「パラレルでない」と判定されて**通常版を押しのける**。サブフォルダは `os.listdir` の 1 階層から構造的に見えないので取り違えようがない。`cards/` 直下は 1 番号 1 ファイル。**★副産物: D-063 v3 の既知の取り違えが消えた**（`SD01-019_轟音.png` が `SD01-018` の複製で轟音の画像が一度も入っていなかった件。`KNOWN_DUPLICATES` を空にし、「直ったら落ちて知らせる」検査は役目を終えたので削除）。`cards/BP01_NO_IMAGE.json` を **58 → 19** に更新（しないと `stale_allowlist` に 39 番号が出て落ちる）。`images.status()` は indexed 104 / implemented 123 / **missing 空・unused 空・duplicates 空**、`test_card_images.py` ＋ `test_cards_folder.py` **25 通過・失敗 0**。**★踏んだ落とし穴: 取り込んだ `webapp/images.py` と `BP01_UNLISTED.json` が PC の実物より古い版で、そのせいで「許容リストは未配線」「未掲載 3 番号は未取得」と 2 回誤報した**（OneDrive 配下・フォルダ一覧の更新日も古かった）。経緯は `cards/MANUAL_SHOTS_20260913.md`

- [x] ~~**便 K（BP01 のカードデータをエンジンに入れる線）**~~ (2026-09-13・D-083 と追記 1〜5・クロエが書き戻し／マスターが PC で実行) — **完全に閉じた。** 38 ファイルの書き戻し（衝突 4 件は手で継いだ）→ ネット移行 12 本 → Rust 再ビルド（wheel 624,467 バイト）→ **全検査 774 通過・25 skip・xfail 1・失敗 0**（除外なし・1 時間 7 分）→ `verify_report.py K` **行 30／不一致 0／再計算できない行 0**。rules **v0.12**・符号化 **v4**（`NA` 78 / `NC` 45 / `OBS_DIM` 1,313 / `ACT_DIM` 226）・BP01 の **87 種がすべて揃った**（正本 169 レコード / 123 番号）。**三者一致（Python / Rust / Windows）が名乗れる状態になった**

- [x] ~~**便 A 後半の次の便を裁定する**~~ (2026-09-13・マスター裁定) — **AI 線（便 E / B / F）はいったん止め、カード線＝便 K の展開へ進む。**理由は「CPU の性能は足りている・ブースターパックのカード性能が公開されたので次の段階へ進みたい」。**積み残し（便 E 本体・便 B・便 F）は「今後より AI を強化したいとき」まで後回し**——クロエの推しは (c) 便 F の前倒しだったが採らなかった。便 F の中身は §3.6 と D-083 に書いてある

- [x] ~~**PC の Rust を再ビルドする（便 A 後半の交代の残り）**~~ (2026-09-13・マスターが実施) — **これで便 A 後半は完全に閉じた。** `rust/dist/meicho_rs-0.1.0-cp311-cp311-win_amd64.whl` が **596,762 バイト・2026-09-13 15:54（JST）** に作り直され（前の版は 584,927 バイト・09-11 18:49）、**再ビルド後の全検査が通った**（マスター報告）。**これで `bundle_p` を使う道具（ラダー・`arena_rs`・門番・`tests/test_lit_a2.py` の Rust を要する 8 件）が PC でも動く。**交代そのものは D-082 追記 2、交代で落ちた検査の直しは追記 2・3 にある

- [x] ~~**champion 交代 `planner_vc4cps_kheb` → `planner_vc4cps_kheb_b75`**~~ (2026-09-13・マスター裁定・D-082 追記 2・クロエが直接実施) — **D-081 の 5 条件をすべて満たしたうえでの交代**: 門番 別帯 n=1,200 で **0.530 [0.502, 0.558]**／対照 null 0.503 [0.475, 0.532]／錨 3 種は 9 本すべて 0 を含む（どこでも弱くなっていない）／fingerprint 4 種 不変・新 **`e1662edb32b144a9`**・一つ前 `e82b796960e04c4a` 不変／ラダー core5 v13 で **Nash の台に単独** nA=0.0000・Elo 1 位。**中身は便 C の 11 個 ＋ `bundle_p = 0.75` だけ**（1 便で変える要素は 1 つ）。**直した 4 か所**: `experiments/champion.py`／`gauntlets/core5.json`（**v14**・`champion` 欄）／`webapp/agents.py` の `DEFAULT_OPPONENT`（旧 champion は一覧に残す）／`experiments/provenance.py` の表示名。**★証拠は便 C の交代よりずっと薄い**——門番の余裕は **0.002**（便 C は 0.013）・**T-14 は 1 手も直っていない**・費用 +27.0%。**閾値は測る前に固定し、測ったあとに動かしていない**（D-081 の撤回を前例にしない）が、**この 0.002 も次の便の前例にしない**。**交代で検査が 7 件落ちた**: 読み替えて直した 4 件（`test_champion_vc4.py` 3・`test_lit_c.py` 1）と、**書き方の誤りを直した 3 件**（`test_lit_a2.py` が `champion.kwargs_for` を**その場で読んで**基準を作っていた——**§10-5 で一度学んだことの繰り返し**。便 C の 11 個を明示の定数に固定し直した）。**1 件も消していない。**最終 **通過 703／失敗 0／skip 26／xfail 1**。**残るのは PC の Rust 再ビルドだけ**（Active）

- [x] ~~便 A 後半（A-2 束ねたソルバ `bundle_p`）~~ (2026-09-12・D-082・クロエが直接実施) — **§3.1 の便 A が閉じた。** 新しいつまみ `bundle_p`（既定 0.0・Python と Rust）＝AI の提出分布を決定化 K 本に**共通**に置いて束ねたゲームを解き、**平均戦略**の最大の手を出す。**既定で挙動不変**（fingerprint 4 種・Rust の digest）／**`bundle_p=1.0` は現行 champion と一手・一点・乱数の消費まで同じ**（計画書の「p=1 で A-1 と一致」を置き換えた）。ソルバは `meicho/bundle.py` ＋ `rust/src/bundle.rs`（**交互更新の予測つき RM+**・反復 300・乱数を引かない・3×3 の厳密解と可搾取度 **1.35e-4**）。**★効いたのは被搾取だけ**: 現 champion 0.145 ±0.028 → **b75 0.180 ±0.031**（+0.035・0.025 を越えた唯一の候補）。**錨 3 種は 9 本すべて 0 を含む**。費用 +27.0%。門番（b75 だけ・別帯・n=1,200）**0.530 [0.502, 0.558]**・対照 null 0.503。**ラダー v13 で Nash の台に単独**（nA=0.0000・Elo 1555・**1 時間 55 分**・82 組を使い回し・抜き取り 9 組は完全一致）。検査 **702 通過・失敗 0・skip 26・xfail 1**（新設 18 件）。**★★ T-14 の 9/3 の 2 敗は 1 手も直らなかった——葉の V・世界の作り方・対抗の集約規則の 3 便続けて、自己対戦では前進して対人の穴は動かない。この 3 つは的の外にある**。**★わざと壊す 3 つのうち「Rust の足し算を降順」は対局の毎手一致をすり抜けた**ので、ソルバの戻り値を**ビット単位**で比べる検査に替えた（`meicho_rs.solve_bundled` を検査専用に開けた）。**★予測つき RM+ は最終反復も収束する**ので「平均戦略を返しているか」は退化した行列でしか見分けられない。**★作業環境は PyPI と crates.io へ出られなかった**——Rust は**マスターの PC の `~/.cargo` の `.crate` 36 個を持ち込んでオフラインでビルド**（sha256 は `Cargo.lock` と全一致）、pytest は `_shim/` の間に合わせ。帯 709000..714999（`next_free` 715000）＋**便 K の 706000..708999 を代行登録**

- [x] ~~便 K 段 K-5（仕上げ）~~ (2026-09-11・D-079 追記 7・クロエが直接実施) — **便 K の実装側は閉じた**。仮デッキ 3 つ × **1,000 局**を Python と Rust で回して**打ち切り 0・例外 0・数字は完全一致**（`K_smoke_ANKO` 平均 23.20 ターン／`K_smoke_SANGE` 32.45・引き分け 1／`K_smoke_TSUBAKI` 25.95）。道具は `scripts/smoke_bp01.py`（新設）。`experiments/verify_report.py K` を新設し **行 30／不一致 0／再計算できない行 0**（`results/k/manifest_K.json`）。**アプリでの表示を検査に固定**（画像の無いカードが名前＋効果文で出ること／`unverified` のスキルに「※テキスト未確認」が出ること）。検査 **426 通過・21 skip・失敗 0**（この作業用の写しに材料が無い 3 モジュールを除外）。**T-K-1 の digest は K-1 から 1 つも動いていない**。**★K-4 までのスモークは A-3 の探索と同じシード帯を踏んでいた**ので、帯 **706000..708999** を登録して回し直した（`next_free` 709000）。**★Rust には `RandomAgent` の型が無い**ので `rs.series` の `{"kind": "random"}` で回す。**残るのは Windows の Rust 再ビルドだけ**（5 点セットの依頼文は `BP01_NOTES.md` §K-5。**送るのは便 C の交代判定のあと**）

- [x] ~~便 K 段 K-4（残りのキャラ固有・21 番号）~~ (2026-09-11・D-079 追記 6・クロエが直接実施) — **BP01 の 68 番号すべてに効果が入った**（スキルを持つ 64 枚・バニラ 4 枚）。**T-K-4 通過**。新 opcode 12・新条件 2（Python と Rust の両方）で opcode の分岐 37 → **50**・条件 13 → **15**。`unverified` のスキル **62 件**。**スモークデッキを 3 つに増やした**（§3.6 は 2 つだが、2 つでは 散華・漂泊者の BP01 カードが 1 度も盤に出ない）。3 デッキとも乱択 60 局で**打ち切り 0・例外 0**。検査 **389 通過・3 skip・失敗 0**。**★付与スキルは付与先にスキルを生やしてはいけない**（`_action_timing_index` のキャッシュがカード定義の不変に寄りかかる）。**★Rust の取りこぼしがもう 1 件**（`own_card_damage_buff` の走査。K-3 の 2 件と同根）。**★opcode のパラメータのキャラ名・カード名は `NAME_PARAM_KEYS` で伏せる**（K-2 と同じ罠の 2 度目）
- [x] ~~便 K 段 K-3（【優勢】と＜音骸＞・12 番号）~~ (2026-09-11・D-079 追記 5・クロエが直接実施) — ＜音骸＞のエリア 1 枚制限 (u5)・協奏エリアの「2 種類以上」(u6・u7)・妨害 5 種（**乱数は 1 枚動かすごとに 1 回**）。処理済み 47/68。opcode の分岐 31 → 37。検査 364 通過・3 skip・失敗 0。**★BP01 のデッキで回して初めて Rust の取りこぼしが見つかった**（`test_rust_engine.py` は SD001/SD02 を回すので BP01 を 1 度も踏まない）→ `tests/test_bp01_k3.py` に**仮デッキでの毎手一致**を足した。**★妨害効果の「選ぶ側」(u8) は自動選択（左端）にした**——選択肢にすると `CHOICE_KINDS` が伸びて符号化の版上げがもう一度要るため
- [x] ~~便 K 段 K-2（新しいタイミングと状態欄）~~ (2026-09-10・D-079 追記 3・追記 4・クロエが直接実施) — **`rules_draft.md` を v0.11 → v0.12 に上げた**（§3.2・§7・§9・§10）。`Timing` 6 → **13 種**・状態欄 **＋12**・条件 5 → **13 種**・opcode の分岐 19 → **31**（Python と Rust の両方）。台帳の (b) 群 **20 番号**の効果を実装＝効果が入ったのは **35/68**。検査 **343 通過・3 skip・失敗 0**（`tests/test_bp01_k2.py` を新設・15 件）。**T-K-1 の digest 8 本は K-1 から 1 つも動いていない**。**★効果の解決の途中で誘発するタイミングは既存の待ち行列に載せられなかった**（`_queue_fire` が `pending_skills` と `choice_resume` を置き換えるため）ので、割り込み専用の `pending_triggers` を足した。**★opcode のパラメータにカード番号を書いたら `test_card_space` が「機構の仕様にカード ID が漏れている」で落ちた**（D-050 条件 1）ので `pending_effect["card"]` から取る形に直した。**★Python/Rust の差 3 件はすべて「片方だけ直した」**で、毎手一致の検査が拾った。**★u18 を新発見・裁定（そのカード自身が与えたときだけ）。推しの理屈が誤っていた**（実装の都合をルールの範囲の話にすり替えていた・D-079 追記 4）
- [x] ~~便 K 段 K-1（68 番号＋3 枠の登録・符号化 v4・ネットの移行）~~ (2026-09-10・D-079 追記 2・クロエが直接実施) — 登録簿 **52 → 123 枚**、`NA` 34→**78** / `NC` 18→**45** / `OBS_DIM` 596→**1,313** / `ACT_DIM` 111→**226**、`ENCODING_VERSION` 3→**4**。**Python と Rust がどちらも `(4, 1313, 226)`**。`reconcile_cards` **食い違い 0 / 未登録 0**。**T-K-1 の digest 8 本すべて不変**＝カードを 71 枚足しても SD001/SD02 の対局は 1 手も変わらない。`scripts/migrate_nets_k.py` を新設。`heuristic.LEADER_COLOR` を導出化（D-047 監査の「高」）。画像の許容リスト `cards/BP01_NO_IMAGE.json`（58 番号）。**★ネット移行の検算は float32 では測れない**（`pi_small64_e10` で 1.05e-5 出たが float64 では 3.55e-15＝厳密。差は BLAS の足し算の順序）ので**関門を float64 の一致に移した**。**★T-K-4 が通る段が K-4 → K-1 に前倒し**（K-0 の裁定 1 の帰結）
- [x] ~~便 K 段 K-0（台帳と不変の検査）~~ (2026-09-10・D-079・クロエが直接実施) — **対局なし・登録簿は 1 行も触っていない**。`cards/` の正本を **166 レコード / 120 番号**に更新（既存 76 行はバイト単位で不変）・`cards/BP01_MECHANICS.md`（68 番号の機構台帳）・`engine/rules_draft_v0.12_proposal.md`・`cards/BP01_UNLISTED.json`・`engine/tests/test_bp01.py`・`engine/BP01_NOTES.md`。`reconcile_cards` **食い違い 0 件 / 未登録 68**。検査 **19 通過・1 xfail（T-K-4）・失敗 0**。分類は **K-1 15 / K-2 20 / K-3 12 / K-4 21**。新しい語彙は **op 26・Timing 7・状態欄 12・条件 10**（既存の 2 倍以上）。**★ 段ごとに登録すると符号化の版上げが 4 回になると分かったので、K-1 で一度に登録する（裁定 1）**
- [x] ~~作業の整理と便 K（BP01 のカード入力）の引継ぎ書~~ (2026-09-10 夜・D-078 追記) — 残る作業を **AI 線（便 C）とカード線（便 K）の 2 本**に分けた。`engine/HANDOFF_20260910_CARDS_K.md`。未登録は 68 番号（前から 13＋新規 55）。**機構の追加（音骸 10・優勢 8・登場 8・レベルアップ誘発 10 ほか）で rules v0.12 に上げてから実装**。**訂正: 既存 `cards_structured.json` は 76 レコード／65 番号**（「76 番号」は誤り。新規 55 は不変）
- [x] ~~カード画像の取得（要否）~~ (2026-09-10・マスター裁定) — **取らない**。テキストで十分。BP01 の新規番号はアプリで名前と効果文の表示。経路の記録（マスターの Chrome 経由・圧縮 webp のみ・「SAMPLE」透かし）は `cards/OFFICIAL_FETCH_20260910.md` と D-078 に残っている
- [x] ~~裁定 2 件（公式テキストの割れ）~~ (2026-09-10・D-078) — **どちらも「任意（〜てもよい）」が正**（マスター裁定）。`BP01-033`「協奏エリアに**置いてもよい**」／`SD02-006`「手札に**加えてもよい**」。**いずれも既存 `cards_structured.json` と一致＝公式サイト側の表記が誤り**。簡体字「散华」4 件は「散華」に、`BP01-032` ★ の行頭 `■` は補って統一。採用値は `adopted_effect` / `adopted_name` の欄に並記し、取得記録の `effect` / `name` は書き換えていない。**D-078 を消費した（次の便は D-079 から）**
- [x] ~~第 1 弾（BP01）のカードリストを公式サイトから取得~~ (2026-09-10) — **全 191 レコード・失敗 0・転記は行単位のハッシュで全行一致を確認**。ユニーク番号 120（BP01 74 / SD01 23 / SD02 23）。BP01 封入は 84 番号（キャラ 37 / アクション 47）。**欠番 3 件は公式が未掲載**（商品詳細の 87 種と差分が一致）。既存 `cards_structured.json` 76 番号との照合で**内容の相違は 2 件だけ**（`BP01-033`・`SD02-006`）、残りは表記の差。**簡体字「散华」の混入 4 箇所はローカライズ漏れ**。成果物は `cards/cards_official_20260910.json` / `.csv` / `OFFICIAL_FETCH_20260910.md`

- [x] ~~PC の Rust 再ビルド（新 champion の `draw_buckets` ぶん）~~ (2026-09-11・マスター実施) — 完了。これで PC でも Rust 側で新 champion を回せる（ラダー・門番・fingerprint）。**便 K のカード拡張ぶんの再ビルドはまだ**（zip の展開時に別途）
- [x] ~~便 C の次の便の裁定~~ (2026-09-11・マスター裁定・D-081 追記 2) — **便 A 後半（A-2 束ねたソルバ）**に決まった。引継ぎ書 `engine/HANDOFF_20260911_LIT_A2.md` を書いて PC に置いた。**計画書 §3.1 からの読み替え 1 件**: A-1 が実装されていないので「p=1 で A-1 と一致」を「p=1 で現行 champion とビット一致」に置き換えた。便 A 前半の判断②・③（`lu50` の交代・追試）は**2 世代前の champion 上の値なので取り下げ**（アプリの候補としては残す）
- [x] ~~**champion 交代 `planner_vc4cps` → `planner_vc4cps_kheb`**~~ (2026-09-11・D-081 と追記 1・クロエが直接実施) — **マスターの指摘「0.51 くらいを上回れば交代する規則だったはず」から記録を当たった結果、0.55 のほうが新しい条文だと分かった**（元の D-034 裁定 4＝2026-08-24 が「n≥300・95% 下端 > 0.5」、GSPRT・p₁=0.55 は 2026-09-09 の改訂 1）。**マスター裁定: 改訂 1 だけを撤回**（改訂 2 のラダー条件は残す）。**これは事後の閾値変更である**——撤回は 0.534 を見たあとなので、この 1 件は事後選択。記録は「規則に照らして自動的に通った」ではなく「**マスターが規則を直したうえで通した**」とし、**以後この前例は使わない**と D-081 に明記した。戻した門番で 5 条件すべて満たす: 直接対決 **別帯 0.542 [0.513, 0.570]**（n=1,200）・合算 **0.534 [0.514, 0.554]**（n=2,400・Elo +23.5）／**ラダー core5 v11 で Nash の台に単独で乗り nA = 0.0000**（前 champion は台から落ちて −0.2141）・Elo **1542 [1521, 1565] の 1 位**（2 位 1522 [1500, 1546]・**区間は重なる**）・非推移性 0 組／監査 違反 0／fingerprint 新 **`e82b796960e04c4a`**・前 `9b5ad48d2de6a7e0` 不変／マスター裁定。**★ラダーの使い回しが実戦で働いた**——91 組のうち 70 組を v9 から使い回し、候補が絡む 13 組と抜き取り検査 8 組で **3 時間 11 分**。**抜き取りは 8 組とも記録と 1 局も違わず**、前 champion の Elo は v9・v11 で完全一致。使う前に Python↔Rust の一致も 4 組 × 40 局で確かめた。**★都合の悪い数字**: ラダー内の直接対決（n=300）は 0.553 [0.497, 0.609] で**下端が 0.5 をわずかに割る**。`ladder.py` 自身の「champion 候補」欄も「なし」と出る（あれはラダー内 n=300 の簡易判定で条文とは別物）。**★交代で検査が 10 件落ちた**（反映漏れ 3・便 C の土台の崩れ 4・由来の表示名 2・検査が狭すぎた 1）。全部**読み替えて**直した（消していない）。最終 **673 通過・26 skip・失敗 0**／`verify_report.py C` **296 行・不一致 0**。**★輪 2 の探索器が champion とずれた**（`vb.py` は不変）＝Waiting On。**残るのは PC の Rust 再ビルドだけ**（依頼文は `engine/WORKER_HANDOFF_LIT_C.md` §9。アプリは再ビルド前でも動く）
- [x] ~~便 C の交代判定（最終候補 `planner_vc4cps_kheb`）~~ (2026-09-11・D-077 追記 5・**結論は同日 D-081 で覆った↑**) — **（当時の条文では）交代しない**。門番 GSPRT（別帯 700000）が**不合格側で止まった**（2,280 局・1,140 ペア・LLR −2.47）。固定 n=1,200 は 0.542 [0.513, 0.570]、段 C-4 の帯の 0.526 と**合算して 0.534 [0.514, 0.554]（n=2,400）＝Elo +23.5**・費用 +17.3%——**強くはあるが門番が問う 0.55 には届かない**。対照 0.506 [0.478, 0.534]（0.5 を含む＝配線は健全）・監査 違反 0・fingerprint は champion `9b5ad48d2de6a7e0`（不変）／候補 `e82b796960e04c4a`。**★いちばんの学び: 段の門番を 3 回通っても交代の証拠にはならない**——同じ比較を段ごとに検定するのは多重検定で、真の値が p₀ と p₁ の中間にあると帯によってどちらにも転ぶ。段の門番は関門、交代は別帯で 1 回。**★ラダーを 5〜9 時間から約 2.4 時間にする仕組みを作った**（`ladder.py --reuse-from` / `--reuse-verify`）——v9 の 78 組は体の定義が変わっていないので使い回し、候補が絡む 13 組だけ回す。**体は 1 つも減らさない**。v9 の 4 組を回し直して**完全一致**を確認済み。**ラダー v11 は回していない**（交代しないので。**のちに撤回後に回した**）。検査 670 通過・26 skip・失敗 0
- [x] ~~便 C 段 C-4（ドロー結果のバケット化 `draw_buckets`）~~ (2026-09-10 深夜・D-077 追記 4) — **残す**。**門番 GSPRT 合格**（720 局・360 ペア・LLR +3.71）／**固定 n=1,200 は 0.526 [0.498, 0.554]＝区間は 0.5 を含む**（言えるのは「弱くなっていない・おそらく少し強い」まで）。錨 3 種とも上端 ≥ 0（H +0.010／貪欲 +0.013／素 planner +0.020）。費用 +17.3%。監査 違反 0。検査 **666 通過・26 skip・失敗 0**。**★被覆率は動かない（cov_hand K=6 が 0.137 → 0.139）——これは「効かない」ではなく「この段の物差しの外」。C-1 と C-3 が直したのは相手の手札の集合、C-4 が触るのはこれから引く札の並びだから**。新設 `meicho/buckets.py`・`rust/src/buckets.rs`・`experiments/c4_cost_audit.py`。候補は **`planner_vc4cps_kheb`**（引継ぎ書の `khweb` から読み替え）
- [x] ~~便 C 段 C-3（II-9 終盤の整合世界の全列挙）~~ (2026-09-10・D-077 追記 3) — **残す**。**門番 GSPRT 合格**（1,160 局・580 ペア・LLR +3.65）／**固定 n=1,200 は 0.535 [0.507, 0.563]＝区間の下端が 0.5 を上回る**（段 C-1 単独の 0.519 より強いが、別の帯なので引き算はできない）。錨 3 種とも上端 ≥ 0（H −0.003／貪欲 +0.020／素 planner +0.040）。**★決定の 13.5% で「ありうる相手の手札」は 64 通り以下しか無く、そこを引かずに全部並べると真の手札を持っている割合は 0.741 → 0.960、W ≤ 16 の 1,081 決定では 1.000（取りこぼしが原理的に無い）**。**★対人局の「烈火を読み切れなかった負け」の局面（W=48）が 0.10/0.50 → 1.00 になった**。**★投票はほとんど手を変えていない（列挙に入った決定の 11.8%）＝効き目の大半は世界の集合が正しくなったこと**。費用 +14.0%。監査 違反 0。検査 662 通過・26 skip・失敗 0
- [x] ~~便 C 段 C-2（II-8 π₀ の到達確率の重み）~~ (2026-09-10・D-077 追記 2) — **落とす**（門番 GSPRT が不合格側で止まった・400 局・LLR −2.91／固定 n=1,200 は 0.505 [0.477, 0.533]＝互角／費用 +24.8%）。**★なぜ効かないかも分かった: η の定義で「t 以降に相手が出した札」を足し戻すので、相手が出した札はどの世界にも入ってしまい、世界を分ける力が消える**（実装は設計どおり・設計が的を外している）。**★2×3 掃引で引継ぎ書の予想が外れた: 本数 6→24 で +0.055〜+0.067。本数 24・重みなしのセルは下端 0.532 が 0.5 を上回る＝「lc が低いから本数増は効きが薄い」は否定された**（ただし費用 2.4 倍・セルどうしの差は断定できない）。検査 655 通過・26 skip・失敗 0
- [x] ~~便 C 段 C-1（II-7 (a) `known_hand`）~~ (2026-09-10・D-077 追記 1) — **残す**。**門番 GSPRT が合格側で止まった**（640 局・LLR +4.00。回す前の予想「不合格側に止まる」は外れた）。固定 n=1,200 は **0.519 [0.491, 0.547]**＝大きさは「弱くなっていない・おそらく少し強い」まで。錨 3 種とも上端 ≥ 0。**被覆率 cov_hand は K=6 で 0.047 → 0.110、見えている札がある決定に限ると 0.018 → 0.257（14 倍）。1 本あたりの当たりやすさ（TSSR プール比）は 0.449 → 1.019 ＝公平になった**。費用 +1.8%。監査 違反 0。検査 647 通過・26 skip・失敗 0。**引継ぎ書 §0.3 (x) の「Rust に `peeked_opp_hand` が無い」は誤りで、最初から在った**
- [x] ~~便 C 段 C-0（片付けと測る道具）~~ (2026-09-10・D-077) — **いまの探索は真の相手手札をほとんど見ずに手を選んでいる**（決定化に真の手札が入る割合は K=6 で 0.047・**K=24 でも 0.069**＝本数増は効かない／1 本あたりの当たりやすさは公平の **45%**）。一方**相手がその対抗で出す札は K=24 で 0.996 が候補に入っている**＝読み違いは「重みの置き方」の側。道具 3 つ（`coverage.py`・`verify_report.py`・`manifest_lit_c.json`）。帯 676000..705999・`next_free` 706000。検査 642 通過・26 skip・失敗 0
- [x] ~~由来の `host` に `cowork-2` を足す~~ (2026-09-10・D-077) — `provenance.HOSTS` に追加。**知らない host 名は拒否する**ようにした（既定は `workenv-2` のまま）
- [x] ~~便 M の結果を照合し、便 C の引継ぎ書を書いて PC に書き戻す~~ (2026-09-10) — 照合はすべて一致。訂正 1 件（`hand_known` の効きは全決定平均 0.743 ではなく、効く場面で 8〜20 分の 1）

- [x] ~~便 M の結果を受け取る（D-076）~~ (2026-09-10) — 分岐の答え 3 つ: **便 C は全部**（天井 +77 Elo）／**便 B は急がない**（中盤 D_t 中央値 0.095）／**便 E 7' の磁石は不要**（循環なし）。訂正 3 件と D-034 改訂条文も入った。検査 636 通過・26 skip・失敗 0
- [x] ~~便 M（診断の残り）を作業者に渡す~~ (2026-09-09)
- [x] ~~便 M の引継ぎ書を書いて PC に書き戻す~~ (2026-09-09)
- [x] ~~便 D（後半）の結果を照合し、zip を PC に展開~~ (2026-09-09)
