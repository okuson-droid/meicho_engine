# MeichoSim（オンライン対戦アプリ）が要るもの。エンジン（meicho/）は標準ライブラリだけで動く。
aiohttp>=3.9
# CPU 対戦の つよい（SD001 の champion）が学習済みのネットを読むのに要る。無ければ つよい は 1 つ下の段の相手にはならず、エラーを知らせて先頭の手を打つ
numpy
# テストだけに要るもの
pytest>=8
pytest-asyncio>=0.23
