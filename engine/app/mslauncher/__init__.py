"""MeichoSim の起動役（要件 R-UPD-1〜9・APP-010）。

配布物は「起動役」と「中身」の 2 層である。ここは**起動役の側**で、固めた実行ファイルに入る。
中身（エンジン・アプリ・画面・AI の登録簿・モデル・カードデータ）は実行ファイルの外の
`contents/versions/<版>/` に素のファイルとして置かれ、更新の対象になる。

守ること:

- **標準ライブラリだけで書く。** 起動役はめったに配り直さないので、外部ライブラリの版に縛られない。
- **`app`・`meicho`・`webapp` を import しない。** 固めるときに中身が実行ファイルへ焼き込まれると、
  更新しても古いコードが動き続ける。中身は子プロセスで、名前を文字列にして読み込む。
- 固めたときは最上位のパッケージ `mslauncher` になる（`app.mslauncher` ではない）。
  だからパッケージの中は相対 import だけを使う。
"""

LAUNCHER_API = 1          # 起動役が中身に約束する版。中身が求める版（launcher_api_min）より小さければ「新しい起動役が必要」（R-UPD-9）
PORT = 8765               # 手元起動のポートは固定（R-UPD-6。変えるとブラウザ内のデッキが見えなくなる）
