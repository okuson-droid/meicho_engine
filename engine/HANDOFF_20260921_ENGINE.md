# 引継ぎ書 — エンジンの持ち場・並行作業が始まった最初のチャットから（2026-09-20〜21）

**これは「作業させる 1 便」の引継ぎ書ではない。**このチャットの文脈を、次のエンジンの持ち場のクロエへ渡すためのものである。

準拠版: rules_draft **v0.18** ／ engine v0.1 ／ 符号化 **v5**（`(5, 1825, 317)`・このチャットでは動かしていない）。
champion は **`planner_vc4cps_kheb_b75`** のまま（交代していない）。Rust は**触っていない**。

**★最初に `LANES.md`（ルート）を読むこと。**2026-09-20 から 2 つのチャットが並行している（エンジン／オンライン対戦アプリ）。
このチャットは**エンジンの持ち場**である。1 つのファイルの書き手は 1 つの持ち場だけ（`LANES.md` §1）。

併読（役割の違う系統）:
- `engine/app/HANDOFF_<日付>_APP.md` — アプリの持ち場の引継ぎ書（**あれば**。このチャットでは存在を確かめていない。アプリの状態は `engine/app/TASKS_APP.md` が正本）
- `engine/HANDOFF_20260919_OFFICIAL_RULES_CLOSE.md` — ひとつ前のエンジン線の引継ぎ書（公式ルール線を閉じたところ）。**この引継ぎ書で置き換わった**
- `engine/HANDOFF_20260920_PARALLEL_REVIEW.md` — 並行作業の設計の依頼書。**済**（結果は `LANES.md` と `engine/PARALLEL_WORK_REVIEW_20260920.md`）

やることの正本は `TASKS.md`。用語は `CLAUDE.md`。**D-番号・APP-番号の「次に使える番号」はここに書かない**（`LANES.md` §8。`decisions.md` の末尾が正本）。

---

## 1. 現在地

- **PC の全検査が失敗 0 に戻った**（2026-09-21・D-116）: `985 passed, 26 skipped in 9789.22s (2:43:09)`。skip は全部 torch 無し（PC では正常）。
  **エンジンの持ち場で PC 待ちのものは無い。**
- このチャットで入れたもの（どれも**打ち方は変えていない**）:
  - D-109〜D-113: 並行作業の決まりへの切り替え（TE-1）／SD02 の強さの段階（TE-6・下記）／`LANES.md` §5 をバイト比較必須に
  - **D-114 M1 のトレース点**（TE-5）: `meicho/trace.py` 新設、`meicho/engine.py` にトレース点、`tests/test_trace_m1.py`（19 件）。**アプリの持ち場の記録器はこの口で組めた**（APP-007・TE-5 は先方が閉じた）
  - **D-115**: 唯一落ちていた `test_worlds_module_matches_diag_pimc` の基準を `results/lit/m5m1_pimc_v018.jsonl` に作り直した（元の記録は残した）
  - TE-2: アプリ用のシード帯 **722000..821999** を登録（`seed_bands.json`・`kind: "app"`）
- **SD02 の CPU の段階は決まった**（D-110〜D-112）: やさしい `heuristic`／ふつう `greedy`／つよい `planner_lh`。数字は `engine/TE6_SD02_TIERS_NOTES.md` §2・§6・§7
- 送り箱: エンジン → アプリ `engine/TO_APP.md` に **TA-1〜TA-7**。**アプリは TA-1〜TA-7 をすべて受けたと台帳に書いた**（`TASKS_APP.md` Done）ので、この引継ぎと同時にエンジンが**閉じた（済にした）**。
  アプリ → エンジン `engine/app/TO_ENGINE.md` は TE-1〜TE-10。**エンジンの手元に残っているのは知らせだけ**（TE-3・TE-4 は直すか未定、TE-7〜TE-10 は読んだ記録のみ）

## 2. 次のチャットの最初の一手

**`LANES.md` §0 の 4 つ（持ち場の確認・相手の送り箱を読む・`engine/WRITELOG_ENGINE.md` の控えと PC の実物を比べる・台帳を読む）をしてから、マスターに次の作業を聞くこと。**
エンジンの持ち場には、いま急ぎのものが無い。マスターはまだ次を選んでいない。

候補（`TASKS.md` より。クロエの推しの順）:
1. **TE-3／TE-4（現行 `webapp/` を直すか）の裁定をもらう**——どちらも小さい。TE-3 は `webapp/record.py` の `RULES_VERSION = "v0.10"` の直書き、TE-4 は記録の `ms` に人間の操作時間が混ざる件
2. **AI 線の材料 2 件を 1 便にまとめるか**（`TASKS.md` Someday）: 公開して手札に入ったカードを決定化が使っていない件（M1 仕様書 §10）と、TE-10 の「積集合で忘れると見えない出方が読めてしまう」件。**どちらも確かめていない**
3. 便 C の裁定 31 件／J-1〜J-6（重い・裁定が要る）
4. Kaggle 登録・配布版・対人局（マスターの手が要る）

## 3. このチャットで確定したこと（決定と理由）

1. **並行作業の決まりは `LANES.md`**（D-109・マスター裁定）。要点は「1 ファイル 1 書き手」「D-番号はエンジンだけが振る・アプリは APP-番号」「頼みごとは送り箱・閉じるのは送った側」「エンジン本体（`engine/meicho/`）は M1 を含めてエンジンの持ち場が実装する」。
   クロエの叩き台（札・番号の範囲予約・末尾追記）は採られず、**共有して書くファイルを無くす**形になった。理由: 共有をやめれば衝突が構造的に起きない（`PARALLEL_WORK_REVIEW_20260920.md` §1）
2. **SD02 の段階**（マスター裁定 2 回）: まず (c)「上に強い版を探す」を試して見つからず（D-111）、次に「アプリの推しを採用」で中段を測って `greedy` に決めた（D-112）。
   選び方（中段の資格＝対 H の下端 > 0.5 かつ 対 `planner_lh` の上端 < 0.5）は**回す前に決めた**
3. **M1 は仕様書から 5 点変えた**（D-114・TA-7）: `action` と `step` は**処理の前**に出す／同時手番の `action` はまとめて先に／`reveal_top_to_hand` の公開は引いた後／マリガンの引き直しは `draw` を出さない／§4.4 の任意 3 つは入れない。アプリの持ち場は「どれも都合が良かった」と返した（TE-9）
4. **`search_deck` は公開する**（公式 103.3）・**B-9 の手札公開は `audience: "all"`**（公式 904.1）。TA-6 で答え、M1 に実装した
5. **`LANES.md` §5 の書き戻しの確認は、バイト比較を必須にした**（D-113・マスター裁定）。サイズだけの比較は不可

## 4. 却下した案・採らなかった案・撤回した結論

- **git のブランチ／worktree で持ち場を分ける案 → 採らなかった**（クロエもレビューも）。チャットから git を叩けない（この環境の PC 側シェルが無い）ため。`LANES.md` §10 に「device_bash か Claude Code が使えるようになったら見直す」とある
- **クロエの並行作業の叩き台 7 項目 → 置き換えられた**（§3-1）。次のチャットが「札」や「D 番号の範囲予約」を再提案しないこと
- **SD02 の「上を足す」候補 3 体 → どれも採らない**（D-111）: ネット抜きの champion のつまみ（0.447）／決定化 12 本（ふるい 0.550 → 別帯 0.497）／mcts640（0.523・1 局約 9 秒）
- **★撤回した結論**: 決定化 12 本（C2_s12）を一度「勝ち残り」とした。**別帯の確かめで 0.497 ±0.040 になり、ふるいの 0.55 は 3 体から最良を選んだ上振れだった**。以後「ふるいの 1 位」を強いと書かないこと
- **M1 で champion の指紋（`check_champion_fingerprint.py`）を測る案 → 測らなかった**。あれは **Rust 版**で回すので Python のエンジンの変更を踏まない。測っても不変の証拠にならない（D-114）
- **`m5m1_pimc` を PC で作り直す案 → 不要になった**。検査が読むのは先頭 2 局だけで、作業環境で約 30 秒で作れた（D-115）

## 5. 言えないこと・未確認（★次のチャットはここを誤読しやすい）

- **「つよい（SD02）を素の `planner` に替えるか」は未裁定**。素の `planner` は `planner_lh` と区別できない（0.538 ±0.040・境界）。D-047 と同じ向きで、替えれば速くなる見込み。**アプリの持ち場の推しは「替えない」**（APP-005 追記 1）。裁定はマスター。言い切るなら別帯 n=2,400 の追試が要る
- 段階の強さは**AI どうしの勝率**であって、人間が感じる難しさを保証しない
- `test_worlds_module_matches_diag_pimc` は D-115 以後「転記の番人」ではなく「**W の数え方の回帰の番人**」。ルールを直して対局が変わったら `experiments/regen_m5m1_pimc.py` で基準を作り直す。59 行目が変わった原因（A-2／A-6／D-088 のどれか）は切り分けていない
- M1 のトレースで **AI を同じスレッドの with の中で呼ぶと探索の `apply` も流れ込む**。守るのはアプリの持ち場（M5）
- TE-10 の「積集合で忘れると見えない出方が読めてしまう」件が**現プールで実際に起きるかは誰も確かめていない**

## 6. 落とし穴（次に同じところで転ばないように）

1. **★OneDrive の書き戻しは、このチャットで何度も載らなかった**（`WRITELOG_ENGINE.md` で 5 回前後・`seed_bands.json` で 2 回）。
   **しかも「サイズは同じで中身が古い」形がある**（控えの行を同じ桁数の値に置き換えたとき）。**書き戻したら再ステージしてバイト比較（`cmp`）**。合わなければ新しい `mtimeMs` で 1 回だけ書き直す。2 回目で必ず載った
2. **★依頼書で出力をファイルに流す（`> x.txt 2>&1`）なら「画面には何も出ない」と先に書く**（D-116）。マスターが「コマンドを実行しても何も起こらない」と止まりかけた。`CLAUDE.md` の罠にも足した
3. **★champion の指紋は Rust 版で測る。**Python 側の変更の不変は `bench_agents.py` の指紋 3 種（H `6427a7b28f7a10fe`・G `59e116cbb2fd094d`・P `453bc27c27c11997`）と Python↔Rust の毎手一致で押さえる。**「基準がその変更を踏むか」を先に言葉にする**
4. **ふるい（複数候補から最良を選ぶ）の結果は、別帯で確かめるまで信じない**（§4 の撤回）
5. **帯は回す前に登録する。**TE-6 の最初の測定で、回し始めてから約 1 分後に登録した（手順の瑕疵として記録に残した）。以後は守った
6. **`python -m pytest` は `tests` と場所を指定する。**付けないと `app\tests` も集まる（TE-7）
7. TASKS.md・decisions.md・LANES.md・TO_APP.md は **CRLF**、CLAUDE.md・HANDOFF_CONVENTIONS.md・`tests/*.py`・`meicho/trace.py` は **LF**。`meicho/engine.py` は **CRLF**

## 7. 再開に必要な外部資源

- **PC のフォルダ**（`C:\Users\奥村優斗\OneDrive\ドキュメント\eclipse_workフォルダ\meicho_engine_v0.1`）への接続。次のチャットでは取り直しが要ることがある
- **このチャットでは PC 側のシェル（device_bash）が使えなかった**。ファイルの読み込み（stage）と書き戻し（commit）だけで作業した。git はチャットから叩けない。
  条件は `engine/HANDOFF_20260920_PARALLEL_REVIEW.md` §1.2（Windows の仮想マシンの要件）。**この PC がどれを満たしていないかは未確認**
- **揮発したもの（次のチャットには残っていない）**: 作業環境に置いた engine 一式・`experiments/`・`webapp/`・学習済みネット 3 本（`drl_sd001_vc4`・`drl_sd001_s1`・`pi_small64_e10`・約 15MB）・Linux 版の `meicho_rs` wheel（`maturin build` で約 40 秒で作れる）。
  **必要ならまた PC からステージし直す**（`experiments/*.py` は 80 本超あるので 2 回に分ける）
- **作業環境で回せなかったもの**: カード画像が要る検査・配布版の検査・torch が要る学習側。これらは PC の全検査で見る
