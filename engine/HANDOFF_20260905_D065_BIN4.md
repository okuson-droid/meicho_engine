# 引き継ぎ（2026-09-05・夕）— D-065 便 4 から

前のセッションで **便 3（学習側）が完了した**。この文書は便 4（輪の再始動・loop 2）を
別のセッションで始めるための引き継ぎである。
正本は `D065_IMPLEMENTATION_PLAN.md`（便 4 は §5）、経過は `D065_NOTES.md`、裁定は `decisions.md`。

**前の引継ぎ書 `HANDOFF_20260905_D065_BIN3.md` は役目を終えた**（内容はこの文書に畳んである）。

---

## 0. 最初に読むもの（この順）

1. `D065_IMPLEMENTATION_PLAN.md` §5（便 4 の指示）・§7（帯）・§9（落とし穴）・§10（再ビルドの依頼文）
2. `D065_NOTES.md` の末尾「便 3」（学習側に何が足されたか）
3. `VALUE_BOOTSTRAP_DESIGN.md` §5〜§7（1 反復の輪と停止条件。loop 2 は loop 1 と同じ形）
4. `REPORTING_RULES.md`（報告の作法）
5. 本文書

**便 1〜便 3 の細部を読み直す必要は無い。** 結論だけ下にまとめてある。

---

## 1. 現在地

### champion（2026-09-06 に交代済み）

`planner_vb3` → `planner_vb3cp` → **`planner_vb3cps`**。
定義は `experiments/champion.py` の `CHAMPIONS["SD001"]` が単一の真実源。

```
extra_turns=1, value_net=drl_sd001_vb3.json,
opp_policy_net=drl_sd001_s1.json, opp_policy_root_only=True,
choice_phases=True, solo_samples=4,                     ← a1
policy_net=pi_small64_e10.json, policy_scope="proxy"    ← a5（蒸留した小さい π）
```

- 2026-09-06 の交代（`vb3cp` → `vb3cps`）は**代打ち π のファイルだけ**を替えたもの。
  **「勝ち越したから」ではなく「同じ強さで 5.6 倍速いから」の交代**である
  （0.496 ±0.028・上端 0.524・帯 625000・n=1,200／錨 3 種とも 0 をまたぐ）。
  採用基準はマスター裁定「弱くなっていなければ採る」。D-034 の「下端 > 0.5」は当てはめていない
- **新 champion の fingerprint（seeds 471500-471509）= `f82625f68bb79a2d`**
- 2026-09-05 の交代（`vb3` → `vb3cp`）の根拠: 旧 champion に **0.632 ±0.027**（下端 0.604・帯 457500・n=1,200）
- **ラダーは 2026-09-06 に完走した**（core5 v8）。現 champion は **2 位・Elo 1479 [1454,1502]**。
  1 位は旧 champion `planner_vb3cp`（1483）で、**差 4 Elo・区間が重なる＝区別できない**。
  D-034 の第 4 条件は字面では未達。**「弱いから 1 位でない」ではない**ので取り違えないこと
- 定義は **4 か所**（`experiments/champion.py` / `experiments/gauntlets/core5.json` 版 8 /
  `webapp/agents.py` / **`experiments/vb.py` の輪 2 の `proxy_pi`**）。**変えるときは 4 つ同時。**
  前 3 つは `tests/test_drl.py::test_champion_definition_is_consistent_everywhere`、
  輪 2 との一致は `tests/test_value_bootstrap.py::test_loop2_proxy_pi_is_fixed_across_iterations` が見る
- **`vb.py` の loop 1 の定義は無傷**（触ったのは輪 2 だけ）

### 便 1 のつまみ（すべて既定で無効・Python と Rust の両方にある）

`policy_net`／`policy_scope`／`choice_phases`／`solo_samples`／`align_leaves`／`align_rollout`／`align_stop`／
`tau`／`opp_mix`／`nash_delta`／`reeval_samples`（Rust のみ）。

便 2 の測定で分かったこと（**再測定する必要は無い**）:

- `align_leaves`（葉の整列）は**有害**。運ぶ距離が長いほど弱い（運ばない 0.567 ＞ ターン終わり 0.545 ＞ 次の自分のターン 0.511）
- 対抗の決定化を 6→12 に増やす（a0）は効かない（0.489）
- 代打ち π は**範囲を絞ると利得が消える**（`policy_scope="proxy_lite"` は a1 単独と区別できない）
- `opp_mix`（A-8）・`nash_delta`（A-9）は**どちらも不採用**。つまみは既定 0.0 で残置（挙動不変・費用 0）

### 便 3 で足したもの（学習側・Python だけ・**既定は従来どおり**）

- `drl_train.py --vtarget fresh` — V の教師を「選んだ手を別の決定化で取り直した値」にする。
  無い決定（版 2 の記録・`reeval_samples=0`）は `max` に落ちるので過去の記録もそのまま回る
- `drl_train.py --calib-by phase_turn` — 探索値 → 勝率の換算表を phase ×「自分のターンか」の層ごとに取る。
  **決定 1,000 件未満の層は全体に落とす**
- `drl_train.py --select target_v` — 残す版を「検証の**教師**に対する損失」で選ぶ（`best_v` は z に対する損失）
- `experiments/diag_optimism.py` — 楽観の診断。**便 4 は毎反復これを回して報告に貼る**
- `drl_record.py` の manifest に `regenerate`（打ち直すコマンド）が入り、`format` が実測の版になった

### 便 4 §5.1 で足したもの（2026-09-05・Python だけ）

- `vb.py` の `LOOPS`（輪ごとの定義）と、4 つの CLI の `--loop`（既定 1）。
  **loop 1 の定義は 1 文字も変えていない**（`LOOP1_GOLDEN` の表で固定）
- 輪 2 の探索器は**便 2 で採った a1 ＋ a5**（＝新 champion と同じ）。
  **計画書 §5.1 の字面（`samples: 12` と `align_leaves: True`）には従っていない**——
  どちらも便 2 で却下されており、§5.1 のコメント自身が「便 2 で採った組み合わせに合わせる」と言っている
- 取り直しは `record_extra` に置き**記録のときだけ**付ける（評価に付けても結果は同じで時間だけ損）
- 輪のつなぎ目: `model_name(deck, 3, loop=2)` = loop 1 の V_3／`model_name(deck, 4, loop=2)` = `drl_sd001_vc4.json`

### 便 4 の下ごしらえで足したもの（2026-09-05・Rust）

- **取り直し専用の乱数**（案 C）。取り直しの有無・本数で対局が変わらなくなった。
  **マスターの PC の再ビルドは 2026-09-05 に完了**（本人の報告で全件通過）
- `meicho_rs.features()` — この wheel に入っている修正の札。**つまみを増やさない修正を入れたら 1 行足すこと**。
  検査の skip 判定はこの札で行う（引数の有無では見分けられない修正があるため）

### 検査

**563 通過・2 skip**（データが無い環境の 2 件のみ）。fingerprint 3 種は
`773a71c15c5bc16e` / `677f28cc3b6995ee` / `6e39c2aa4b35d876` で不変。

**torch が無い環境（マスターの PC）では 18 件が torch のため skip・落ちるもの 0** になる。
学習側の 18 件は `_needs_torch()`（`pytest.importorskip("torch")`）で skip する。
**skip は「通った」ではない**——学習側は torch を入れた作業環境で **0 skip** が完了条件である。
マスターに検査を頼むときは、**skip の件数と理由も一緒に聞くこと。**

所要は作業環境（2 コア）で **約 7 分**（champion が 5.6 倍速くなったので、便 4 より前の約 20 分から縮んだ）。
**`bash` ツールの 600 秒上限に収めるには次の 2 本に割って回すこと。**

```
pytest tests -q -p no:randomly --ignore=tests/test_discovery.py    # 約 3 分
pytest tests/test_discovery.py -q -p no:randomly                   # 約 2.5 分
```

**申し送り**: 便 2 で champion が遅くなったとき、発見ループの重い検査 3 本は
局数を 20 → 6〜10 に減らしてしのいでいた。**便 4 で速さが戻ったので 20 に戻してある。**

---

## 2. 便 4 に入る前に決めること（**判断が要る点**）

### 2.1 記録は **25 倍**遅い（実測・2026-09-05。計画書 §5.2 の「12 倍」は誤り）

**実測した**（`D065_NOTES.md` 便 4 §5.1・§3）。遅さの正体は**代打ち π（a5）ただ 1 つ**である。

| 構成 | 秒/局 | 倍率 |
|---|---|---|
| loop 1（従来） | 0.27 | 1.0x |
| ＋a1（選択フェイズ・solo 4 本） | 0.31 | **1.2x** |
| ＋a5（代打ち = π） | 6.77 | **25.5x** |
| 輪 2 の記録（a1+a5+取り直し 4） | 8.16 | 30.8x |

**輪 2 の 1 反復は 2 コアで約 28 時間**（記録 21・τ の下見 3・門番と錨 4）。4 コアで約 14 時間。
**輪 1 は 4 反復回している**ので、この費用は 1 回では終わらない。

**→ (a) 蒸留を先にやることでマスターの裁定が出て、2026-09-06 に完了した。**

小さい代打ち π（幹 64・π 頭 32）を蒸留し、**先生と一致 88.1%・5.6 倍速い**版ができた。
**門番 0.496 ±0.028（上端 0.524）で採用基準（弱くなっていないこと）を満たし、錨 3 種も 0 をまたぐ。**
これで輪 2 の 1 反復は **28 時間 → 約 6 時間**（2 コア）。
版は `results/models/pi_small64_e10.json`、測定は `results/vb/d065_pi64.json`、
経過は `D065_NOTES.md`「便 4 の速度の手当て」。

**残る裁定**: 蒸留版を **champion 自身にも入れるか**（§2.3）。

### 2.3 ~~蒸留版を champion にも入れるか~~ → **2026-09-06 に入れた**（`planner_vb3cps`）

マスター裁定で入れた。4 か所同時（`champion.py` / `gauntlets/core5.json` 版 8 / `webapp/agents.py` /
`vb.py` の輪 2）。**輪 2 の代打ちも固定の小さい π にした**（計画書 §5.1 の「V_{k-1} の π 頭」からの逸脱・
理由は `D065_NOTES.md`「便 4 の速度の手当て・続き」§2）。

### 2.4 ~~ラダーを測るか~~ → **2026-09-06 に完走した**（core5 v8）

**現 champion `planner_vb3cps` は 2 位（Elo 1479 [1454,1502]）。1 位は旧 champion
`planner_vb3cp`（1483 [1460,1507]）。差 4 Elo で区間が重なり、直接対決も 0.487 ±0.057。
「1 位と 2 位が区別できない」というのが結論であって、現 champion が劣るという結論ではない。**

したがって **D-034 の第 4 条件は字面では満たしていない**。報告するときは
「**弱いから 1 位でない**」と「**1 位と区別できないから 1 位と言えない**」を混同しないこと。
3 位 `planner_vb3`（1417）との差 62 Elo ははっきりしている。
詳細は `results/ladder.md` の末尾と `D065_NOTES.md`「ラダー」。

**ラダーの回し方が変わった**（この回から）:
- `--resume` / `--budget-sec` / `--block` で**中断・再開できる。組の途中でも止まれる**
- `--engine auto` で **Rust 版**で回る（`mcts160` が絡む組だけ Python 版）。重い組で 1.5〜4.0 倍速い。
  **使う前に `experiments/check_ladder_engines.py` で両実装が同じ数を出すことを確かめること**
- どの組をどちらで回したかは `results/ladder.json` の各組の `engine` に残る
- 所要 255 分（2 コア）

### 2.2 ~~`reeval_samples` の乱数汚染~~ → **2026-09-05 に実装完了（案 C）**

取り直しが本編の乱数を進めてしまい、同じシードでも取り直しの有無で対局が変わっていた問題は**直した**
（`D065_NOTES.md`「便 4 の下ごしらえ」・`decisions.md`）。取り直し専用の乱数を 1 本足し、
取り直しの間だけ 3 本の乱数器を差し替えて元へ戻す。**既定の挙動は不変・fingerprint 3 種一致・検査 543 通過。**

**残っているのはマスターの PC の Rust 再ビルド 1 回だけ**（`agents.rs` と `lib.rs` を変えたため）。
依頼文は計画書 §10 をそのまま使う（マスターの PC は Windows・`win_amd64.whl`）。
**再ビルドが済むまで、マスターの PC では `tests/test_d065.py` の T-22（6 件）が skip になる。skip は「通った」ではない。**

**便 4 の残りは Python だけ**（`vb.py` の `LOOPS`・各 CLI の `--loop`）なので、
**次に Rust の再ビルドを頼むことは当面ない見込みである。**

---

## 3. 便 4 の手順（計画書 §5 の要点）

1. `vb.py` に `LOOPS` を足す（loop 2 = 便 2 で採った探索器 ＋ `pi_proxy`）。**loop 1 の定義と既定の挙動は変えない**
2. `drl_record.py --vb k --loop 2` / `eval_vb.py --iter k --loop 2` / `pilot_tau_vb.py --loop 2` /
   `pick_lambda_vb.py --loop 2` に `--loop`（既定 1）を足す
3. 1 反復の輪: 記録 → τ の下見（**必ずやり直す**。`choice_phases` で探索した決定が増え尺度も変わる）→
   学習 → `eval_vb.py` ＋ **`diag_optimism.py`**
4. **記録を取ったら manifest をマスターの PC に書き戻す**（`.bin` は持ち帰らない）
5. 反復 4' で門番を越えたら D-034 の手順で champion 交代の判定へ

**反復 4' は 2026-09-07 に完走した**（D-066）。門番 0.606 ±0.028（下端 0.578・越えた）／
錨 3 種とも 0 をまたぐ／カナリアは 153/200 → 77/200 に**半減**（D-043 により採否には効かせない）。
モデルは `results/models/drl_sd001_vc4.json`、結果は `results/vb/loop2_iter4.json`。

**そのとき見つかった、まだ片づいていない 2 件**:
- **D-067（裁定待ち）**: V の教師が偏っている。較正を「自己対戦 ＋ 対 H ＋ 対貪欲」の混合で
  取っているが、探索値には相手が誰かが入っていないので 1 本の曲線で両方に当てるのは
  構造的に無理である。自己対戦で +0.052／リーグで **−0.263** ずれる。
  自己対戦だけで取り直すと `root−z` は −0.0001 になる（裏取り済み）。
  選択肢 A 較正に相手の層を足す／**B リーグの局面を V の損失から外す（推し）**／C 何もしない。
  費用は学習 10 分＋門番 45 分（記録の取り直しは不要）
- カナリア半減の読みが 2 通りある（より良い線を選んだ／Lv2 の線を見失った）。D-067 と
  つながる可能性がある（H 相手の局面の教師が −0.184 低かった）

**門番の組み方の注意**（計画書 §5.1）: 「反復 4' の版 vs 反復 3'（= V_3 を載せた**同じ探索器**）」でなければならない。
探索器が違うと、探索器の差を V の差と取り違える。`model_name(deck, 3, loop=2)` は
**`drl_sd001_vb3.json`**（loop 1 の V_3）を返すこと。

---

## 4. 帯の台帳（`experiments/seed_bands.json`）

**`next_free` = 630000。** 新しい測定は必ずここから登録してから回す（D-028）。
`probe_d065.py` の `_check_band` が登録外のシードを拒む。

計画書 §7 で登録済みの帯（loop 2 用）:

- 480000..529999 — loop 2 の**記録**（反復ごと 10,000 幅）。**評価に使わない**
- 530000..549999 — loop 2 の**評価**（反復ごと 4,000 幅・`eval_vb.py --seed0`）。
  **4'（530000..533999）は使用済み**（2026-09-07）。次の反復 5' は 534000
- 470000..479999 — 診断（強さは読まない）。便 3 の端から端までの確認で 471900..471999 を使った

**申し送り**: この台帳は複数セッションが触る。一度、古い版で上書きして別セッションの帯を消す事故が起きている。
**書く前に必ず読むこと。**

---

## 5. 作業環境（毎回まっさら）

- Linux・2 コア。`/root/.cargo/bin` に `cargo`、`pip install maturin` で maturin。
  ビルドは `cd engine/rust && maturin build --release --out dist`（約 45 秒）→
  `pip install --force-reinstall --no-deps dist/*manylinux*.whl`
- `torch` は `pip install torch`（**PyTorch の公式インデックスは塞がれている**。素の PyPI から入れること）
- **カード画像（`cards/*.png`）は持ち込まない。** ただし `tests/test_card_images.py` は
  **画像の中身を見て「別カードが同じ画像を指していないか」を確かめる**ので、
  **1 枚ずつ違う中身の**プレースホルダを作業環境にだけ置くこと（全部同じ中身だと落ちる）。
  さらに **SD01-018 と SD01-019 だけは同じ中身にする**（`KNOWN_DUPLICATES` の既知の 1 件）。
  **マスターの PC の画像は絶対に触らない**
- **コンテナは約 10 分の無操作で回収される。** `nohup` の裏プロセスは死ぬ。
  長い測定は前面で・短いブロックに割って・再開できる形で回すこと。
  **2026-09-07 にこれで門番 1 回ぶんを丸ごと失った。**「長いから裏に回す」は必ず失敗する。
  `eval_vb.py` にも `--chunk` / `--budget-sec` / `--resume` が入った（`ladder.py` と同じ形）
- `bash` ツールの上限は 600 秒。`timeout 580 …` を付けて途中経過をファイルに残す作りにする
- 持ち込むファイル: `engine/meicho`・`engine/experiments`（`datasets/` と `gauntlets/` も）・`engine/tests`・
  `engine/scripts`・`engine/webapp`・`engine/rust`・`engine/results`（`models/` は必須）・
  `engine/decklists`・`cards/cards_structured.{csv,json}`。**`device_stage_files` は 1 回 50 ファイルまで**

---

## 6. マスターとの作法

- **節目ごとに定期的に報告する**（マスターの明示的な要望。「不安なので」と言われている）
- 数字には**必ず基準・n・95% 信頼区間**を添える。区間が広いうちは「まだ読めない」と正直に言う
- **判断が要る点は勝手に決めず裁定を仰ぐ**。ただし選択肢と推しは自分で用意する
- 間違えたら**結論から先に、率直に**訂正する
- 成果物は `SendUserFile` → `device_commit_files` で**マスターの PC に書き戻す**。
  パスは `C:\Users\奥村優斗\OneDrive\ドキュメント\eclipse_workフォルダ\meicho_engine_v0.1\engine\...`
- **マスターの PC ではコマンドを実行できない**（`device_bash` が無い）。検査やビルドの結果は本人に聞く
- **1,000 局ごとに定期報告する**（2026-09-07 のマスターの明示的な要望）。
  段の切れ目でしか数字が出ない測定では、そのことを断ってから段ごとに報告する
