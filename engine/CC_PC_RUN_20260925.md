# PC の Claude Code による再ビルドと PC の組の検査（REBUILD_REQUEST_20260922b の (1)(3)(4)）

- 実施: 2026-09-26（PC 版 Claude Code・段 3 の最初の仕事）
- 場所: `C:\Users\奥村優斗\Documents\eclipse_workフォルダ\meicho_engine_v0.1\engine`
  - 依頼書に書かれている OneDrive 側（`OneDrive\ドキュメント\...`）のフォルダは、ルートや `engine\` 直下のファイルと `.git` の `HEAD`・`config`・`index` が欠けていて、git としても読めなかった。そのため、ファイルがそろっている `Documents` 側で実施した
- 基点の commit: `964353a`（main）
- (2) のネットの移行は済んでいるので飛ばした（`results\models` に `*.bak.json` が 36 個ある）
- 失敗があっても直さない方針で実施した（結果として失敗は無かった）

## まとめ

| 段 | 結果 |
|---|---|
| (1) 載っていることの確認 | 7 項目すべて通過 |
| (3) 再ビルド | 成功。**655.9 秒**（cargo の release ビルドは `10m 42s`） |
| (3) インストール | `meicho-rs 0.1.0` が入った（`pip show` で確認） |
| (3) 指紋 | 実測 **`f4b80b25c35cfa77` → 一致**（検査の所要 307 秒） |
| (4) PC の組の検査 | **335 passed, 1 skipped, 0 failed**、984 deselected（962 秒 = 16 分 02 秒） |

## (1) 確認

どれも 1 行以上出た。

| 検索語 | ファイル | 行数 |
|---|---|---|
| `ENCODING_VERSION = 6` | `meicho\encode.py` | 1 |
| `ENCODING_VERSION: i64 = 6` | `rust\src\encode.rs` | 1 |
| `fn push_belief` | `rust\src\encode.rs` | 1 |
| `enc5.bak.json` | `scripts\migrate_nets_v6.py` | 2 |
| `def test_belief_matches_full_enumeration` | `tests\test_encoding_v6.py` | 1 |
| `def pytest_addoption` | `tests\conftest.py` | 1 |
| `pc_tests` | `tests\test_sets.json` | 2 |

## (3) 再ビルドと指紋

- `maturin build --release --out dist` → `Built wheel for CPython 3.11 to dist\meicho_rs-0.1.0-cp311-cp311-win_amd64.whl`。**655.9 秒**（依頼書の見込みは 1〜3 分）。`target\` が無い状態からの全ビルドになったためと思われる
- 警告は 2 件（`linker_messages` と、`cargo fix` で直せる 1 件）。エラーは無し
- 確認の 1 行目の `python -c "import meicho_rs; print('encoding_v6' in meicho_rs.features(), meicho_rs.encoding_info())"` は **`RuntimeError: meicho_rs.load_cards() が未呼び出し`** で落ちた。
  `encoding_info()` を呼ぶ前に `load_cards()` が要るようになっている。依頼書の文面が古いだけと見られ、直していない。
  なお `encoding_v6` の札は、指紋の検査が表示する「wheel の札」の一覧に入っていた
- `python scripts\check_champion_fingerprint.py`:

      wheel の札       ['bp01_k5', 'bundle_p', 'd065_bin1', 'd065_reeval_isolated_rng', 'draw_buckets', 'encoding_v6', 'endgame_enum', 'known_hand', 'lethal_uniform', 'opp_from_seat', 'te13_switched_scope', 'world_weight']
      champion         planner_vc4cps_kheb_b75
      期待             f4b80b25c35cfa77
      実測             f4b80b25c35cfa77
      → 一致

## (4) PC の組の検査

    python -m pytest tests --pc -q -rs

最後の行:

    335 passed, 1 skipped, 984 deselected in 962.32s (0:16:02)

- **通過 335**: 見込みの 270〜290 より多い。数のずれは報告だけでよいとされているので、そのまま記す
- **失敗 0**: `FAILED` の行は無し
- **skip 1**:

      SKIPPED [1] tests\test_card_profile_proj.py:24: could not import 'torch': No module named 'torch'

  理由: PC の Python 3.11 に `torch` が入っていない
