> **取り下げ（2026-09-08 夜・マスターの指示「発売前だからといって計画を変更する必要はありません。当初の計画通り進めます」）**
> 本書は発売向けの締めとして書いたが、計画外なので取り下げる。**作業者はこの文書の手順を実行しないこと。**
> 次の便は計画どおり **便 D（後半）: `HANDOFF_20260908_LIT_D2.md`**（GSPRT の道具 D-4・ラダー解析 D-5・対人 4 局の記録と回帰局面）。
> §2「対人 4 局の一次読み」は事実の記録として有効で、便 D（後半）の §2 に引き継いだ。

# 引継ぎ書（2026-09-08 夜）— 便 R: 発売前の締め — 対人 4 局の読み取りと診断・発売版の凍結・文書の訂正

rules_draft.md v0.11 準拠 / engine v0.1 / champion = **`planner_vc4cps`**（D-073・2026-09-08 交代・fingerprint `9b5ad48d2de6a7e0`・ラダー core5 v9 で Elo 1522 [1500, 1546]）/ 発売 2026-09-12（土）
報告の作法は `REPORTING_RULES.md`（§2.6 5 点セット・§2.7 対人局の区間・§2.8 V 凍結）。用語は `GLOSSARY.md`。

**位置づけ**: 便 E-0（D-073）で champion が `planner_vc4cps` になり、マスターが 2026-09-08 夜に**新 champion と 3 局、候補 `planner_vc4cps_lu50` と 1 局**を打った（`results/human_games/2026-09.jsonl` の 4〜7 件目・すべて SD001）。**AI が初めてマスターに勝った**（新 champion 2 勝 1 敗・lu50 1 勝 0 敗）。発売まで 3 日で、探索器や V を変える便を入れる余地は無い。本便は**対局をほぼ回さず**、(1) 4 局を正しく読んで負けの 1 局を再生で診断し、(2) 発売に載せる版を文書として凍結し、(3) 文書の食い違いを直す。**champion もアプリの既定も変えない。Rust は触らない。再ビルド無し。**

**マスターの指示**: 引継ぎ書は**次に作業させる便だけ**。本便の結果を確認してから次（発売後の便）を書く。設計判断が要る点は推測で補完せず「判断が要る点」として報告する。**提案はすべて推しを採用**（包括方針・D-069）。**SD02 の対人局はまだ意味を持たない**（2026-09-08・マスターの指摘）: SD02 には学習した部品（葉の V・相手モデル π₀・代打ち π）が無く、champion は素の計画探索のままである（D-058 で SD02 専用の π は門番に届かず据え置き。D-064 裁定 3 で V の線は SD001 のみ・「効いたら同じ手順書を SD02 に」）。対人局で測る対象が無いので、SD02 の基準値は **SD02 への横展開（V の反復ブートストラップ）を回してから**取る。本便では文書をそのとおりに直す（§3.5）。

---

## 0. 結論（先に）

### 0.1 何をするか

1. **対人 4 局の報告**（`human_games_ci.py`・相手ごとに分ける・Wilson と Clopper-Pearson）。新しい記録文書 `HUMAN_GAMES_20260908_NOTES.md`（2026-09-03 の記録文書と同じ型）。
2. **AI が負けた g001（対 `planner_vc4cps`・7 ターン・残りライフ マスター 15／AI 0）を再生して診断する**。「9/3 型（詰みの烈火を持ってパス／青）」の再発かどうか、そうでなければ何型か。負けを決めた対抗があれば T-14 に足す。
3. **AI が勝った 3 局も再生して、詰みの機会を取りこぼしていないか**を数える（g002 T7 の 燃える烈火 0.606・g003 T10 の 燃える烈火 0.430 は詰みだったか／g004 T7 は `lethal_uniform` の等重み規則が発火して取った勝ちだが、旧規則でも取れたか）。
4. **発売版の凍結**: `RELEASE_20260912.md` を新設し、載せる版・モデルの sha256・fingerprint・ラダー・対人局の区間・既知の限界を 1 枚に。
5. **文書の訂正**（打ち方は変えない）: `RULES_VERSION` の 3 重定義（`provenance.py` v0.11／`ladder.py` v0.10／`webapp/record.py` v0.10）を 1 か所に・`provenance._champion_label` が `null` になる件（便 E-0 の所見 3）・`PRERELEASE_GOAL.md` と計画書の「SD001 3・SD02 3」を「SD001 のみ（SD02 は学習した AI が無いので横展開後）」に。
6. 次の便の候補を、本便の診断結果で**並べ替えて**報告する（書かない。予告のみ）。

### 0.2 作らないもの（本便では）

- `lethal_uniform` の新 champion 上での再測定（門番・錨）。**発売後**。本便で取るのは対局を回さない診断（3 の g004 T7 の再生）だけ。
- 便 E 本体（輪 2 の反復 5'）・便 F（相手の型）・便 B・便 C。
- 配布版（D-074・`HANDOFF_20260908_DIST.md`）の Windows 版の作成。**マスターの PC の作業**であり本便の範囲外。ただし §3.4 の凍結文書に「配布版は D-074 の道具で champion の定義から自動で組む」と書く。
- champion・アプリの既定・Rust・`vb.py`・`seed_bands.json`（対局を回さないので帯は取らない。再生に帯は要らない）。

### 0.3 完了の判定

1. `HUMAN_GAMES_20260908_NOTES.md` に 4 局の区間（相手ごと）・g001 の診断（型の名前と根拠）・詰み機会の数え上げ・g004 T7 の旧規則との比較が載っている。
2. g001 に「負けを決めた対抗」があれば `tests/test_d065.py` の T-14 と同じ型で回帰局面に足してあり、無ければ「対抗の 1 手では説明できない負け（型: …）」と書いてある。
3. `RELEASE_20260912.md` があり、そこに書かれた sha256・fingerprint・Elo が `results/vb/champion_challenge_vc4.json`・`results/ladder.md`・`tests/test_champion_vc4.py` と一致する。
4. `RULES_VERSION` が 1 か所（`meicho/` 配下）で `v0.11` になり、3 本がそれを読む。記録・ラダー・由来の検査が通る。
5. 検査が全部通る（通過・失敗・skip の件数と理由）。fingerprint 4 種（`773a71c15c5bc16e` / `677f28cc3b6995ee` / `6e39c2aa4b35d876` / champion `9b5ad48d2de6a7e0`）が不変。
6. `decisions.md` に D-075。

---

## 1. 守ること

- **打ち方は 1 ビットも変えない。** 本便は診断と文書。fingerprint 4 種が動いたら止める。
- **対人局は判定ではなく診断**（§2.7）。n=3 と n=1 から勝率は決まらない。区間を書き、「言えること／言えないこと」を分ける。**相手が違う記録を混ぜない**（`planner_vc4cps` 3 局と `planner_vc4cps_lu50` 1 局は別の行。9/3 以前の 4 局は版がさらに違う）。
- **再生は記録の `opponent.seed` で行う**が、アプリと同じ決定化になる保証は無い（`replay_clash_eval.py` の注記）。再生で出た値が記録の `ai_clash` の `totals` と食い違うなら、その差を書き、記録側の値を優先する。
- **T-14 に足す局面の基準**は 9/3 と同じ: engine の後知恵の詰み表で「AI に詰みがあった対抗で、AI が詰みを選ばなかった」もの。単に評価が低かった手・結果的に負けた手は足さない（答えを見てから問題を選ばない）。
- **ファイルは消さない。旧 champion の登録は消さない。** `RULES_VERSION` の 1 本化で古い記録（`rules_version: v0.10`）が読めなくならないこと（読み込みは値を検査しないはず。検査で固定）。
- 成果物は `SendUserFile` → `device_commit_files`。書き戻す前に `device_list_dir` で mtime を見比べる（配布版の便が同時進行した事故の教訓・`dist_build`）。

---

## 2. 対人 4 局の一次読み（記録 `2026-09.jsonl` から。クロエが確かめた分。**再生はしていない**）

記録の 4〜7 件目。`played_at` 2026-09-08 21:51〜22:22 JST。デッキはすべて SD001。

| # | game_id | 相手 | マスターの席 | 勝者 | ターン | 残りライフ [席0, 席1] |
|---|---|---|---|---|---|---|
| 4 | g001 | `planner_vc4cps` | 0（先手） | **マスター** | 7 | [15, 0] |
| 5 | g002 | `planner_vc4cps` | 1 | AI | 10 | [12, 0] |
| 6 | g003 | `planner_vc4cps` | 0 | AI | 11 | [0, 14] |
| 7 | g004 | `planner_vc4cps_lu50` | 1 | AI | 7 | [20, 0] |

**区間**（`human_games_ci.py` の `wilson` / `clopper_pearson` で計算）:

- 対 `planner_vc4cps`（現 champion）: **AI 2 勝 1 敗**。Wilson [0.208, 0.939]・Clopper-Pearson [0.094, 0.992]。**「AI がマスターに勝ちうる」ことの初めての証拠**であり、勝率については何も言えない。
- 対 `planner_vc4cps_lu50`: AI 1 勝 0 敗。Wilson [0.207, 1.000]・CP [0.025, 1.000]。1 局から言えることは無い。
- 9/3 以前の 4 局（`planner_pi` 1 局・`planner_vb3` 2 局・8 月の 1 局）: AI 0 勝。CP 上端 0.602。**版が違うので今回と足さない。**

**記録の `ai_clash` から読めること**（AI の対抗ごとの候補と点数。すべての対抗で AI は最大値の手を選んでいる＝argmax の整合は取れている）:

- **g001（AI の負け）**: AI の対抗時の点数が T1 0.492 → T2 0.483 → T3 0.449 → **T4 0.351** → T5 0.317 → T6 0.319 → T7 0.285 と**一方向に下がり続けた**。どの対抗にも 燃える烈火 や 1.0 の手は無い。マスターは T1 に SD01-006、T3 に SD01-005 をレベルアップして T3・T5 に交代、T7 に 3 枚の連撃で締めた。**9/3 型（詰みを持ってパス）ではなく、序盤のテンポで押し切られた負けに見える。** 最大の落ち込みは T3→T4（−0.098。マスターの T3 レベルアップ＋交代の直後）。AI のマリガンは 1 枚（記録 `mulligan cards [0]`）、マスターは 3 枚。
- **g002（AI の勝ち）**: T7 に AI は 燃える烈火（0.606）・燃える闘志 ×2（0.696）を持ちながら ババン・回避反撃（0.717）を選んだ。詰みだったかは再生で確かめる（π₀ の列で 1.0 になっていないので、少なくとも「予測した相手の手に対しては」詰みではない）。T10 に 旋風（0.823）→ 連撃で勝ち。
- **g003（AI の勝ち）**: T10 に 燃える烈火 0.430 を持ちながら 鉤縄 0.531。T11 に 燃える闘志 1.000・轟音 1.000 が並び、闘志で勝ち。**詰みが π₀ の列で見えているときは取っている。**
- **g004（lu50・AI の勝ち）**: T1〜T6 は `rule: pi0`（`lethal_frac` すべて 0）。**T7 に `lethal_frac` 0.553 で等重み規則が発火し、音の形・通常攻撃（等重みで 0.834）を選んで連撃で勝ち。** 対人でこの規則が発火したのはこれが初めて。**旧規則（π₀ の最尤 1 手）でも同じ手を選んだかは再生で確かめる**（同じなら lu50 の寄与は無かった）。

**クロエの仮読み（本便で確かめる）**: 新 champion は「詰みが見えれば取る」ことはできていて、負けたのは詰み見逃しではなく**序盤の組み立て（レベルアップの速さ・交代）**で差をつけられた局である。これが正しければ、次に効くのは対抗の規則ではなく**序盤の型**（便 F の「相手の型」か、便 E の V の反復）である。

---

## 3. 手順（この順で。対局なし。Python のみ）

### 3.1 区間と記録文書（30 分）

`python3 experiments/human_games_ci.py --json results/lit/r_human_games_ci.json` を回す。道具は既に**相手ごと・デッキごと**に分けて区間を出し（`by_opponent` / `by_deck`）、相手が混在していれば `mixed_opponents` を立てる。報告では **`by_opponent` の行を本文に使い、`overall`（全 8 局・版混在）は参考としてだけ**載せる。`HUMAN_GAMES_20260908_NOTES.md` を 9/3 の文書と同じ章立て（流れ／詰み表／探索の再生／所見／提案／帯と再現）で書き始める。

### 3.2 g001 の診断（2 時間）

1. `verify_lethal_human.py` は**最後の対抗**しか見ない。`--all-clashes` を足して**AI のすべての対抗**（T1〜T7）で詰み表を出す（各対抗の局面は `webapp.record` の再生で取る。`scan_missed_lethal.py` の `lethal_avail` / `lethal_taken` と同じ定義）。
2. `replay_clash_eval.py --opponent planner_vc4cps --detail` を**各対抗**で回せるようにし（同じく `--all-clashes`）、決定化ごとの π₀ の予測とマスターの実際の提出を並べる。**記録の `ai_clash.totals` と再生の値が合うか**を最初の対抗で確かめてから読む。
3. ターンごとの盤面（両者のライフ・協奏・場のキャラのレベル・手札枚数）を再生から表にし、**どこで差がついたか**を書く。候補: マスターの T1・T3 のレベルアップと T3・T5 の交代／AI のマリガン 1 枚／T4 の 鉤縄（AI 側の最大の落ち込みの直後）。
4. 判定: (a) 後知恵の詰み表で AI に詰みがあったのに取らなかった対抗があれば **T-14 に足す**（`tests/test_d065.py` の `_human_clash_positions` に g001（seed 130004）を加える。既存 2 局面の検査は動かさない）。(b) 無ければ「対抗の 1 手では説明できない負け」として、**型の名前**（例: 序盤のレベルアップ先行＋交代）と、その型を自己対戦で作るならどの語彙（発見ループの `rush_chara` / `fix_leader` / 便 F の型）で表せるかを書く。**(b) は便 F の材料である。**

### 3.3 勝った 3 局の詰み機会（1 時間）

同じ `--all-clashes` で g002・g003・g004 を走査し、**詰みがあった対抗の数と取った数**を出す（自己対戦の 0.857 と並べる。ただし n が小さいので区間は書かない）。加えて:

- g002 T7: 燃える烈火 は後知恵の詰み表で詰みだったか。詰みなら「π₀ の列では 0.606 に見えた」理由（マスターの実際の提出と π₀ の予測の差）を書く。
- g004 T7: 記録のとおり `rule: uniform`・`lethal_frac` [0, 0, 0.553]。**同じ局面を `lethal_uniform=0`（現 champion）で再生し、同じ 音の形・通常攻撃 を選ぶか**を見る。同じなら「lu50 の寄与は無かった局」、違えば「lu50 が勝ちを作った初めての対人局」。どちらでも 1 局なので**強さの根拠にはしない**。

### 3.4 発売版の凍結（1 時間）

`RELEASE_20260912.md` を新設。内容: (1) champion の定義（`champion.py` の kwargs をそのまま）と交代の根拠（D-073 の表）、(2) モデル 3 本の sha256（`champion_challenge_vc4.json` の由来ブロックから: `drl_sd001_vc4.json` `f050f22ee0a38993…`・`drl_sd001_s1.json` `84b9b3df71cfbbca…`・`pi_small64_e10.json` `88ea7e0f170e2059…`）、(3) fingerprint 4 種、(4) ラダー core5 v9 の順位表（`results/ladder.md` から）、(5) 対人局の区間（§3.1・相手ごと）、(6) アプリの既定と選べる相手の一覧（`webapp/agents.py`）、(7) 配布版は D-074 の `scripts/make_dist.py` が `champion.py` から自動で組む（Windows 版はマスターの PC・`HANDOFF_20260908_DIST.md`）、(8) **既知の限界**: 9/3 型の負けは V を替えても直っていない（便 E-0 E0-3）／搾取可能性は未測定（被搾取の基準値は V_3 上の 0.112）／SD02 のマスター基準値は無い（対人局は当面 SD001 のみ）／対人局は n=3 で勝率は決まらない、(9) 発売後の最初の便の候補（§6）。**数字はすべて出典ファイル名つき**で、検査 `tests/test_release_doc.py`（1 本）が sha256・fingerprint・Elo の文字列を出典と突き合わせる（文書が古くなったら落ちる）。

### 3.5 文書と定数の訂正（1 時間）

1. **`RULES_VERSION` を 1 か所に**: `meicho/` 配下（例: `meicho/version.py` に `RULES_VERSION = "v0.11"`・`ENGINE_VERSION = "v0.1"`）を真実源にし、`experiments/provenance.py`・`experiments/ladder.py`・`webapp/record.py` はそこから読む。**記録に書く値は v0.11 になる**（今までの記録は v0.10 のまま読める。読み込み側が値で分岐していないことを検査で固定）。`results/ladder.md` のヘッダは次にラダーを回したときに直る（v9 の記録は書き換えない）。
2. **`provenance._champion_label`**: `champion.spec()` が付ける `kind` を落としてから比べる（便 E-0 の所見 3）。`champion_challenge_vc4.json` は書き換えず、検査で「現 champion の spec に札が付く」ことを固定。
3. **`PRERELEASE_GOAL.md`** §2.1〜2.2 と §7: 「各デッキ 10 局」「SD001 3・SD02 3」を **SD001 のみ**に。SD02 は「学習した部品（V・π₀・代打ち π）が無く champion は素の計画探索（D-058・D-064 裁定 3）。対人局で測る対象がまだ無い。**SD02 の基準値は横展開（SD02 の V の反復ブートストラップ）を回してから**取る」と明記する。§1.3 の表に 2026-09-08 の 3 局を足す（版を明記）。**「両デッキで満たす」という目標の本文は触らない**（裁定事項。「SD02 は横展開後に判定する」を判断が要る点として出す）。
4. **`LITERATURE_PLAN_20260906.md`** §3.1 手順 8 と §4.2 の「SD001 3・SD02 3」を同様に。§10.3 として本便の位置づけを 3 行（クロエが書く §10.2 の続き。作業者は本文の該当箇所だけ直す）。
5. `webapp/agents.py` の表示名に Elo が入っているものは v9 の値に揃っているか確認（便 E-0 で更新済みのはず。ずれていれば直す）。

### 3.6 検査と報告（30 分）

検査を全部回す（通過・失敗・skip の件数と理由）。fingerprint 4 種の不変を確かめる。`decisions.md` に **D-075**（対人 4 局の区間・g001 の型・詰み機会の数・g004 T7 の旧規則比較・凍結文書・訂正の一覧・次の便の候補の並べ替え）。**「確定した数値」「境界で追試が要るもの」「設計に返す所見」を分けて書く。**

---

## 4. 検査（新設は少なくてよい）

| # | 検査 | 何を固定するか |
|---|---|---|
| T-R1 | `test_human_games_ci_by_opponent_on_20260908_records` | 2026-09-08 の 4 局が `by_opponent` で `planner_vc4cps`（2/3）と `planner_vc4cps_lu50`（1/1）に分かれ、`mixed_opponents` が真になる（既にある道具の固定） |
| T-R2 | `test_verify_lethal_all_clashes_matches_last_clash` | `--all-clashes` の最後の対抗の結果が従来の（最後の対抗だけの）結果と一致 |
| T-R3 | `test_rules_version_single_source` | `RULES_VERSION` の定義が 1 か所で、3 本がそれを読む。値は `rules_draft.md` の見出しの版と一致 |
| T-R4 | `test_old_records_with_v010_still_load` | `rules_version: v0.10` の記録が読めて再生できる |
| T-R5 | `test_champion_label_attaches` | `provenance.block(champion.spec(...))` の `label` が `null` でない |
| T-R6 | `test_release_doc_matches_sources` | `RELEASE_20260912.md` の sha256・fingerprint・Elo が出典と一致 |
| （条件つき）T-14 追加 | `test_..._human_positions` | §3.2 (a) のときだけ。g001（seed 130004）の対抗を既存 2 局面と同じ形で |

---

## 5. 転びやすいところ

1. **再生の値が記録と合わない** → 決定化の乱数が違う（`replay_clash_eval.py` の注記）。記録側を正とし、差を書く。合わせようとして乱数の消費順を変えない。
2. **「負けた局面を T-14 に足したい」誘惑** → 詰み表で詰みが無い対抗は足さない。足すのは「詰みがあったのに取らなかった」ものだけ。
3. **`RULES_VERSION` を変えて記録の検査が落ちる** → 期待値を直書きしている検査を探し、真実源から読む形に直す。値の直書きを増やさない。
4. **`PRERELEASE_GOAL.md` を書き換えすぎる** → 直すのは SD02 の対人局に関する行と §1.3 の表だけ。目標の本文（両デッキで満たす）は**裁定事項**なので触らず、「SD02 は自己対戦のみで判定する案」を判断が要る点として出す。
5. **配布版の便（D-074）と同じファイルを触る** → `webapp/agents.py`・`webapp/record.py` は配布版の便も触った。書き戻す前に mtime を見比べる。
6. **1 局の結果で結論を書く** → g004 の勝ちは lu50 の根拠にならない。「初めて発火した」までにとどめる。

---

## 6. 次の便（予告のみ・本便では作らない）

本便の §3.2 の型の名前で並べ替える。候補は (a) **便 F 前倒し**（相手の型: g001 型＝序盤のレベルアップ先行＋交代、9/3 型＝安い札で対抗して連撃、を自己対戦の相手として作り、混同行列と「型別の勝率」を出す。lu50 のような対人向けの直しを 600 対で測れるようにする）、(b) **`lethal_uniform` の新 champion 上での再測定**（Kaggle が整えば Kaggle・整わなければ作業環境で錨 3×600 と門番＋対照）、(c) **便 E 本体**（輪 2 の反復 5'・Kaggle）、(d) **SD02 への横展開**（D-064 裁定 3 の「効いたら同じ手順書を SD02 に」。SD001 の V の線が champion 交代と対人の初勝利まで届いたので条件は満たした。記録 1 反復 6 時間級なので Kaggle 前提。SD02 の対人局の基準値はこの後）。**マスターの Kaggle 導入（`COMPUTE_PLAN` §4.1〜4.3）の進み具合で (b)(c) の置き場が決まる。** 発売後の最初の便は、本便の結果と SD02 の事情を見てから 1 通だけ書く。
