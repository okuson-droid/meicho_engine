# 引継ぎ書（HANDOFF_*.md）の書き方 — 別チャットのクロエへ（2026-09-09）

このファイルは、引継ぎ書を書く役を別のチャットに移すために、これまでの運用を 1 枚にまとめたものである。
プロジェクト記憶（`MEMORY.md` 索引・`literature_plan.md`・`champion_vc4.md` ほか）と合わせて読む。

## 現在地（2026-09-09）

- champion は **`planner_vc4cps`**（葉 = V_4'・decisions.md D-073・fingerprint `9b5ad48d2de6a7e0`・ラダー core5 v9 で Elo 1522 [1500, 1546]）。SD02 は素の計画探索のまま（学習した AI が無い）。
- 完了: 文献計画の 便 D 前半（D-070）・便 A 前半（D-071・`lethal_uniform` は champion にしない）・便 E-0（D-073・champion 交代）。対人 4 局（2026-09-08 夜）は D-073 追記に一次読み。
- **作業待ち: 便 D（後半）`HANDOFF_20260908_LIT_D2.md`**（GSPRT の道具 D-4・ラダー解析 D-5・対人 4 局の記録と回帰局面）。作業者の結果報告は **D-075** の予定。
- 取り下げ: 便 R（`HANDOFF_20260908_RELEASE.md`・冒頭に札）。理由: マスター指示「発売前だからといって計画を変更しない」。
- その次（予告のみ）: 計画書 §2 の残りの診断 M4（カンニング版の天井・便 C の範囲を決める）・M5・M1・M6 D1 → 便 C の範囲・便 B の実装・便 E 本体（Kaggle・D-072）。SD02 への横展開は便 E の枠。
- 帯の `next_free` は 666000（便 D 後半が 666000..669999 を使う案。着手時に PC の `experiments/seed_bands.json` を取り直して確かめる）。

## 進め方（マスターの指示。順番を変えない）

1. **前の便の結果を確認する**: `engine/LIT_NOTES.md` の該当章と `decisions.md` の最新 D-番号を読み、**報告の数字を結果 JSON から組み立て直して照合する**（`results/vb/*.json`・`results/lit/*`・`results/ladder.md`）。食い違いは報告の訂正として書く。
2. **次に作業させる 1 便だけ**を書く。後の便を先取りしない（予告は最後の節に 3〜5 行）。
3. 判断が要る点には**選択肢と推し**を並べる。マスターの包括方針: **提案はすべて推しを採用**（D-069）。前の便が出した判断が要る点は、次の引継ぎ書の §0.3 で推しを採って閉じる（「異なる裁定なら読み替える」と書く）。
4. **計画は変えない**（2026-09-08）。順序は `LITERATURE_PLAN_20260906.md` §3・§2・§4.1。計画外に気づいたことは報告の「気づいた点」に残すだけで、やるかどうかはマスターの裁定。
5. **PC に書き戻す**: `SendUserFile` → `device_commit_files`。**書き戻す前に `device_list_dir` で mtime を見比べる**（別セッションの作業者が同じファイルを触っていることがある。弾かれたら force せず、再ステージして差分を当て直す）。あわせて `decisions.md` に短い項を**追記**（既存の項は消さない。D-番号は最新＋1）、計画書 §10 に追記、`MEMORY.md` 索引と `literature_plan.md` を更新。
6. マスターへの返事は簡潔に（モバイル・表を使わない）。数字は基準・n・95% 区間つき。

## 引継ぎ書の型（`HANDOFF_20260908_LIT_D2.md` と同じ）

冒頭: 準拠版（rules_draft v0.11・engine v0.1）／champion（名前・fingerprint・Elo）／発売日／報告の作法の参照／**位置づけ**（計画のどこか・前の便との関係）／**マスターの指示**（1 便ずつ・推し採用・計画不変・SD02 は学習した AI が無いので対人局は SD001 のみ）。
§0 結論（0.1 何をするか／0.2 作らないもの／0.3 前提の裁定／0.4 完了の判定）→ §1 守ること → §2 前の便の結果の確認（数字つき）→ §3 手順（順番・コマンド・帯・期待値・所要時間）→ §4 検査（本体より先に書く。「直したと言う前にわざと壊す」）→ §5 帯 → §6 報告（`LIT_NOTES.md` の章・`decisions.md` の D-番号・判断が要る点・1,000 局ごとの定期報告）→ §7 転びやすいところ（症状→意味→直し方）→ §8 次の便の予告（作らない）。

## 毎回書かせること

- 1 便で変える探索器の要素は 1 つ。既定の挙動は不変（fingerprint 3 種 `773a71c15c5bc16e` / `677f28cc3b6995ee` / `6e39c2aa4b35d876`＋champion の値）。中身が変わったら名前も変える。
- Python が真実源・Rust は写し（毎手一致は手だけでなく点数まで）。Linux wheel は作業環境で自前ビルド。**マスターの PC の再ビルドは打ち方に関わる Rust 変更のときだけ**で、依頼は 5 点セット（`REPORTING_RULES.md` §2.6。「前回と同様に」禁止）。
- 帯は `seed_bands.json` に登録してから回す（D-028）。学習帯で評価しない。対照は同シード・差は局ごとに対にする。門番も対照（null）を同じ帯で取って対にする。
- 前面・短い塊・再開可能（`--budget-sec 480`・`--resume`）。1 時間を超える測定は Kaggle（D-072。Kaggle が整っていなければ作業環境で回す条件を書く）。
- V を凍結し由来（sha256・host・workers）を開始時に残す（§2.8）。
- 覗き見禁止（D-026）。透視・カンニング版は測定専用・`peek_` 名・champion 候補にも学習相手にもしない。
- 対人局は判定ではなく診断（Wilson/CP・相手の版を混ぜない・SD001 のみ）。負けた対抗は後知恵の詰み表で詰みがあったものだけ T-14 に足す。
- `.bin` は持ち帰らない・manifest は持ち帰る。ファイルは消さない。旧 champion の登録は消さない。
- 検査は通過・失敗・skip の件数と理由（skip は「通った」ではない。マスターの PC は torch 無し・wheel が古いのが正常＝理由つき skip）。

## 関連文書

`LITERATURE_PLAN_20260906.md`（§10〜10.3 に経過）／`LIT_NOTES.md`（便 D・A・E-0 の報告）／`decisions.md`（D-069〜D-074・D-071 裁定・D-073 追記）／`COMPUTE_PLAN_20260908.md`（D-072）／`HANDOFF_20260908_DIST.md`（D-074・配布版はマスターの PC の作業）／`REPORTING_RULES.md`／`GLOSSARY.md`。

## 効率化の決まり（2026-09-10・マスター指示「次からやってね」）

便 C の引継ぎ書を書くのに、照合（前の便の数字を JSON から組み立て直す）が全体の約 3 割、成果物の発見と記録の更新が約 2 割かかった。次からは次の 4 つで縮める。

1. **照合は 1 コマンド。** 作業者は `experiments/verify_report.py <便>` を育て、報告の数字を結果 JSON から再計算して「数字 → ファイル・鍵 → 再計算値 → 一致/不一致」を印字する。LIT_NOTES の章の末尾にその出力を貼る。引継ぎ役はこの出力に無い数字を疑い、食い違いだけを読む。**深掘り（jsonl まで降りる）は「言えないこと」に関わる 1〜2 点に絞る。**
2. **成果物の発見はマニフェストで。** 作業者は書き戻したファイルの一覧を `results/manifest_<便>.json`（パス・バイト数・sha256・日時）に残す。引継ぎ役はフォルダの再帰リストを取らない（9 万文字を超えて失敗する）。
3. **状態は `TASKS.md` にだけ書く。** Cowork の索引・各トピック・CLAUDE.md には状態を書かない（便が進むたびに 4 か所直すのをやめた）。引継ぎ役が便ごとに更新するのは: 引継ぎ書（新規）・`decisions.md`（追記）・計画書 §10（追記）・`TASKS.md`・便の結果メモ 1 本（`lit_bin_<便>.md`。**書く前に読む**）。索引を触るのは数字の訂正があるときだけ。
4. **引継ぎ書は差分だけ書く。** 共通部分（§1 守ること・§6 報告の型・§7 の共通項・帯の登録手順・5 点セット）は本ファイルの「毎回書かせること」を参照にし、引継ぎ書には便固有の §0（結論・裁定）・§2（前の便の確認）・§3（手順）・§4（検査）・§5（帯）を書く。

削らないもの: **設計の材料をコードまで降りて確かめる部分**（便 M の W_0 の退化は確かめずに書いたために起きた。便 C で Rust に `peeked_opp_hand` が無いのを見つけられたのは確かめたから）。
