# 依頼 — D-106 の直しを確かめる（クロエ → マスター）

**再ビルドは要らない。Python の検査を 1 本直しただけである。**

## なぜ

2026-09-19 の全検査（**2 失敗 / 964 通過 / 26 skip・3 時間 6 分 49 秒**）で落ちた 2 件のうち、
**1 件は A-6 の取りこぼし**だった。

    tests/test_bp01_k2.py::test_on_heal_is_capped_by_the_per_turn_count
    AssertionError: 0 回復で数えている / assert 1 == 0

この検査の後半は「**満タンなら 0 回復になる**」ことを使って「0 回復では数えない」を
確かめていた。A-6（回復にライフの上限は無い・D-104）で満タンからでも本物の +1 になったので、
**検査の前提のほうが死んでいた**。0 回復の確認を**明示的に 0 を渡す**形に書き替え、
あわせて「満タンからの回復は本物である」ことも同じ検査で固定した（D-106）。

`meicho/engine.py` が `MAX_LIFE` を import したまま使っていなかったので外した。
**Rust には触っていない。**

## 1. どこで

    C:\Users\奥村優斗\OneDrive\ドキュメント\eclipse_workフォルダ\meicho_engine_v0.1\engine

## 2. 何を打つか

**先に載っていることを確かめる**（検索語は ASCII だけ・D-101）。

    cd C:\Users\奥村優斗\OneDrive\ドキュメント\eclipse_workフォルダ\meicho_engine_v0.1\engine
    set PYTHONPATH=%CD%
    findstr /c:"E._heal(t, 0, 0)" tests\test_bp01_k2.py
    findstr /c:"HAND_LIMIT, OPENING_HAND, RUSH_UNLIMITED," meicho\engine.py

2 つとも出ること。出なければここで止めてほしい。

    python -m pytest tests\test_bp01_k2.py tests\test_official_a6.py tests\test_engine.py -q

期待: **失敗 0**（作業環境では 123 通過）。

## 3. 全検査は急がなくてよい

直したのは検査 1 本と死んだ import 1 行だけで、**エンジンの挙動は 1 手も変わっていない**。
次にまとまった直しを入れるときに一緒に回せばよい。
そのときの期待は **落ちるのは `tests\test_lit_c.py::test_worlds_module_matches_diag_pimc` の 1 件だけ**
（記録が旧エンジン製・作り直し待ち）、通過は 964 → **966**（新しい assert が 2 本増える）。

## 4. 決めてほしいこと（PC は要らない・2 件）

> **2026-09-19 追記: 2 件とも裁定が下りた（D-107）。**
> **(a) `MAX_LIFE` を消す — 推しを採用。**「消す」と「次に Rust を触る便に相乗りさせる」の両方。
> いまは着手せず、`TASKS.md` に裁定済み・実施待ちとして残した。
> **(b) champion の強さを測る — 測らない。**項目は閉じた。
> **「弱くなっていない」と確かめたのではなく、確かめないと決めた**のであって、
> D-097 §5（次の交代判定は新エンジンで測り直す）はそのまま生きている。
> **以下は裁定前の文面である。**


### (a) `MAX_LIFE` という名前を消すか

`meicho/state.py` と `rust/src/state.rs` に `STARTING_LIFE = 20` と `MAX_LIFE = 20` が並んでいる。
中身は同じで、`MAX_LIFE` を使っているのは**検査 2 本だけ**である。
**上限が無いのに `MAX` という名前が残っているのは、将来もう一度同じ誤解を呼ぶ。**

**クロエの推しは「消す」**（`STARTING_LIFE` に寄せる）。ただし `rust/src/state.rs` に触るので
**再ビルドが要る**ので、**次に Rust を触る便に相乗りさせたい**というのがあわせての推しである。

### (b) A-6 で champion が弱くなっていないかを測るか（前の便からの持ち越し）

`SD01-023`「奏鳴」はコスト 3 で +5 回復。上限があったころは序盤ほぼ死に効果だったものが、
いま本物の +5 になった。**champion のネットは上限があった世界で学習している**ので、
**奏鳴の価値を過小評価している可能性がある**。**クロエの推しは「測る」**。
