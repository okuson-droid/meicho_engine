# 引継ぎ書 — 段階1B を PC で閉じたところから（2026-09-16）

**これは「作業させる 1 便」の引継ぎ書ではない。**このチャットの文脈を次のチャットのクロエへ渡すためのものである。

準拠版: rules_draft **v0.12** ／ engine v0.1 ／ 符号化 **v5**（`ENCODING_VERSION = 5`）。
併読: `HANDOFF_CONVENTIONS.md`（毎回書かせること・効率化の決まり）／`REPORTING_RULES.md`／
`TASKS.md`（**やることの正本。ここには状態を書かない**）／`CLAUDE.md` と `memory/`（用語の索引）。

**★併走している別系統の引継ぎ書がある**: `HANDOFF_20260916_PTCG_WRITEUPS.md`（同日・別チャット・文献読解の便 D-090）。
**落とさないこと。**役割が違うので、日付が同じでも「新しい方だけ読む」をしてはいけない。
作業者（Codex）向けの `WORKER_HANDOFF_*` 系統は、連携を中断したので現在は動いていない。

---

## 1. 現在地

- **段階1B は閉じた**（D-089 ＋ D-091 ＋ 追記 1）。符号化 **v5**（`OBS_DIM` 1,825 / `ACT_CODE_LEN` 13 / `ACT_DIM` 317）。
  PC の wheel は **655,617 バイト**に作り直され、`meicho_rs.encoding_info()` が **`(5, 1825, 317)`** を返す。
  （**2026-09-17 訂正**。当初 655,672 と書いたのは写し間違いで、現物 `engine/rust/dist/meicho_rs-0.1.0-cp311-cp311-win_amd64.whl` は
  **655,617 バイト・2026-09-16 22:44 JST**。ビルド時刻は `encode.rs` の直しより後なので、wheel 自体は直った版である。
  `decisions.md` D-091 本文にサイズの記載は無い＝この数字の出所は本書だけだった。D-091 追記 2 に訂正を残した。）
  学習済みネット **12 本**を移行済み（控えは `results/models/*.json.enc4.bak.json`。**消さない**）。
- **champion は `planner_vc4cps_kheb_b75` のまま。交代していない。**
  ただし**指紋を貼り替えた**: `e1662edb32b144a9` → **`9d4ff39d024e4394`**（D-091 §3）。
- 最後に回した検査: `tests/test_drl.py` **18 通過・3 skip・失敗 0**、`scripts/check_champion_fingerprint.py` **一致**。
- **★全検査は回していない**（マスター裁定で今回だけ見送り・D-091 追記 1）。
  したがって「段階1B の後に全検査で失敗 0」とは**言えない**。
- 次に使える D-番号は **D-092**。帯の `next_free` は **715000**（段階1B で対局・学習・帯は使っていない）。
- **正本は PC のフォルダ**（2026-09-15 裁定）。GitHub は貼るための置き場であって正本ではない（`GITHUB_SETUP.md` §7）。

## 2. 次のチャットの最初の一手

**`TASKS.md` の Active の先頭「公式ルールと実装の再検証・結果の反映」から。**

- **担当はクロエ**（2026-09-15 裁定。Codex との連携を中断したので引き取った）。
- やり方は「`engine/official_rules/` の 3 文書と `engine/meicho/` の**静的照合**をクロエが行い、**検査の実行はマスターに 5 点セットで依頼する**」。
- 初回監査（Codex 作・`official_rule_audit_20260915.md`）の 6 件を**確定一覧として扱わない**。対象コミット・条文・再現結果・Python/Rust の検証範囲と未検証箇所を揃え直す。
- **マスターの確認済み裁定 2 件**: 回復に上限は無い／【レベルアップ】の誘発対象は総合ルールを正とし、食い違う FAQ は採用しない。

## 3. このチャットで確定したこと（決定と理由）

1. **Codex（ChatGPT）との連携を中断した**（マスター裁定 2026-09-15）。あわせて**正本を PC のフォルダとする**（選択肢 (b)）。
   理由: 書き手が 2 人になったのに正本が 1 つに決まっておらず、`TASKS.md` が `git stash pop` で衝突した。
   `.github/workflows/stage1b-rust.yml` は**残した**（`workflow_dispatch` で手で回せる。ただし Ubuntu なので**「Windows で検証した」ことにはならない**）。
2. **Rust のビルドを止めていた 2 件を直した**（D-091 §1）。`Zone` の import 漏れ（`encode.rs`・`lib.rs`）と、
   `state.rs::to_json` の非網羅 match（`Resume::AfterClashCosts` / `StartRush`）。
   **Python は 1 行も触っていない。**Rust が Python に合わせた形である。
3. **符号化の食い違いを直した**（D-091 §2）。**index 95 ＝「いま出ている選択肢の数」**で、Rust が選択の種類ごとに手書きしていて 4 か所ずれていた。
   Python は種類を問わず `len(pending_choice["options"])` を取るのが正。
4. **digest と champion 指紋の基準を貼り替えた**（マスター裁定・D-091 §3）。
   段階1A が決定列に新しい選択行動を足したので、**打ち方を変えなくても digest と指紋は動く**。
   `BASELINE_DIGESTS_230000`（`meicho/drl_data.py`）と `check_champion_fingerprint.py` を D-088 以後の値へ。
5. **全検査を回さずに段階1B を閉じた**（マスター裁定・D-091 追記 1）。**この見送りを前例にしない。**

## 4. 却下した案・採らなかった案

- **貼り替えの前に「勝敗そのものを突き合わせる」測定をする案** → 採らなかった（マスターが「後者」を選択）。
  クロエの推しは測定する側だった。**次のチャットがこれを再提案しないこと。**やるとしたらマスターから言い出したときだけ。
- **`zone_card` の `options.len() + optional` を直す案** → 直さない。Python が `optional` のとき `options` に
  `{"type": "stop"}` を 1 つ足してから渡すので、**現状の Rust が正しい写し**である。
- **rustc が提案する `todo!()` で非網羅 match を埋める案** → 採らない。実行時に panic するだけである。
- **`migrate_nets_stage1b.py` に `sys.path` の前置きを足す案** → 提案したが未実施（マスターの返答なし）。
  当面は `set PYTHONPATH=%CD%` で回す。

## 5. 言えないこと（★次のチャットはここを誤読しやすい）

- **「champion の打ち方は変わっていない」は推論であって実測ではない。**
  根拠は (a) Python↔Rust の毎手一致の検査が通っていること、(b) 段階1A が決定列の変化を申告していること、の 2 点だけ。
  **勝敗そのものを突き合わせていない。**過去の勝率をそのまま読み続けてよいかはこの推論に乗っている。
  疑いが出たら、champion と固定の相手を同じ帯で回し、**指紋ではなく 1 局ごとの勝敗**を比べること。
- **歴代 champion 3 体の指紋は未再測。**`planner_vc4cps_kheb` / `planner_vc4cps` / `planner_vb3cps` は
  同じ理由で古くなっているはずだが測っていない。`check_champion_fingerprint.py` に
  **「D-088 以前の値・未再測」の札**をコメントで付けてある。**不一致が出ても本物か貼り忘れか区別できない。**
- **全検査の件数は受け取っていない。**D-089 の作業環境の 391 通過・48 失敗が PC で消えたかは未確認。

## 6. 落とし穴（次に同じところで転ばないように）

1. **★書き戻したファイルが PC に載っていないことがある（OneDrive）。**
   症状: 直したはずなのに検査の失敗の値が **1 ビットも変わらない**（`0.0019919338523333163` まで同一だった）。
   実際に起きたこと: `encode.rs` の更新日時だけ進んで**中身は古いまま**。サイズが 14,134 のままで、直っていれば 14,248 だった。
   直し方: **書き戻したら、次の作業を頼む前にサイズか中身で載ったことを確かめる。**
   D-084 でも版ずれで 2 回誤報している。同じ罠の裏返しである。
   マスターにビルドを頼む前に `findstr /c:"<直した行の一部>" <ファイル>` を打ってもらうのが安い。
2. **`python scripts\xxx.py` の形で起動すると `meicho` が見つからない。**
   `sys.path` に載るのはスクリプトのあるフォルダ（`engine\scripts`）で、`engine` ではない。
   `smoke_bp01.py` と `check_champion_fingerprint.py` は自前の前置きを持つが、
   **`migrate_nets_stage1b.py` は持っていない**。`experiments\bench_agents.py` も自分のフォルダしか足さない。
   直し方: `engine` で **`set PYTHONPATH=%CD%`** を先に 1 回。コンソールを開き直したら打ち直す。
3. **ネットの移行をワイルドカードで打たない。**`results\models\` には `*.meta.json` と `*.enc3.bak.json` が同居していて、
   どちらもネットではないので assert で落ちる。**12 本を名指しする**（依頼文 `STAGE1B_REBUILD_REQUEST.md` に貼れる形がある）。
4. **`meicho_rs.encoding_info()` は `load_cards()` を先に呼ばないと `RuntimeError`。**
   クロエが確認手順に素で書いて 1 往復無駄にした。検査を回すほうが確実である。
5. **エラーが 10 件出ても原因は 2 つ、ということがある。**
   今回の E0433 × 9 はすべて `Zone` の import 漏れ 1 つが場所ごとに出ていただけだった。
   **件数で怯まず、原因ごとにまとめてから報告する。**

## 7. 再開に必要な外部資源

- **PC のフォルダ**（`C:\Users\奥村優斗\OneDrive\ドキュメント\eclipse_workフォルダ\meicho_engine_v0.1`）へのアクセス許可。
  次のチャットでは**取り直しが要る**。
- **揮発したもの**: `build_stage1b.txt` / `check_stage1b.txt` / `digests_230000.txt` は PC のルートに残っているが、
  **作業用の一時ファイル**である。要らなくなったら消してよい。消す前に D-091 に必要な数字は写してある。
- Rust のビルドにはマスターの PC が要る（作業環境には `cargo` / `rustc` が無い・D-089）。
