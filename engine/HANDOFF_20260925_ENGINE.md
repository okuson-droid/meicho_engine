# 引継ぎ書 — エンジンの持ち場・段階2 のあと TE-13 を直したチャットから（2026-09-25 改訂）

**これは「作業させる 1 便」の引継ぎ書ではない。**このチャットの文脈を、次のエンジンの持ち場のクロエへ渡すためのものである。
**同じ名前の旧版（2026-09-25 朝・段階2 のチャットが書いたもの）をこの版で上書きした。**旧版の内容のうち、まだ有効な項目はここに移した。`engine/HANDOFF_20260924_ENGINE.md` はさらに前の版で、読まなくてよい。

準拠版: rules_draft **v0.19**（このチャットで v0.18 から上げた）／engine v0.1／符号化 **v6**（`(6, 1923, 317)`・変えていない）／記録の形式 MCDR v3。
champion は **`planner_vc4cps_kheb_b75`** のまま（交代していない・指紋 `f4b80b25c35cfa77`）。**TE-13 の直しで SD001/SD02 の対局は 1 手も変わっておらず、指紋もそのままである。**

**★最初に `LANES.md`（ルート）§0 の 4 つを読んで、そのとおりにすること。**このチャットは**エンジンの持ち場**である。

併読:
- `engine/app/HANDOFF_<日付>_APP.md` — アプリの持ち場の引継ぎ書。このチャットでは `engine/app/` を読んだだけで、書いていない
- `engine/HANDOFF_CONVENTIONS.md` — 引継ぎ書の書き方

やることの正本は `TASKS.md`。用語と罠は `CLAUDE.md`。決定は `engine/decisions.md`（**D-134 がこのチャットの分**。D-129〜D-133 は前のチャットの分）。
「次に使える D-番号」はここに書かない（`decisions.md` の末尾が正本）。

---

## 1. 現在地

- 計画書 `GENERALIST_AI_REVIEW_D086.md` §6 の**段階2 は終わっている**。段階3（汎用 V の反復）の土台の V は、前のチャットが作った（D-133・V_id の乱数 0・1・2 番の 3 本のアンサンブル `results/models/s2v_id_ens3.json`）。**次は段階3 の設計書を書く**（マスター指示・2026-09-25）
- **このチャットでしたこと: TE-13 を直した（D-134・rules v0.19）。**【切り替え】のスキルは、入れ替わった 2 枠（リーダーと、入れ替えたバック）のキャラだけが誘発するようにした。根拠は公式 603.1.2.1.1／906.2／913.9.1 の条文。Python `meicho/engine.py::_queue_switch_triggers` と Rust `rust/src/engine.rs::queue_switch_triggers` の両方、3 か所（行動の切り替え・効果 `switch_leader`・名指しの `switch_leader_to`）。検査 `tests/test_te13_switched_scope.py` 41 件
- **マスターの PC の Rust の再ビルドは後回しにした**（2026-09-25 マスター裁定）。依頼書 `engine/REBUILD_REQUEST_20260925.md` は書いて PC に置いてあり、`TASKS.md` の Waiting On に載っている。**段階3 の設計を書くのに再ビルドは要らない**（作業環境は自前でビルドする）
- アプリの持ち場には `engine/TO_APP.md` TA-11 で知らせた。そこで、アプリの台帳に TA-9・TA-10 を受けた記録が無いことも伝えた

**D-134 の数字（出典 `decisions.md` D-134 §4〜§6）**:
- SD001/SD02 は不変: 全検査の digest と指紋の検査が通過。`scripts/check_champion_fingerprint.py` で `f4b80b25c35cfa77` 一致（作業環境・Linux 版 Rust）
- 環境デッキの同型戦（random・シード 0..49・Python）: ショアキーパーを含む 8 種で 400 局中 349 局の展開が変わり、147 局の勝敗が変わった。含まない 16 種は 800 局中 0 局
- 全検査 `--run-slow`（39 分 51 秒・torch あり）: **1,315 通過・5 失敗・4 skip**。失敗は既知の 5 件（カード画像 4 件・`test_d065` の蒸留 1 件）。skip が前回より 2 件多いのは資材を持ち込まなかったため（`test_gendata` のデータセット・`test_lit_c` の `m5m1_pimc` の基準）

## 2. 次のチャットの最初の一手

`LANES.md` §0 の 4 つ（持ち場の確認／`engine/app/TO_ENGINE.md` を読む／`WRITELOG_ENGINE.md` と PC の実物をバイト比較／台帳を読む）をしてから、**計画書 §6 段階3（汎用 V の反復）の設計書を書いて、マスターの裁定をもらう**。

1. 先に読むもの（すべて PC から取る。作業環境の GitHub の写しは 09-16 で古い）:
   `engine/GENERALIST_AI_REVIEW_D086.md` §6（段階3 の節は「### 段階3：汎用Vの反復」）・§5.4・§8.4／`engine/PTCG_KAGGLE_NOTES_20260916.md` §PT-6・§PT-5／`decisions.md` の D-119（J-5: 段階3 は PT-6 を先に、PT-5 を後に）・D-131〜D-134／`engine/COMPUTE_PLAN_20260908.md`（Kaggle・D-072）
2. 設計書に書くこと（前のチャットが挙げた項目＋このチャットで増えた 1 つ）:
   - 反復 1 の教師（`record_mix.NETFREE`＋葉 `s2v_id_ens3.json`）・相手の配分（D-131 と同じか）・帯・局数（約 10,000 局）
   - V の更新の仕方（乱数 3 本を学び直してまた束ねるか）・反復を選ぶ規則（調整デッキで前の反復と対にして・乱数 3 本）
   - 費用（D-133 §4: 記録だけで作業環境 少なくとも約 8 時間。Kaggle を使うか）
   - PT-6（V の分布ヘッド）の扱い。**材料**: D-131 の教材は引き分け 0 局なので、勝・引・負の 3 値分布は実質 2 値になる。入れるなら HL-Gauss 型で「何の量のビンにするか」を決める必要がある（前のチャットの読み・裁定は無い）
   - **★新しく増えた項目: D-131 の教材を段階3 で使い回すか。**TE-13 の前のエンジンで記録したので、ショアキーパー系（SK 系）の学習デッキ 4 つ（`SANGE_SK_ANKO`・`SANGE_SK_TSUBAKI`・`YANG_SK_ANKO`・`YANG_SK_TSUBAKI`）が絡む局は旧挙動である。選択肢は「SK 系の局を除く」「取り直す」「そのまま使う」。D-132/D-133 の V もこの教材で学んだ。**段階3 の新しい記録は必ず直したエンジンで回す**
   - **最終評価の 4 デッキは反復を選んだあとに一度だけ開く**（4 つとも SK 系＝TE-13 で最も挙動が変わった群）
3. 設計の前に安く確かめられること（**約 2 時間・相談してから**・前のチャットからの持ち越し）: アンサンブルが V 1 本より強いか。D-132 追記 4 と同じ課題・同じシード（841000..841149・各ブロック 150 局＝2,400 局）で `eval_s2_repr.py` に `--arm v_ens3=results/models/s2v_id_ens3.json` を足して回し、乱数 0・1・2 番の V_id と局ごとに対にする。**調整デッキは SK 系を含まないので、TE-13 の直しの前後で結果は変わらない**（比べてよい）

**判断 1 つは推しで閉じた扱い**（D-132 追記 5）: アンサンブルは「ロジットの平均」（勝率の平均ではない）。報告では一度触れること。

そのほかの候補（`TASKS.md` より）: PC の再ビルド（後回し中）／`test_d065` の壊れた検査を直す（小さい）／監査の採用 2 件（D-126）／Kaggle 登録・配布版・対人局（マスターの手が要る）。

## 3. このチャットで確定したこと（決定と理由）

1. **TE-13 は段階3 の前に直す**（2026-09-25 マスター裁定）。直さずに回すと、段階3 の教材にこの誤りが焼き込まれるため
2. **【切り替え】で誘発するのは入れ替わった 2 枠のキャラだけ**（D-134・rules v0.19 §7）。公式の条文で決まる事項で、推測ではない
3. **重なりの下のカードの【切り替え】は誘発させる**（TE-13 が判断を任せた点・推しで閉じた・D-069）。枠ごと位置が変わるし、キャラカードは重ねたすべてのカードのスキルを持つ（§6.3-3）。【登場】の「一番上だけ」に当たる限定は条文に無い
4. **`tests/test_sets.json` は `pc_files` に 1 つ足しただけで、`slow`・`pc_tests` は作り直さなかった**。足した検査は最長 0.26 秒で `slow` に入らないため（D-133 と同じ扱い）。理由は §6-4
5. **PC の再ビルドは後回し**（2026-09-25 マスター裁定）

## 4. 却下した案・撤回した結論

- **★撤回した見立て**: 旧版の引継ぎ書 §2 と、このチャットの最初の報告で「TE-13 を直すと champion の指紋が動き、PC の再ビルドと指紋の貼り替えが要る」と書いた。**誤りだった。**SD001/SD02 には【切り替え】を持つカードが 1 枚も無く（現行プールで持つのは `BP01-008` だけ）、指紋は動かなかった。再ビルドが要るのは Rust を変えたからで、指紋のためではない。**次のチャットは「TE-13 で指紋が動いた」と読まないこと**
- `make_test_sets.py` で `test_sets.json` を丸ごと作り直す案 → 採らなかった（§6-4）
- 旧版から持ち越し（まだ有効）:
  - **V_pf は採らない・V_id が既定**（D-132 追記 4）。乱数 0 番で一度「V_pf を採る」と報告したが、乱数 1・2 番で取り下げた。**「V_pf は乱数 0 番で勝った」を採用の根拠に拾わないこと**
  - 乱数 2 番の V_id（6 本で最強の 0.645）を 1 本だけ段階3 に使う案 → 採らなかった（調整デッキで当たりを選ぶことになる）。ただし「検証の当てはまりが最良の 1 本」を**事前の規則として**選ぶのは、アンサンブルが重すぎたときの控えとして残っている
  - 3 本のネットを Rust に並べて渡す案 → 採らなかった（Rust の変更と再ビルドが要る。幅 3 倍の 1 本で済む）

## 5. 言えないこと・未確認（★次のチャットはここを誤読しやすい）

- **PC の Rust はまだ直す前の挙動である。**対人検証アプリの AI・新アプリの CPU・PC で回す `rs.series` 系は、ショアキーパーのデッキで旧挙動になる。PC の `test_te13_switched_scope.py` の毎手一致は、`meicho_rs.features()` に `te13_switched_scope` が無いので **skip になる**（失敗ではない）
- 現行エンジンでの champion の対 素 planner の勝率は測っていない（旧版から持ち越し。75.2% は旧エンジンの値）
- アンサンブルが V 1 本より強いかは測っていない
- 最終評価の 4 デッキでは何も言えない（開けていない）
- 学習の乱数の揺れ ±0.03 は、この教材・調整デッキ 4 つで見た値である
- 旧版から持ち越し（このチャットでも確かめていない）: 閾値 `th-1-provisional` は仮／環境デッキ 24 種の強さは測っていない／方策ネット（代打ち・π₀・PolicyAgent）は `encode` の第 3 引数（相手のデッキ表）を渡していない／SD02 の「つよい」を素の `planner` に替えるかは未裁定

## 6. 落とし穴（次に同じところで転ばないように）

1. **★挙動の変化を予告する前に、そのタイミングを持つカードがどのデッキに入っているかを数える。**`CHARA_CARDS`/`ACTION_CARDS` の `skills[].timing` を走査すれば 1 行で分かる。これをせずに「指紋が動く」と書いた（§4）
2. **★PC 側のシェル（`device_bash`）はこのチャットでも使えなかった。**作業環境は次の手順で作った。GitHub（`okuson-droid/meicho_engine`・`git clone` はそのまま通る）の 09-16 の commit を取る → PC で更新日時が 09-16 以降のファイルを `device_stage_files` で上書きする。**`cards/` と `engine/results/*/` は GitHub に無い**ので PC から取る（カードのデータ類・`results/models` のネット約 35 本・`results/decksim`）。環境デッキ `decklists/env/` は `experiments/make_env_decks.py` で作り直せる（乱数なし・サイズ一致を確認した）。**文書類（`GENERALIST_AI_REVIEW_D086.md` など）は GitHub の写しが古い**ので、読むときは PC から取る
3. **★torch は `download.pytorch.org` が組織の設定で遮られている。**`pip install torch --break-system-packages`（PyPI）なら入る（2.14.0・約 2 分）
4. **★`scripts/make_test_sets.py` を、資材の揃い方が違う作業環境で打たない。**このチャットで試しに打つと、`pc_tests` が 50 件 → 9 件に減った（作業環境に資材があるぶん PC に回す検査が消える）。検査を足しても 20 秒未満なら `pc_files` を手で足すだけにする
5. **★bash の heredoc は必ず引用符付き（`<<'E'`）で書く。**引用符なしで書いたら、文中のバッククォート（`` `…` ``）がコマンドとして実行され、台帳の文面が壊れた（書き戻す前に気づいて作り直した）
6. `meicho/engine.py`・`rust/src/engine.rs`・`rust/src/lib.rs`・`TASKS.md`・`decisions.md`・`TO_APP.md`・`WRITELOG_ENGINE.md` は **CRLF**。`rules_draft.md`・`version.py`・`tests/*.py`・`test_sets.json`・`CLAUDE.md`・依頼書・引継ぎ書は **LF**。書き換えは Python で `\r\n` を保って行った
7. 旧版から持ち越し（まだ有効）: V の比較を乱数 1 本でやらない／`experiments/drl_train.py` は CRLF／`test_d065.py::test_distil_makes_the_student_agree_with_the_teacher` は前から壊れている（自分の変更のせいと誤読しない）／100 万決定の記録を読むとメモリを超える（`read_records` は 2 回読み）／`seed_bands.json` は末尾に改行が無い／再ステージで作業用の写しが上書きされる・書き戻したら必ずバイト比較

## 7. 再開に必要な外部資源

- **PC のフォルダ**（`C:\Users\奥村優斗\OneDrive\ドキュメント\eclipse_workフォルダ\meicho_engine_v0.1`）への接続。`device_bash` は無い前提で動くこと
- **揮発するもの（次のチャットには残っていない）**: 作業環境の engine 一式（§6-2 の手順で作り直す）・Linux 版の `meicho_rs` wheel（`rust/` で `maturin build --release --out dist`・約 45 秒）・torch・**D-131 の記録本体 .bin**（manifest の `regenerate` 行から作り直す・約 17 分。ただし §2 の判断しだいで取り直しになる）・アンサンブル `s2v_id_ens3.json`（約 39 MB・部品 3 本から `python3 experiments/ensemble_net.py --out results/models/s2v_id_ens3.json results/models/s2v_id_s0.json results/models/s2v_id_s1.json results/models/s2v_id_s2.json` で作り直す・sha 先頭 `2d4504e2125bc244`）
- **PC に残したもの**: このチャットで書き戻した 13 ファイル（D-134 の直し・検査・rules v0.19・台帳・依頼書・控え。すべてバイト比較済み）。前のチャットのネット `results/models/s2v_{id,pf}_s{0,1,2}.json` と `.meta.json`・結果 `results/drl/s2_*`・`results/decksim/*`
- 作業環境は 2 コア。段階3 の反復（約 10,000 局 × 最大 3 反復・葉が 3〜4 倍重い）は作業環境だと長いので、Kaggle を使うかを設計で決める
